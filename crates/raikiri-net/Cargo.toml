[package]
name = "raikiri-net"
version = "0.1.0"
description = "NetworkProvider / ReplacedResolver base implementations for raikiri (FileNetworkProvider, ImageResolver<N>). Sandboxed*/DenyAllPolicy/DefaultSandboxPolicy wrappers are a separate, not-yet-implemented follow-up."
edition.workspace = true
rust-version.workspace = true
license.workspace = true

[dependencies]
data-url       = { workspace = true }
image          = { workspace = true }
raikiri-traits = { workspace = true }
url            = { workspace = true }

[dev-dependencies]
png      = { workspace = true }
tempfile = { workspace = true }

[lints]
workspace = true
