//! Validate expectations files (spec §12.10 line 3101).
//!
//! M1 stub. M3 populates malformed / duplicate / conflicting / expired
//! detection and wires this into CI.

fn main() {
    eprintln!(
        "validate-expectations: M3 will populate malformed / duplicate / conflicting / expired checks."
    );
}
