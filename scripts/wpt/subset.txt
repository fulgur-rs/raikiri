# Stable sparse roots for the one WPT checkout shared by all task worktrees.
# `fetch.sh` applies these patterns to the shared checkout, so do not narrow
# this list or add a one-task path: another branch could then hide test files
# already visible through sibling worktree symlinks.
# Keep the complete CSS tree plus ACID plus its shared font/image fixtures materialized.
acid
css
fonts
images
# /resources is anchored (leading slash) so it matches only the top-level
# resources/ directory, not the thousands of per-test resources/ helper
# directories nested throughout the WPT tree. It carries the real
# testharness.js, testharnessreport.js, check-layout-th.js, and
# testdriver*.js so JS-driven WPT pages can load their scripts from files.
/resources
# Upstream wptrunner, wptserve, and the root CLI used by run-raikiri.sh.
/tools
/wpt
# The root CLI eagerly reads every command registry, including docs/commands.json.
/docs/commands.json
