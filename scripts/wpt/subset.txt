# WPT subset needed for raikiri VRT tests. Keep minimal — current implementation scope only
# needs bundled fonts for cross-machine determinism.
# Future test subsets (css/css-page, etc.) added under separate changes.
fonts
css/selectors/parsing
css/css-text/parsing
css/css-text
css/css-values

# remaining 19 parsing categories (goal G 2026-09-14)
css/compositing/parsing
css/css-color/parsing
css/css-backgrounds
css/css-gaps/parsing
css/css-images/parsing
css/css-lists/parsing
css/css-masking/parsing
css/css-overscroll-behavior/parsing
# CSS Paged Media category sweep: parser, page-box, margin-box, and pagination
# candidates are needed for the css-page goal. The runner still filters to
# files with reftest links, so non-reftest parsing cases remain cheap to carry.
css/css-page
# CSS Fragmentation category sweep for break-before/after/inside and page flow.
css/css-break
css/css-pseudo/parsing
css/css-rhythm/parsing
css/css-scroll-anchoring/parsing
css/css-size-adjust/parsing
css/css-sizing/parsing
css/css-transitions/parsing
css/css-view-transitions/parsing
css/css-will-change/parsing
css/css-writing-modes/parsing
css/css-writing-modes
css/cssom-view/parsing
css/filter-effects/parsing
css/css-text-decor/parsing
css/css-flexbox/parsing
css/css-flexbox
css/css-grid/parsing
css/css-grid

# css-tables reftest sweep (goal: tables PASS expansion, 2026-09-15).
# css-tables holds the tests; css/reference holds the generic green-square
# refs (ref-filled-green-100px-square*.html/xht) that ~half the tables
# reftests link via ../reference/. Without css/reference those pairs are
# undiscoverable (MissingReference) and silently skipped from sweeps.
css/css-tables
css/reference

# <img> replaced-element sizing reftests (2026-09-17, the earlier change).
# All 9 files use .svg fixtures for precise intrinsic-size/ratio control;
# quarantined in expectations/known-issues.txt (PNG-only scope). Fetched
# anyway so the runner sees and correctly skips them rather than silently
# missing the whole category.
css/CSS2/visudet

# css-fonts reftest sweep (@font-face goal the earlier change).
# expectations/tracked-wpt.txt already lists css/css-fonts/ as P1; the fetch
# subset was lagging behind (only css/css-fonts/support/ was present via the
# `fonts` entry's surroundings). The runner filters to files with reftest
# links, so non-reftest cases (parsing/, descriptors/) remain cheap to carry.
css/css-fonts
