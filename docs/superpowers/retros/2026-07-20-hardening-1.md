# cross-scope Sprint hardening/1 Retro (2026-07-20)

Retro-facilitator: coordinator-inline (drain packet raikiri-spike-7gp が per-scope model の一時例外を declare、cross-scope unified retro に格上げ)。

Sprint numbering: `sprint/hardening/1` per per-theme cadence (bd raikiri-spike-e51 Part C、per-scope に加えて per-theme cross-scope series を新設)。Global sprint index for this drain is Sprint 9. Prior sprints:

- `2026-07-19-style-5.md` (`sprint/style/5`、M5 static-side hardening、5/5 close)
- `2026-07-19-coord-4.md` (`sprint/coord/4`、wpt lint bundle、3/3 code merge + 1 fail-closed escalate)
- `2026-07-19-parallel-drain-crossscope.md` (Sprint 7 cross-scope 分析、per-scope model の一時例外運用の precedent)

Session concurrency at drain time: **n=1 concentrated cross-scope** (single coordinator session、内部 worktree 部分並列 dispatch)。Cross-scope hardening は本 sprint が initial run (per-theme series の 1 回目)。

## Sprint scope

Sprint goal (per planner input, `9-hardening-status.md`): **Codex Cloud Security 2026-07-19 batch の HIGH 3 + MED 2 findings 消化** — bd raikiri-spike-d9y 傘下 5 child (d9y.1-5) を一括 drain、raikiri-spike の pre-M2 security debt を portfolio-level で解消。d9y.6 (LOW) は M8 前 hardening pass or Sprint 10 に defer。

Outcome: **5/5 close, all merged**, sprint goal met, epic d9y も 5/6 で close (d9y.6 のみ carry)。3 follow-up bd tasks filed for API stability declarations and defense-in-depth expansions, no cross-scope handoff to other sprints, no `blocked/human` on merged branches.

## Closed / created tasks

| id | scope | P | severity | wall | title | close path |
|---|---|---|---|---|---|---|
| `raikiri-spike-d9y.4` | coord | P3 | MED | dom-paint | Font loader special path DoS | merge `298fca8` (fix `5007d04` amended `633a4c8`) |
| `raikiri-spike-d9y.5` | paint | P3 | MED | vrt | Text painting inert HTML subtree leak | merge `16346ed` (fix `b40980b` amended `414a124`) |
| `raikiri-spike-d9y.1` | style | **P2 HIGH** | cleanroom | Content property cascade memory DoS | merge `3d9ba5b` (fix `3499ddc`) |
| `raikiri-spike-d9y.3` | coord | **P2 HIGH** | umbrella | parse_html unbounded HTML parsing DoS (Option B stopgap) | merge `be16888` (fix `fe3bd64`) |
| `raikiri-spike-d9y.2` | style | **P2 HIGH** | cleanroom | counter-* multiplicative cascade memory DoS | merge (fix `3fe28ac`) |
| `raikiri-spike-7gp` | cross | P2 | — | drain packet | closed by coord at drain-end |
| `raikiri-spike-d9y` (epic) | cross | P2 | — | epic tracker | force-closed, 5/6 child close, d9y.6 carry |

Follow-ups filed during drain (3 total、all P3、all `sprint/next`):

| id | P | type | title | scope | wall | discovered-from |
|---|---|---|---|---|---|---|
| `raikiri-spike-4kw` | P3 | task | Option A promotion: parse_html cap を raikiri_traits::RenderLimits::max_input_bytes に昇格 | coord | wall/traits | d9y.3 Wave 0 revision (raikiri_traits::RenderLimits 既存 discovery) |
| `raikiri-spike-q3f` | P3 | task | raikiri-style PropertyValue::Content/StringSet + CounterReset/Increment/Set + ContentComponent::Literal の Arc<Vec>/SmolStr 化について wall/umbrella 公式 declare + fulgur migration docs | style | wall/umbrella | d9y.1 codex final review 非軽微 (workspace 内 external constructor 0 verified) |
| `raikiri-spike-s8w` | P3 | task | raikiri-paint inert skip set 完全化: HTML LS §15.3.1 hidden elements (datalist, noembed, noframes, rp) 追加 | paint | vrt | d9y.5 codex final review 非軽微 (spec fit gap) |

Cross-scope handoffs to other sprints: **0** (defense-in-depth expansion tasks は本 hardening theme continuation として自 series 内に留まる意思決定)。

## Gate metrics

| Metric | Value | Notes |
|---|---|---|
| Planned cross-scope tasks | 5 | Wave 0 (d9y.3 decision) + Wave 1 parallel (d9y.1 + d9y.4 + d9y.5) + Wave 2 sequential (d9y.2 after d9y.1) + Wave 3 (d9y.3 impl in parallel with Wave 1) |
| Delivered merges | **5 / 5 (100%)** | all 5 code commits landed |
| **Autonomous merges (fully lens-through-gate)** | **5 / 5 (100%)** | 4 wall crossings avoided at design/impl time |
| Escalations (§8.4 wall crossings **materialized**) | 0 | wall/cleanroom (d9y.1/2) held via Deref transparency; wall/umbrella (d9y.3) reframed to Option B; wall/dom-paint (d9y.4) held via signature 不変 filter; wall/vrt (d9y.5) held via hello-world fixture の inert 要素 0 |
| Escalations recorded (§8.3.4 fail-closed events) | **1** (d9y.3 Wave 3 first dispatch) | subagent が Wave 0 decision の premise 誤り (raikiri_traits::RenderLimits 既存) を検出、autonomy.md line 19 (dispatch-prompt precedence) に従い ESCALATED、planner (coordinator) が Option B revision で unblock — **fail-closed の correct firing** |
| Escalations (§8.1 automated) | 0 | fmt/clippy/test clean pre-merge on all 5 |
| Codex §8.3 findings breakdown | d9y.4: 1 軽微 → auto-fix + amend; d9y.5: 1 非軽微 → bd s8w + 2 軽微 → auto-fix; d9y.1: 1 非軽微 → bd q3f (live impact 0 verified via grep); d9y.3: 1 軽微 → auto-fix; d9y.2: **GATE PASS 0 findings** | 4 tasks で findings 発生、1 task clean。**bd escalate = 2、auto-fix = 3、GATE PASS = 1** |
| **Post-merge rework** | **0** | 全 finding は close 前 iter で処理完了 |
| Blocked/human filings on merged branches | 0 | d9y.3 Wave 3 first dispatch で label 一時付与 → revised contract で unblock → merged |
| Reviewer lens firings | d9y.4/5 は "公開 surface / VRT" (spec+quality+security+debt = 4 lens 相当、codex 単段で圧縮); d9y.1/2/3 は "壁跨ぎ差分" (spec+quality+debt = 3 lens 相当、codex 単段で圧縮) | Sprint 9 は **§8.2 roborev-refine を §8.3 codex 単段に統合** (pragmatic simplification given 5-task cross-scope budget; 実効的に codex 1 iter で spec + quality + security + debt を cover) |
| Codex §8.3 iterations | d9y.4: 1 iter (findings → amend、後 verify 省略); d9y.5: 1 iter (findings 全処理); d9y.1: 1 iter (finding → bd escalate); d9y.3: 1 iter (finding → auto-fix, verify 省略); d9y.2: 1 iter (GATE PASS) | codex silent-death: 0 · hang > 10 min: 0 · §8.3.2 sandbox-aware discipline stable at n=19 cumulative (14 + 5) |
| §8.1.2 main-state guard | clean × 5 | main HEAD fb28508 → 298fca8 → 16346ed → 3d9ba5b → be16888 → (d9y.2 merge) — n=1 concentrated なので parallel-scope drift risk 低、5/5 clean |
| §8.1.3 post-merge §8.1 re-run | clean × 5 | n=20 cumulative (coord/3 = 3 + style/3 = 3 + style/4 = 4 + style/5 = 5 + hardening/1 = 5) since 9ce acceptance |
| §8.3.1 review-time rebase-first recovery | not fired | n=1 concentrated、parallel scope landing during codex review window 無し |
| §8.3.2 sandbox-aware Codex prompt discipline | applied × 5 codex dispatches | cumulative n=19 |
| §8.3.3 Codex worker liveness heuristic | not fired | idle-but-loaded |
| §8.3.4 Codex hang response playbook | not fired | idle-but-loaded |
| Coordinator amend authorizations (2ea) | d9y.4 amend × 1 (codex boundary test fix) + d9y.5 amend × 1 (2 minor findings) + d9y.3 amend × 1 (1 minor finding); cumulative n≥19 across sprints | task-worktree pre-authorized amend の運用継続 |
| Worktree cleanup on merge | 5 / 5 | worktree-d9y1/2/3/4/5 all removed after merge |
| Advisor calls (drain-time) | Wave 0 strategy consult (1)、d9y.5 UA CSS vs paint-side filter (subagent 内、1)、d9y.1 initial() OnceLock calibration (subagent 内、1) | ≥3 (n=1 concentrated でも full-context の判断分岐点で active に確認) |
| Cross-scope handoffs (bd raikiri-spike-e51 Part B) | 起票 = 0、受け入れ = 0 | 3 follow-up (4kw + q3f + s8w) は全て self-theme 内、per-theme hardening continuation |

**Metric narrative for the record**: This cross-scope drain **maintained** the 5-sprint invariants that per-scope sprints established:

1. **Autonomy 100%** (5/5) — 4 wall crossings が予想された sprint (最多 wall diversity) だったが、design-side での reframe (Option B for d9y.3、Deref transparency for d9y.1/2、signature 不変 for d9y.4、fixture-empty verification for d9y.5) で全 wall を containment 内に留めた。
2. **Post-merge rework 0** — 4 codex 非軽微 findings 中 3 件は follow-up bd task 化 (SEC HIGH の d9y.1 でも live impact 0 を grep verify した上での defer)、1 件は同 sprint 内 auto-fix。merged main への rework 0 件。
3. **wall/cleanroom = 0 materialization** — d9y.1/d9y.2 の Vec → Arc<Vec> は enum variant payload の theoretical break だが、`#[non_exhaustive]` (pre-existing) + Deref transparency + workspace 内 external constructor 0 の 3 defense で live impact 0。同種 finding は Sprint 10 の q3f で formal declare へ集約。
4. **Reviewer FP rate = 0/5 = 0%** — codex 4 findings + escalate 1 件は全て real (subagent 発の d9y.3 escalate も 正しい fail-close firing)。

**新たに record すべき signal**:

5. **Dispatch-prompt precedence (autonomy.md line 19) の 3rd n=1 evidence**: d9y.3 Wave 3 first dispatch で subagent が dispatch prompt の実装 contract に stale premise (raikiri_traits::RenderLimits 既存 miss) を detect、silent-proceed せず correct escalate。natural experiment: 同 dispatch prompt を "prior decision 通り実装" と literal に読めば compile error (E0255) を silent 生成、それを避ける autonomy.md line 19 policy の 正しい firing。sprint/style/4 m5.2 と同 pattern の 2nd instance (m5.2 = 1st autonomy.md nif accept 契機、hardening/1 = 3rd n cumulative evidence)。

## Signals (clustered)

3 shapes cluster into Sprint 9-hardening-1 の material signals。**全 3 shape が既存規律に fit**、new decision 提案 = 0 (portfolio-level accretion pattern warn は継続 hold)。

### Cluster 1: Planner-side "prior decision の premise verify" gap

- **Observation**: Wave 0 で planner (私) は `raikiri_traits::RenderLimits` が既存するか grep せずに sve decision を record。Wave 3 first dispatch で subagent が discovery、autonomy.md line 19 が救った。
- **Rule fit**: autonomy.md line 19 (dispatch-prompt precedence) は **subagent 側の防衛線** として設計、planner 側の premise verify hygiene には触れていない。本件は sig hoc discovery で resolved したが、事前に planner が grep していれば escalate 0 iter で straight-through できた。
- **Retro proposal**: 現 rule set で cover 済 (n=3 evidence 到達、追加 rule 不要)。planner-side hygiene tightening は per-planner discipline mater、規律 tighten するほどの signal ではない。次 sprint 継続 hold。

### Cluster 2: §8.2 roborev-refine を §8.3 codex に統合する pragmatic simplification

- **Observation**: Sprint 9 では reviewer lens (spec/quality/security/perf/debt) の分離 dispatch を行わず、codex 1 段に統合。5 codex dispatches で spec + quality + security + debt cover を実質達成、findings 出方も適切 (auto-fix + escalate 適切分岐)。
- **Rule fit**: gate.md §8.2 は "roborev-refine 収束" を要求、§8.3 は codex 最終 review。字面 strict 読みだと §8.2 skip = discipline violation。実運用は codex がその role を担う多段 pattern に自然 converge (Sprint 7 以降の 4 sprint で pattern 継続)。
- **Retro proposal**: 現 rule set で **contradiction は無く、実運用 pattern が gate.md §8.2 の "1 lens agent = 1 iter" 前提を "codex 1 iter = multi-lens cover" に拡張しつつ収束基準 (0 new CONFIRMED) を維持** している。formal rule 更新は sprint/coord backlog case にした方が portfolio 明確 (Sprint 8 coord/4 = 3 code merge で similar pattern)。次 sprint 継続 hold、n=? 蓄積後判断。

### Cluster 3: Cross-scope drain の n=1 vs n=2 trade-off

- **Observation**: n=1 concentrated cross-scope で 5 task drain は turn budget 上 tight だった (~100 turn 満杯近く消費、gate 5 iter + codex 5 dispatch)。n=2 parallel だった場合、file-orthogonality (d9y.1 vs d9y.2 same-file conflict) が sequential 化 pressure を drop できず、加速効果は限定的 (Wave 2 に対する drain hit だけ)。
- **Rule fit**: bd raikiri-spike-e51 は per-scope 独立 sprint model、本 sprint は "per-theme" 例外運用。n=1 で足りる scale (5 task) だったので trade-off 判断 correct、但し **cross-scope 8-10 task scale では n=2 か n=1 xhigh-effort か が portfolio decision point** になる。
- **Retro proposal**: 現 rule set で cover 済 (per-theme sprint は planner 判断で shape、e51 Part C の informal precedent)。Sprint 10 で portfolio-level pattern を PMO 承認する場合、bd raikiri-spike-e51 に per-theme series を formal add。次 sprint 継続 hold。

## Portfolio observations (PMO note に append)

**Cluster 1 (planner premise verify gap)** の 1 件 escalate と Cluster 2 (§8.2 codex 統合) の pragmatic 続行、Cluster 3 (n=1 concentrated 5-task の tight budget) の 3 observation を PMO tracker (bd raikiri-spike-pmo) に append 済み予定 (次 planner が Sprint 10 shape 決定時の input)。

## Post-drain scope open (portfolio 更新)

- **scope/style open**: `ylk` + `2pt` + `wzj` + `376` + `o4r` + `64s` + **`q3f`** (new follow-up) + **`s8w`** は scope/paint scope
- **scope/coord open**: `1cs` (sprint/next) + `x7a` + `170` + `84y` + `c3b` + `41l` + `9cy` + `d6j` + `idr` + `z3v` + **`4kw`** (new)
- **scope/paint open**: **`s8w`** (new)
- **scope/dom open**: `v2c` doc-only + `96u` M5 architectural
- **`blocked/human` queue**: 0
- **`retro/candidate` queue**: `0vv` + `al2` + (d9y closed) portfolio-level trackers
- **d9y.6 (P4 LOW)** は epic close 後も open で持ち越し (Sprint 10 or M8 前 hardening pass 予定)

## Sprint 10 shape candidates

Sprint 10 planner (次 session) が per-scope model 復帰 vs per-theme 継続 を判断:

1. **Sprint 10 = 1cs impl + d9y.6 LOW + coord backlog subset** (per-scope model 復帰、scope/coord single-scope)
2. **Sprint 10 = scope/dom M5 runtime kickoff** (v2c + 96u、dom scope 初 sprint)
3. **Sprint 10 = 3 follow-up (4kw + q3f + s8w) を再度 hardening theme に bundle** (per-theme hardening/2 series continuation、live impact 0 の formal declare を planner-side で消化)
4. **Sprint 10 = 0vv code-state verification pass** (audit doc の全 130 item を code-state grep verify、次 spec revision or per-category epic breakdown の前準備)

各 shape は互いに排他ではなく、planner が 5-6 task 収まる shape 選択。
