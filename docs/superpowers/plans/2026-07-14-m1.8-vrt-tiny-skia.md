# raikiri-vrt m1.8 — VRT harness thin wrapper Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** raikiri-vrt crate に `rasterize` (anyrender backend 越しに RGBA バッファ生成) と `encode_png` (tiny-skia で PNG bytes 生成) の 2 public fn を実装し、byte-identical baseline を検証する 3 unit test を通す。

**Architecture:** backend-agnostic な thin wrapper。`rasterize<R: ImageRenderer>` は anyrender の `ImageRenderer::render_to_vec` を呼ぶ 5 行のラッパ、`encode_png` は `tiny_skia::Pixmap::from_vec` + `encode_png` の 5-10 行ラッパ。合計 ≤ 150 行の single `src/lib.rs`。

**Tech Stack:** Rust 1.88 (workspace MSRV) / anyrender 0.11 / anyrender_vello_cpu 0.14 (multithreading) / tiny-skia 0.11 / peniko 0.6 / kurbo 0.13

**Related design:** `bd show raikiri-spike-m1.8` (design + acceptance criteria フィールド)、設計仕様書 §12.7 (Reference documents / T1) §12.8 (Byte-identical scope)。

**Related spike:** `crates/raikiri-feasibility/src/anyrender_byte_identical.rs` (nzv.7、削除は m1.16)。

## Global Constraints

- Rust edition/rust-version は workspace 継承 (`edition.workspace = true` / `rust-version.workspace = true`)
- `[lints] workspace = true` を維持
- 全 pub item に doc comment (workspace lint `#![deny(missing_docs)]` 想定)
- `src/lib.rs` ≤ 150 行 (テスト含む)
- panic on invariant violation、Result 型を導入しない
- backend 非依存: production signature に `anyrender_vello_cpu` を named-mention しない
- Cargo.toml `[workspace.dependencies]` は変更しない (すべて既宣言)

---

## File Structure

**Modify:**
- `crates/raikiri-vrt/Cargo.toml` — deps 整理
- `crates/raikiri-vrt/src/lib.rs` — stub を実装で置換

**No other files change.** workspace `Cargo.toml` / 他 crate は無変更。

---

## Task 1: Cargo.toml refresh + crate-level doc

**Files:**
- Modify: `crates/raikiri-vrt/Cargo.toml`
- Modify: `crates/raikiri-vrt/src/lib.rs`

**Interfaces:**
- Consumes: workspace deps (`anyrender`, `tiny-skia`, `anyrender_vello_cpu`, `peniko`, `kurbo`) — all 既宣言
- Produces: build-green stub with new dep set — Task 2 が `rasterize` 追加時に anyrender/anyrender_vello_cpu を import

- [ ] **Step 1: Cargo.toml を書き換え**

`crates/raikiri-vrt/Cargo.toml` を以下に書き換え:

```toml
[package]
name = "raikiri-vrt"
version = "0.1.0"
description = "VRT (visual regression testing) harness for raikiri: anyrender rasterizer + tiny-skia PNG encoder"
edition.workspace = true
rust-version.workspace = true
license.workspace = true
publish = false

[dependencies]
anyrender = { workspace = true }
tiny-skia = { workspace = true }

[dev-dependencies]
anyrender_vello_cpu = { workspace = true }
peniko              = { workspace = true }
kurbo               = { workspace = true }

[lints]
workspace = true
```

削除: `raikiri` dep、`insta` dev-dep。
追加: `anyrender` runtime dep、`peniko`/`kurbo` dev-dep。
移動: `anyrender_vello_cpu` を runtime → dev-dep。

- [ ] **Step 2: crate-level doc を更新**

`crates/raikiri-vrt/src/lib.rs` を以下に書き換え (実装は Task 2/3 で追加):

```rust
//! raikiri-vrt — VRT harness (backend-agnostic anyrender rasterizer + tiny-skia PNG encoder).
//!
//! Thin wrapper providing the last-mile of the M1 render pipeline:
//!   `anyrender::PaintScene` write → RGBA8 buffer → PNG bytes.
//!
//! Backend: caller supplies any `anyrender::ImageRenderer` (M1 default =
//! `anyrender_vello_cpu`). Signature is generic over `R: ImageRenderer` so
//! future GPU backends can be swapped without API break.
//!
//! Downstream consumers:
//! - `raikiri` umbrella `html_to_png` (M1 end-to-end pipeline)
//! - hello-world VRT (m1.14)
//! - determinism test (m1.13)
//! - rayon thread-count test (m1.18)
```

- [ ] **Step 3: build 確認**

Run: `cargo build -p raikiri-vrt`
Expected: `Compiling raikiri-vrt v0.1.0` → `Finished`, 0 warnings.

- [ ] **Step 4: downstream 破壊なし確認**

Run: `cargo build --workspace --all-targets`
Expected: green。`raikiri` dep 削除で downstream 破壊があった場合、この時点で失敗する (M1 現時点では raikiri umbrella が raikiri-vrt を dep していないはずなので影響なし想定)。

- [ ] **Step 5: commit**

```bash
git add crates/raikiri-vrt/Cargo.toml crates/raikiri-vrt/src/lib.rs
git commit -m "chore(raikiri-vrt): refresh Cargo.toml deps for m1.8 (drop raikiri/insta, add anyrender/peniko/kurbo)"
```

---

## Task 2: `rasterize` public fn + tests

**Files:**
- Modify: `crates/raikiri-vrt/src/lib.rs`

**Interfaces:**
- Produces: `pub fn rasterize<R: anyrender::ImageRenderer>(renderer: &mut R, paint: impl FnOnce(&mut R::ScenePainter)) -> Vec<u8>`
  - Task 3 が同じ file で `encode_png` を追加、test module から両方使用

- [ ] **Step 1: 失敗する test を追加**

`crates/raikiri-vrt/src/lib.rs` の末尾に以下を追加:

```rust
#[cfg(test)]
mod tests {
    use anyrender::PaintScene;
    use anyrender_vello_cpu::VelloCpuImageRenderer;
    use kurbo::{Affine, Rect};
    use peniko::{Color, Fill, color::palette::css};

    use super::rasterize;

    const W: u32 = 100;
    const H: u32 = 100;

    /// Deliberately trivial scene: a solid axis-aligned red rectangle.
    /// If this is not deterministic then nothing else in the pipeline will be.
    fn draw_red_rect<S: PaintScene>(scene: &mut S) {
        let color: Color = css::RED;
        let rect = Rect::new(10.0, 10.0, 90.0, 90.0);
        scene.fill(Fill::NonZero, Affine::IDENTITY, color, None, &rect);
    }

    #[test]
    fn rasterize_produces_expected_buffer_shape() {
        let mut renderer = VelloCpuImageRenderer::new(W, H);
        let buf = rasterize(&mut renderer, |scene| draw_red_rect(scene));
        assert_eq!(buf.len(), (W as usize) * (H as usize) * 4);
        // Pixel (50, 50) is inside the red rect; must not still be transparent-black.
        let idx = (50 * (W as usize) + 50) * 4;
        assert_ne!(
            &buf[idx..idx + 4],
            &[0, 0, 0, 0],
            "expected non-transparent pixel at (50,50); scene did not render",
        );
    }

    #[test]
    fn rasterize_is_byte_identical() {
        let mut r1 = VelloCpuImageRenderer::new(W, H);
        let mut r2 = VelloCpuImageRenderer::new(W, H);
        let a = rasterize(&mut r1, |scene| draw_red_rect(scene));
        let b = rasterize(&mut r2, |scene| draw_red_rect(scene));
        assert_eq!(a.len(), b.len());
        assert!(
            a == b,
            "independent renderers diverged: {} of {} bytes differ",
            a.iter().zip(&b).filter(|(x, y)| x != y).count(),
            a.len(),
        );
    }
}
```

- [ ] **Step 2: test failure を確認**

Run: `cargo test -p raikiri-vrt`
Expected: `error[E0425]: cannot find function 'rasterize' in module 'super'`(コンパイルエラー、まだ未実装)

- [ ] **Step 3: `rasterize` を実装**

`crates/raikiri-vrt/src/lib.rs` の crate-level doc の直後 (test mod の前) に以下を追加:

```rust

use anyrender::ImageRenderer;

/// Rasterize an `anyrender` scene into an RGBA8 pixel buffer via the supplied
/// backend.
///
/// The `paint` closure receives `&mut R::ScenePainter` and issues draw
/// commands via the `anyrender::PaintScene` trait.
///
/// # Buffer layout
///
/// `width * height * 4` bytes, tightly packed rows, premultiplied RGBA8
/// (the `anyrender_vello_cpu` convention that `encode_png` accepts).
///
/// # Panics
///
/// Panics if the supplied renderer panics — e.g. zero dimensions passed
/// to `VelloCpuImageRenderer::new`. The wrapper adds no additional
/// validation; VRT usage prefers fail-fast over `Result`.
pub fn rasterize<R: ImageRenderer>(
    renderer: &mut R,
    paint: impl FnOnce(&mut R::ScenePainter),
) -> Vec<u8> {
    let mut buf = Vec::new();
    renderer.render_to_vec(paint, &mut buf);
    buf
}
```

- [ ] **Step 4: test pass を確認**

Run: `cargo test -p raikiri-vrt`
Expected: 2 tests passed (`rasterize_produces_expected_buffer_shape`, `rasterize_is_byte_identical`), 0 failed.

もし byte-identical が fail する場合 → nzv.7 で確認済の property なので、rasterize 実装バグを疑う (buf clear 忘れ等)。

- [ ] **Step 5: commit**

```bash
git add crates/raikiri-vrt/src/lib.rs
git commit -m "feat(raikiri-vrt): add rasterize (backend-agnostic anyrender wrapper) + byte-identical test"
```

---

## Task 3: `encode_png` public fn + test

**Files:**
- Modify: `crates/raikiri-vrt/src/lib.rs`

**Interfaces:**
- Consumes: `rasterize` from Task 2 (test 内で RGBA バッファ生成に使用)
- Produces: `pub fn encode_png(rgba: &[u8], width: u32, height: u32) -> Vec<u8>`

- [ ] **Step 1: 失敗する test を追加**

`crates/raikiri-vrt/src/lib.rs` の `mod tests` 内、`use super::rasterize;` を `use super::{encode_png, rasterize};` に変更し、末尾に以下 test を追加:

```rust
    #[test]
    fn encode_png_produces_png_signature() {
        let mut renderer = VelloCpuImageRenderer::new(W, H);
        let rgba = rasterize(&mut renderer, |scene| draw_red_rect(scene));
        let png = encode_png(&rgba, W, H);
        // PNG magic bytes: \x89 P N G \r \n \x1A \n
        assert_eq!(
            &png[..8],
            &[0x89, b'P', b'N', b'G', b'\r', b'\n', 0x1A, b'\n'],
            "PNG magic bytes mismatch — encoder produced non-PNG output",
        );
    }
```

- [ ] **Step 2: test failure を確認**

Run: `cargo test -p raikiri-vrt`
Expected: `error[E0432]: unresolved import 'super::encode_png'` (未実装).

- [ ] **Step 3: `encode_png` を実装**

`crates/raikiri-vrt/src/lib.rs` の `rasterize` の直後 (test mod の前) に以下を追加:

```rust

/// Encode a premultiplied RGBA8 buffer to PNG bytes via `tiny_skia::Pixmap`.
///
/// The buffer must be exactly `width * height * 4` bytes. Buffer format is
/// premultiplied RGBA8 — the `anyrender_vello_cpu` output convention.
/// `tiny_skia` stores pixmaps in the same format, so encoding is a direct
/// wrap-then-serialize.
///
/// # Panics
///
/// - `rgba.len() != width * height * 4`
/// - `width == 0 || height == 0` (invalid `tiny_skia::IntSize`)
/// - PNG serialization failure (tiny-skia never returns an error for a
///   well-formed pixmap in practice; treated as an invariant violation)
pub fn encode_png(rgba: &[u8], width: u32, height: u32) -> Vec<u8> {
    let expected = (width as usize) * (height as usize) * 4;
    assert_eq!(
        rgba.len(),
        expected,
        "encode_png: expected {expected} bytes for {width}x{height}, got {}",
        rgba.len(),
    );
    let size = tiny_skia::IntSize::from_wh(width, height)
        .expect("encode_png: width/height must be > 0");
    let pixmap = tiny_skia::Pixmap::from_vec(rgba.to_vec(), size)
        .expect("encode_png: rgba slice must match width * height * 4");
    pixmap
        .encode_png()
        .expect("encode_png: tiny_skia::Pixmap::encode_png should not fail for a valid pixmap")
}
```

- [ ] **Step 4: test pass を確認**

Run: `cargo test -p raikiri-vrt`
Expected: 3 tests passed (前 2 + `encode_png_produces_png_signature`), 0 failed。

もし PNG signature が fail する場合 → tiny-skia の PNG encoding は well-formed PNG を出すはず、`rgba.to_vec()` の premul convention mismatch は考えにくい (signature test は色ではなく header だけ見る)。

- [ ] **Step 5: commit**

```bash
git add crates/raikiri-vrt/src/lib.rs
git commit -m "feat(raikiri-vrt): add encode_png (tiny-skia Pixmap PNG encoder) + signature test"
```

---

## Task 4: 最終検証 (clippy / fmt / workspace)

**Files:** なし (検証のみ、必要なら fmt/clippy fix)

- [ ] **Step 1: 行数確認**

Run: `wc -l crates/raikiri-vrt/src/lib.rs`
Expected: ≤ 150 行

もし超えた場合、doc comment を短縮 (実装は最小、doc は情報を落とさない範囲で圧縮)。

- [ ] **Step 2: fmt check**

Run: `cargo fmt --check -p raikiri-vrt`
Expected: 0 diff。もし diff があれば `cargo fmt -p raikiri-vrt` で修正。

- [ ] **Step 3: clippy**

Run: `cargo clippy -p raikiri-vrt --all-targets -- -D warnings`
Expected: 0 warnings。よくある失敗:
- `useless_conversion` on `rgba.to_vec()` → `rgba.to_vec()` は `&[u8] → Vec<u8>` の copy で必要、`allow` ではなく現状維持で正当化されるはず。もし clippy が別の suggestion を出せば従う。
- Missing docs on pub fn → Task 2/3 で書いた doc で cover 済のはず。

- [ ] **Step 4: workspace-wide test**

Run: `cargo test --workspace`
Expected: raikiri-vrt の 3 test を含む全 workspace test が pass。

- [ ] **Step 5: workspace-wide build (final)**

Run: `cargo build --workspace --all-targets`
Expected: green。

- [ ] **Step 6: (もし fmt/clippy fix が必要だったら) commit**

```bash
git add crates/raikiri-vrt/src/lib.rs
git commit -m "chore(raikiri-vrt): fmt/clippy fixes for m1.8"
```

fix が不要なら Task 4 は commit 発生しない。

---

## Post-implementation

- [ ] `bd show raikiri-spike-m1.8` の acceptance criteria チェックボックスを bd の notes 経由で更新するか、close 時に verify
- [ ] `superpowers:verification-before-completion` で最終確認
- [ ] `superpowers:finishing-a-development-branch` で merge/PR strategy を決める
- [ ] User approval で `bd close raikiri-spike-m1.8`

## Notes / risk log

- **Premul convention**: 実装時点で vello_cpu 出力が premul か unpremul か未確認。tiny-skia は premul 前提。もし encode_png の色が明らかにおかしい (test 3 は header check だけで検知しない) → Task 3 の docstring と impl を premul 変換込みに修正。ただし visual verify はこの m1.8 では行わない (m1.14 hello-world VRT で発覚する想定)。
- **`draw_red_rect` の generic**: `<S: PaintScene>` の generic fn を closure でラップしてから rasterize に渡す (`|scene| draw_red_rect(scene)`)。直接渡しの inference は Rust バージョン間で挙動差があるため closure 化で安定させる。
- **downstream 破壊 (Task 1 Step 4)**: raikiri umbrella の Cargo.toml が `raikiri-vrt` を dep している場合 M0 stub の insta 依存に基づく可能性があり、insta 削除が umbrella の compile を壊すかは Task 1 Step 4 で判定される。壊す場合は plan を一時停止して user に相談 (このケースは想定外だが hedge)。
