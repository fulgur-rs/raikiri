# m1.10.3 Test-Coverage Gap Fill — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** raikiri-spike-m1.10 final review Minor #3 で報告された `compare_png` dim-mismatch branch と `run_and_compare` page-count panic の 2 untested path を、`crates/raikiri-vrt/tests/reference_harness.rs` への test 追加で verify する (item 3 の TIER2/TIER3 slow-path は M2 defer)。

**Architecture:** test-only change。`src/reference.rs` は無変更。既存 `reference_harness.rs` の pattern (synthetic fixture via `tempfile`, `encode_png` で PNG bytes 合成, `EnvGuard::read()` で `RAIKIRI_UPDATE_GOLDENS` env-set/read race を排他) をそのまま踏襲。

**Tech Stack:** Rust 2024 edition, rust-version 1.89.0, `raikiri-vrt` crate (leaf, dev-only)。既存 dep のみ (`tempfile`, `tiny-skia`, `raikiri_vrt::{encode_png, reference::*}`)。

## Global Constraints

- 対象 crate: `crates/raikiri-vrt` のみ (test file 1 個の修正)
- `src/reference.rs` は無変更 (test-only change; production code path 変更しない)
- 新規 dep 追加なし
- 既存 test 18 件 (9 unit + 9 integration) を regression させない
- `#[non_exhaustive]` FixtureError enum に対する match は既存パターン踏襲 (`other => panic!(...)`)
- `panic::catch_unwind` は `AssertUnwindSafe` で closure を wrap (既存 `test_run_and_compare_writes_artifacts_on_diff` 準拠)
- `EnvGuard::read()` は `RAIKIRI_UPDATE_GOLDENS` を **読み取る** test で必須 (他 test の env-set と leak しないよう)
- rustc / clippy `-D warnings` green 維持
- cargo fmt --check green 維持
- M1 scope は EXACT-only。Tolerance TIER2/TIER3 の end-to-end verify は M2 defer (design 参照)

---

## File Structure

**Modify:**
- `crates/raikiri-vrt/tests/reference_harness.rs` — 末尾に 2 test fn を追加 (~40 行)

**Create:** なし

**Test:** 追加自身が test であるため別途 test fixture 不要。

---

## Task 1: `test_compare_png_dimension_mismatch` — dim-mismatch branch (compare_png:207-217) verify

**Files:**
- Modify: `crates/raikiri-vrt/tests/reference_harness.rs` (末尾に追加)

**Interfaces:**
- Consumes: 既存 import (`raikiri_vrt::encode_png`, `raikiri_vrt::reference::{Tolerance, compare_png}`), 既存 helper `solid([u8;4], u32, u32) -> Vec<u8>`
- Produces: 追加なし (test-only)

**Note on TDD framing:** 本 task は既に実装済み behavior (reference.rs:207-217 dim-mismatch branch) を verify する coverage test。従って test は初回実行で **PASS が期待される**。もし FAIL したら、`compare_png` の dim-mismatch branch に bug 発見の可能性 — plan 停止して investigate。

- [ ] **Step 1: 追加する test を `crates/raikiri-vrt/tests/reference_harness.rs` の末尾 (`test_run_and_compare_writes_artifacts_on_diff` の後) に write**

```rust
#[test]
fn test_compare_png_dimension_mismatch() {
    // compare_png:207-217 branch: actual と expected の decode dim が異なる場合、
    // DiffReport::{width, height} は expected 側の dim を、mismatched_pixel_count
    // は ew * eh を報告する。色を変えているのは EXACT byte-eq shortcut を
    // 偶然通っていないことが視覚的に読めるようにするため (実際には dim check が
    // 先に走るので shortcut に到達しない)。
    let actual_png = encode_png(&solid([255, 0, 0, 255], 10, 10), 10, 10); // 10x10 red
    let expected_png = encode_png(&solid([0, 0, 255, 255], 5, 5), 5, 5); // 5x5 blue

    let err = compare_png(&actual_png, &expected_png, Tolerance::EXACT)
        .expect_err("dim mismatch should be detected");

    assert_eq!(err.width, 5, "width should be expected dim");
    assert_eq!(err.height, 5, "height should be expected dim");
    assert_eq!(err.mismatched_pixel_count, 25, "should equal ew * eh");
    assert!(
        err.first_mismatch.is_none(),
        "dim mismatch has no pixel-level info"
    );
    assert!(err.actual_png_path.is_none(), "compare_png does no I/O");
    assert!(err.diff_png_path.is_none(), "compare_png does no I/O");
    assert_eq!(err.page_index, 0, "compare_png sets page_index to 0");
}
```

- [ ] **Step 2: test を走らせて PASS を確認**

Run: `cargo test -p raikiri-vrt --test reference_harness test_compare_png_dimension_mismatch -- --nocapture`

Expected: `test test_compare_png_dimension_mismatch ... ok`, `1 passed; 0 failed`

もし FAIL したら停止して `src/reference.rs:207-217` を read し、DiffReport field の値が assert と食い違う原因を突き止める (test を assert に合わせるのではなく production code の shape を確認)。

- [ ] **Step 3: 既存 test 全部を走らせて regression なしを確認**

Run: `cargo test -p raikiri-vrt 2>&1 | tail -20`

Expected: `9 passed` (unit) + `10 passed` (integration = 既存 9 + 新 1) + `0 passed` (doc) = 全 green。

- [ ] **Step 4: clippy を pass することを確認**

Run: `cargo clippy -p raikiri-vrt --all-targets -- -D warnings`

Expected: warnings 0、exit code 0。

- [ ] **Step 5: fmt を pass することを確認**

Run: `cargo fmt -p raikiri-vrt -- --check`

Expected: no diff、exit code 0。もし failure したら `cargo fmt -p raikiri-vrt` で修正してから再確認。

- [ ] **Step 6: commit**

```bash
git add crates/raikiri-vrt/tests/reference_harness.rs
git commit -m "$(cat <<'EOF'
test(raikiri-vrt): cover compare_png dimension-mismatch branch (m1.10.3 item 1)

reference.rs:207-217 branch was previously unexercised. Adds
test_compare_png_dimension_mismatch verifying that when actual and expected
PNGs decode to different (w, h), compare_png returns Err(DiffReport) with
expected dims, mismatched_pixel_count = ew*eh, no first_mismatch, no paths.
EOF
)"
```

---

## Task 2: `test_run_and_compare_page_count_mismatch` — page-count panic (run_and_compare:395-402) verify

**Files:**
- Modify: `crates/raikiri-vrt/tests/reference_harness.rs` (末尾に追加)

**Interfaces:**
- Consumes: 既存 import (`raikiri_vrt::reference::{Tolerance, run_and_compare}`), 既存 helper `solid`, `build_fixture`, `tiny_png`, `EnvGuard::read()`, および `std::panic`
- Produces: 追加なし (test-only)

**Note on TDD framing:** Task 1 と同じく既存 behavior verify。初回 PASS が期待される。

**Note on env guard:** `EnvGuard::read()` の hold は `run_and_compare` 内部で `std::env::var(UPDATE_GOLDENS_ENV)` を読み取るため必須 (他 test の set と race し、意図せず update-goldens branch に入ると本 test 意図が消える)。既存 `test_run_and_compare_success_removes_no_files` / `_writes_artifacts_on_diff` と同じ pattern。

- [ ] **Step 1: 追加する test を `crates/raikiri-vrt/tests/reference_harness.rs` の末尾 (Task 1 で追加した test の後) に write**

```rust
#[test]
fn test_run_and_compare_page_count_mismatch() {
    use std::panic;

    // run_and_compare:395-402 branch: pipeline output page count が fixture の
    // expected_pages.len() と一致しないとき、compare_png loop の手前で panic!。
    // 副作用として diff artifacts は書かれない (loop に入らないため)。
    let _guard = EnvGuard::read(); // exclude concurrent env-setter tests

    let png_a = encode_png(&solid([255, 0, 0, 255], 4, 4), 4, 4);
    let png_b = encode_png(&solid([0, 255, 0, 255], 4, 4), 4, 4);
    // Fixture: 1 expected page. Pipeline は 2 page 返す → mismatch。
    let dir = build_fixture(Some(b"<p>hi</p>"), &[(0, png_a.clone())]);

    let fixture_name = dir
        .path()
        .file_name()
        .unwrap()
        .to_str()
        .unwrap()
        .to_string();
    let diff_dir = Path::new(env!("CARGO_MANIFEST_DIR"))
        .join("target")
        .join("reference-diffs")
        .join(&fixture_name);
    let _ = fs::remove_dir_all(&diff_dir);

    let png_a_clone = png_a.clone();
    let png_b_clone = png_b.clone();
    let dir_path = dir.path().to_path_buf();
    let result = panic::catch_unwind(panic::AssertUnwindSafe(|| {
        run_and_compare(&dir_path, Tolerance::EXACT, move |_html| {
            vec![png_a_clone, png_b_clone]
        });
    }));

    let payload = result.expect_err("run_and_compare should panic on page count mismatch");
    // Panic payload は run_and_compare の panic!("...") が生成する String。
    let msg = payload
        .downcast_ref::<String>()
        .map(String::as_str)
        .or_else(|| payload.downcast_ref::<&'static str>().copied())
        .unwrap_or("");
    assert!(
        msg.contains("page count mismatch"),
        "expected 'page count mismatch' in panic payload, got: {msg}"
    );
    assert!(
        msg.contains("expected 1"),
        "expected 'expected 1' in panic payload, got: {msg}"
    );
    assert!(
        msg.contains("actual 2"),
        "expected 'actual 2' in panic payload, got: {msg}"
    );

    // panic は compare_png loop の手前で走るので diff artifacts は生成されない。
    assert!(
        !diff_dir.exists(),
        "page-count panic path must not create diff dir: {}",
        diff_dir.display()
    );
}
```

- [ ] **Step 2: test を走らせて PASS を確認**

Run: `cargo test -p raikiri-vrt --test reference_harness test_run_and_compare_page_count_mismatch -- --nocapture`

Expected: `test test_run_and_compare_page_count_mismatch ... ok`, `1 passed; 0 failed`

もし FAIL したら停止して原因を突き止める:
- panic 文字列 assert がずれた場合 → `src/reference.rs:395-402` の `panic!(...)` format 文字列を read し assert を実測値に合わせる (production code は既に landed 済みで固定)
- `diff_dir.exists()` が true → panic branch より前に compare_png loop に入っている bug 発見の可能性、investigate

- [ ] **Step 3: 既存 test 全部を走らせて regression なしを確認**

Run: `cargo test -p raikiri-vrt 2>&1 | tail -20`

Expected: `9 passed` (unit) + `11 passed` (integration = 既存 9 + Task 1 + Task 2) + `0 passed` (doc) = 全 green。

- [ ] **Step 4: clippy を pass することを確認**

Run: `cargo clippy -p raikiri-vrt --all-targets -- -D warnings`

Expected: warnings 0、exit code 0。

- [ ] **Step 5: fmt を pass することを確認**

Run: `cargo fmt -p raikiri-vrt -- --check`

Expected: no diff、exit code 0。もし failure したら `cargo fmt -p raikiri-vrt` で修正してから再確認。

- [ ] **Step 6: commit**

```bash
git add crates/raikiri-vrt/tests/reference_harness.rs
git commit -m "$(cat <<'EOF'
test(raikiri-vrt): cover run_and_compare page-count mismatch panic (m1.10.3 item 2)

reference.rs:395-402 panic branch was previously unexercised. Adds
test_run_and_compare_page_count_mismatch verifying that when pipeline
output length differs from fixture.expected_pages.len(), run_and_compare
panics with "page count mismatch" before entering the compare loop, and
no diff artifacts are written under target/reference-diffs/.

Closes raikiri-spike-m1.10.3 (item 3 TIER2/TIER3 slow-path deferred to
M2 platform matrix work per issue description).
EOF
)"
```

---

## Task 3: bd acceptance criteria の test-count を実測値に合わせる (housekeeping)

**Files:**
- Modify: 対象なし (bd issue の field 更新のみ)

**Interfaces:**
- Consumes: なし
- Produces: なし

**Note:** design 段階で "既存 12 → 14" と書いたが、実際の baseline は 9 unit + 9 integration = 18 tests。本 task の結果、20 tests になる。acceptance criteria を実測値に合わせる。

- [ ] **Step 1: bd acceptance を実測値に更新**

```bash
bd update raikiri-spike-m1.10.3 --acceptance "$(cat <<'EOF'
1. 2 tests 追加 in `crates/raikiri-vrt/tests/reference_harness.rs`:
   - `test_compare_png_dimension_mismatch` — dim-mismatch branch (compare_png:207-217) を verify
   - `test_run_and_compare_page_count_mismatch` — page-count panic (run_and_compare:395-402) を verify
2. `cargo test -p raikiri-vrt` green (既存 18 = 9 unit + 9 integration + 新 2 = 20 tests total)
3. `cargo clippy -p raikiri-vrt --all-targets -- -D warnings` green
4. `cargo fmt -p raikiri-vrt -- --check` green
5. Item 3 (`Tolerance::TIER2` / `TIER3` slow-path) は **未 verify** — design 中で M2 defer 理由を明記済み
6. `src/reference.rs` は無変更 (test-only change)
EOF
)"
```

Expected: `✓ Updated issue: raikiri-spike-m1.10.3 ...`

- [ ] **Step 2: bd show で反映確認**

Run: `bd show raikiri-spike-m1.10.3 2>&1 | grep -A 2 "18 = 9 unit"`

Expected: 更新後の acceptance 文字列が表示される。

- [ ] **Step 3: plan ファイル自体を commit**

```bash
git add docs/superpowers/plans/2026-07-17-m1.10.3-test-coverage-gap.md
git commit -m "docs(plans): implementation plan for m1.10.3 (test-coverage gap fill)"
```

(bd の field 更新は git commit を伴わない — Dolt DB は git 外に持続する。plan file は git 内なので commit する。)

---

## Self-Review (post-write check)

- **Spec coverage:** design の Scope 表 3 items 全部に対応するか?
  - Item 1 (dim-mismatch) → Task 1 ✓
  - Item 2 (page-count) → Task 2 ✓
  - Item 3 (TIER2/TIER3) → 意図的に scope 外 (design と Global Constraints に明記) ✓
- **Placeholders:** TBD / TODO / "implement later" 等の red flag なし ✓
- **Type consistency:** `Tolerance::EXACT`, `Tolerance`, `compare_png`, `run_and_compare`, `DiffReport::{width, height, mismatched_pixel_count, first_mismatch, actual_png_path, diff_png_path, page_index}`, `EnvGuard::read()`, `build_fixture`, `solid`, `tiny_png`, `encode_png` は全て既存 file (`crates/raikiri-vrt/src/reference.rs`, `tests/reference_harness.rs`) で確認済み ✓
- **Commit messages:** feat/test/docs prefix を既存 log convention (`b48d264 docs(plans):`, `cf8c3a4 refactor(raikiri-vrt):` 等) に合わせる ✓
