# m1.19 m0-drift-spec-revise Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** `docs/feasibility-report.md` §4.2 / §4.3 / §4.4 / §5.4 の 4 件と、recon で発見した §M0 Tasks の MSRV 乖離 1 件を、`docs/superpowers/specs/2026-07-13-raikiri-rebuild-design.md` に反映する docs-only sync PR。

**Architecture:** 単一 markdown ファイルへの 5 edit (§13.0 に drift log 追加 + §M0 内 4 か所に inline annotate + §4 dep 表直下に note 追加)。既存 narrative は削除せず annotate で verify 履歴を保存する保守的な revise。編集後 grep で 5 acceptance marker がヒットすることを確認し、単一 `docs:` commit を作成。

**Tech Stack:**
- Markdown 編集 (Edit tool)
- `grep -nE` for verification (acceptance criteria matcher)
- git (single commit, `docs:` prefix, no `--amend`)

## Global Constraints

- **docs-only PR**: コード変更ゼロ (Cargo.toml / .rs / .toml いずれも touch しない)。
- **単一 commit**: `docs:` 接頭辞、Claude-Session フッター付き。
- **既存 narrative は削除しない**: 撤回対象も annotate 追記のみ (verify 履歴保存)。
- **Feasibility report が authoritative source**: `docs/feasibility-report.md` §4.2 / §4.3 / §4.4 / §5.4 の文言を優先。design doc 側の narrative と齟齬があれば feasibility 側を採用。
- **line 3155 / 3169-3170 / 3173 / 3180 は現 HEAD (worktree base) の line number**: edit 後にずれるので、Edit tool には unique な text marker で `old_string` を渡す。
- **Related beads issue**: `raikiri-spike-m1.19` (status: in_progress、design + acceptance 保存済)。closing は plan 完了後の別ステップ。
- **Related follow-up**: `raikiri-spike-m1.17` (Style: !Send under calc feature の invariant 決定) を (d) の Style !Send caveat 内で参照する。

---

## File Structure

**Modify**:
- `docs/superpowers/specs/2026-07-13-raikiri-rebuild-design.md` (5 edit)

**Create**: なし

**Test**: なし (docs 変更、grep verification のみ)

---

## Task 1: 5 edits の順次適用 → grep 検証 → 単一 commit

**Files:**
- Modify: `docs/superpowers/specs/2026-07-13-raikiri-rebuild-design.md`

**Interfaces:**
- Consumes: `docs/feasibility-report.md` §4.2 / §4.3 / §4.4 / §5.4 (authoritative source)
- Produces: なし (docs update のみ、他 crate の compile は影響を受けない)

**Edit 順序**: 上から下へ (line 数の若い順に applying して、後の edit の anchor がずれない)。順序: (c)+(d) → (a) → (b1) → (b2) → (b3) → 追加(line 3173)。ただし Edit tool は unique text で anchor するので順序依存性は実際には無い; 上→下は human-review friendly のため。

---

### Step 1: 現状 baseline を grep で確認 (acceptance marker が **無い** ことを verify)

Run:
```bash
grep -nE 'Actual drifts recorded|1\.85 → 1\.88 → 1\.89|Crate-local deps|taffy features 明示|nzv\.11 で verify 済' docs/superpowers/specs/2026-07-13-raikiri-rebuild-design.md
```

Expected: no matches (5 marker すべて未追加)。

もし matches がある場合、別セッションで先行実装された可能性 → `git log --all --since='2026-07-14' -- docs/superpowers/specs/` で確認し、以降 step を skip して既存 diff の再点検に切替える。

---

### Step 2: (c) + (d) の edit を §4 dep 表直下に追加

**Anchor**: 現行 line 303 の `**Consumer 目線**: \`use raikiri::*\` だけで足りる...` の直前に insert。

Edit tool の `old_string`:

```
| `parse_html()`, `plan()`, `render_streaming()`, `render_batch()`, `html_to_png()` | raikiri (umbrella) | Consumer |

**Consumer 目線**: `use raikiri::*` だけで足りる。sub-crate 直接依存も可能
(advanced case)。
```

Edit tool の `new_string`:

```
| `parse_html()`, `plan()`, `render_streaming()`, `render_batch()`, `html_to_png()` | raikiri (umbrella) | Consumer |

**Crate-local deps (not workspace-wide)** (feasibility §4.3 対応):

- `precomputed-hash = "0.1"` は raikiri-style crate の **direct dep のみ**、
  workspace scope に上げない。理由: encapsulation policy — stylo-side
  implementation detail (`SelectorImpl` 内部要求) は raikiri-style の外に
  漏らさない (feasibility §2.4 経緯、commit `4a5d2c6`)

**taffy features 明示** (feasibility §4.3 対応):

- 有効 features: `[block_layout, flexbox, grid, content_size, calc, std]`
  (stylo_taffy / blitz-dom と一致)
- **OFF**: `taffy_tree` — raikiri-dom は自前 arena で `LayoutPartialTree` /
  `TraversePartialTree` を trait 直下 impl するため
- **Style: !Send under calc feature caveat**: `Dimension`
  (via `CompactLength`) が `calc()` 有効時に `*const ()` を保持するため
  `Style: !Send`。LayoutBuffer が `Style` を保持する場合の invariant 決定は
  M1 (`raikiri-spike-m1.17`) で対応

**Consumer 目線**: `use raikiri::*` だけで足りる。sub-crate 直接依存も可能
(advanced case)。
```

---

### Step 3: (c) + (d) の marker を grep で verify

Run:
```bash
grep -nE 'Crate-local deps|taffy features 明示' docs/superpowers/specs/2026-07-13-raikiri-rebuild-design.md
```

Expected:
- 2 hits (2 marker それぞれ 1 hit)
- Line numbers は 305 付近 (元 303 + 2 line 上に insert したので)

---

### Step 4: (a) §13.0 drift log を Compression trigger の直後に追加

**Anchor**: 現行 line 3129-3131 の Compression trigger 段落の直後、`### M0: Dep feasibility + production workspace` ヘッダ (line 3133) の直前。

Edit tool の `old_string`:

```
**Compression trigger**: M0-M2 で drift fix commit が 5 つを超える場合、spec を
"§4 型定義 + §13 milestone + §16 用語集" に圧縮し、narrative は
`docs/superpowers/rationale.md` に分離する (compression PR は独立 review)

### M0: Dep feasibility + production workspace (round 3 review 対応)
```

Edit tool の `new_string`:

```
**Compression trigger**: M0-M2 で drift fix commit が 5 つを超える場合、spec を
"§4 型定義 + §13 milestone + §16 用語集" に圧縮し、narrative は
`docs/superpowers/rationale.md` に分離する (compression PR は独立 review)

**Actual drifts recorded (M0 期間中)**:

- **MSRV drift**: `1.85 → 1.88 → 1.89`
  - 1.85 → 1.88: nzv.3 Task 4、parley 0.10 要件 (commit `edce05d`)
  - 1.88 → 1.89: blitz oracle dev-deps 揃え (commit `23b5343`)
  - §M0 Tasks の "MSRV = 1.85" 記述は歴史的初期値、実 toolchain は 1.89
- **nzv.8 verdict 遷移**: `NEEDS_DESIGN_CHANGE → OK`。fix は `precomputed-hash` を
  raikiri-style crate-local に encapsulate (§4 Crate-local deps note 参照)
- **raikiri-style の M0 seed 化**: 元は "M0 stub、M1 で populate" だったが、
  nzv.8 fix で SelectorImpl seed を M0 内に追加 (commit `4a5d2c6`)
- **nzv.11 refute**: 「PaintScene と blitz-paint 別 shape → bridge trait 必要」
  仮説は refute。§M0 検証項目リストの該当行 inline annotate 済

### M0: Dep feasibility + production workspace (round 3 review 対応)
```

---

### Step 5: (a) の marker を grep で verify

Run:
```bash
grep -nE 'Actual drifts recorded|1\.85 → 1\.88 → 1\.89' docs/superpowers/specs/2026-07-13-raikiri-rebuild-design.md
```

Expected:
- `Actual drifts recorded` — 1 hit
- `1.85 → 1.88 → 1.89` — 1 hit

---

### Step 6: (b1) §M0 検証項目リストの anyrender PaintScene 適合面行に inline annotate

**Anchor**: `**anyrender PaintScene 適合面** ... で verify` のブロック。

Edit tool の `old_string`:

```
- **anyrender PaintScene 適合面** (round 3 #4 で明示 task 化): blitz-paint と
  shape 一致するか、adapter compile-spike で verify
```

Edit tool の `new_string`:

```
- **anyrender PaintScene 適合面** (round 3 #4 で明示 task 化): blitz-paint と
  shape 一致するか、adapter compile-spike で verify
  → **(nzv.11 で verify 済、bridge trait 不要と確定。§13.0 drift log 参照)**
```

---

### Step 7: (b2) M0 内部 DAG の feasibility-paintscene-adapter-compile-spike 行に inline annotate

**Anchor**: `- feasibility-paintscene-adapter-compile-spike (round 3 #4)` (M0 内部 DAG list 内)。

Edit tool の `old_string`:

```
   - feasibility-selectors-cssparser-version-compat (round 3 #4)
   - feasibility-paintscene-adapter-compile-spike (round 3 #4)
```

Edit tool の `new_string`:

```
   - feasibility-selectors-cssparser-version-compat (round 3 #4)
   - feasibility-paintscene-adapter-compile-spike (round 3 #4、nzv.11 で verify 済)
```

---

### Step 8: (b3) §M0 "Tasks (依存順)" の feasibility-paintscene-adapter-compile-spike 行に inline annotate

**Anchor**: Tasks 依存順リスト内の multiline 列挙。unique match のため 2 行 context 込みで anchor。

Edit tool の `old_string`:

```
- feasibility-parley, feasibility-taffy, feasibility-anyrender-vello-cpu,
  feasibility-selectors, feasibility-cssparser,
  feasibility-selectors-cssparser-version-compat,
  feasibility-paintscene-adapter-compile-spike
- feasibility-report (`docs/feasibility-report.md` に全結果、API surface 記録)
```

Edit tool の `new_string`:

```
- feasibility-parley, feasibility-taffy, feasibility-anyrender-vello-cpu,
  feasibility-selectors, feasibility-cssparser,
  feasibility-selectors-cssparser-version-compat,
  feasibility-paintscene-adapter-compile-spike (nzv.11 で verify 済、§13.0 参照)
- feasibility-report (`docs/feasibility-report.md` に全結果、API surface 記録)
```

---

### Step 9: (b1)(b2)(b3) の marker を grep で verify

Run:
```bash
grep -nE 'nzv\.11 で verify 済' docs/superpowers/specs/2026-07-13-raikiri-rebuild-design.md
```

Expected: 3 hits (line 3155 / 3170 / 3180 相当、edit 後は line ずれあり)。

---

### Step 10: 追加 edit — §M0 Tasks の rust-toolchain-msrv 行に MSRV 実態 annotate

**Anchor**: `- rust-toolchain-msrv (\`rust-toolchain.toml\` + MSRV = 1.85)` 行。

Edit tool の `old_string`:

```
**Tasks** (依存順、round 3 #5 対応):
- rust-toolchain-msrv (`rust-toolchain.toml` + MSRV = 1.85)
- workspace-scaffold (`Cargo.toml` workspace root、production)
```

Edit tool の `new_string`:

```
**Tasks** (依存順、round 3 #5 対応):
- rust-toolchain-msrv (`rust-toolchain.toml` + MSRV = 1.85 → **実 MSRV は 1.89 に drift、§13.0 参照**)
- workspace-scaffold (`Cargo.toml` workspace root、production)
```

---

### Step 11: 全 acceptance marker を grep で verify

Run:
```bash
grep -nE 'Actual drifts recorded|1\.85 → 1\.88 → 1\.89|Crate-local deps|taffy features 明示|nzv\.11 で verify 済' docs/superpowers/specs/2026-07-13-raikiri-rebuild-design.md
```

Expected: **7 hits 合計**
- `Actual drifts recorded` — 1 hit (§13.0 drift log header)
- `1.85 → 1.88 → 1.89` — 1 hit (§13.0 drift log MSRV bullet)
- `Crate-local deps` — 1 hit (§4 note header)
- `taffy features 明示` — 1 hit (§4 note header)
- `nzv.11 で verify 済` — 3 hits (§M0 line 3155 / 3170 / 3180 相当)

Additional verify:
```bash
grep -nE '実 MSRV は 1\.89 に drift' docs/superpowers/specs/2026-07-13-raikiri-rebuild-design.md
```

Expected: 1 hit (line 3173 相当)。

---

### Step 12: diff 全体を目視 review

Run:
```bash
git diff docs/superpowers/specs/2026-07-13-raikiri-rebuild-design.md
```

Check list:
- (a) §13.0 drift log が Compression trigger の直後に入り、`### M0` ヘッダより上
- (b1) §M0 検証項目 7 個の最後 (anyrender PaintScene 適合面) に "nzv.11 で verify 済" annotate
- (b2) M0 内部 DAG の feasibility-paintscene-adapter-compile-spike 行に annotate
- (b3) Tasks 依存順の同じく annotate
- (c) §4 dep 表直下に Crate-local deps note
- (d) その直後に taffy features 明示 note (Style !Send caveat 含む)
- 追加 §M0 Tasks の rust-toolchain-msrv 行に MSRV 実態 annotate
- コード / manifest / lock ファイルに変更なし (git diff で他ファイル無し)

---

### Step 13: Cross-check with feasibility-report §4.2 / §4.3 / §4.4 / §5.4

Run:
```bash
sed -n '574,606p' docs/feasibility-report.md
sed -n '652,660p' docs/feasibility-report.md
```

Confirm design doc の下記記述が feasibility-report と語義的に整合:
- §4.2 の "nzv.11 refute" → drift log の "nzv.11 refute" 項 + §M0 3 か所 annotate
- §4.3 (1) の "precomputed-hash crate-local" → §4 Crate-local deps note
- §4.3 (2) の "taffy features [block_layout, flexbox, grid, content_size, calc, std] / taffy_tree OFF" → §4 taffy features 明示
- §4.3 (3) の "Style: !Send under calc feature" → §4 taffy features Style !Send caveat + m1.17 参照
- §4.4 の "MSRV 1.85 → 1.88 (edce05d) → 1.89 (23b5343)" → §13.0 drift log MSRV
- §4.4 の "nzv.8 verdict 遷移 (NEEDS_DESIGN_CHANGE → OK)" → §13.0 drift log nzv.8 verdict
- §4.4 の "raikiri-style M0 seed 化" → §13.0 drift log raikiri-style M0 seed
- §5.4 (=§4.4 の再言及) → 追加なし (§13.0 drift log で cover)

---

### Step 14: git status 確認と commit

Run:
```bash
git status
```

Expected: `modified: docs/superpowers/specs/2026-07-13-raikiri-rebuild-design.md` の 1 行のみ (`docs/superpowers/plans/2026-07-16-m1.19-m0-drift-spec-revise.md` は既に untracked として表示されている場合あり、これも stage する)。

Run:
```bash
git add docs/superpowers/specs/2026-07-13-raikiri-rebuild-design.md docs/superpowers/plans/2026-07-16-m1.19-m0-drift-spec-revise.md
git commit -m "$(cat <<'EOF'
docs: sync spec with feasibility-report — MSRV drift + PaintScene refute + §4 dep notes (m1.19)

feasibility-report §4.2 / §4.3 / §4.4 / §5.4 の 4 件と、recon で発見した
§M0 Tasks の MSRV 乖離 1 件を、design doc に反映する docs-only sync。

Changes (5 edit, single file):

- §13.0: Compression trigger の直後に "Actual drifts recorded (M0 期間中)"
  bullet 追加 (MSRV 1.85→1.88→1.89 / nzv.8 verdict 遷移 / raikiri-style M0
  seed 化 / nzv.11 refute)
- §M0 検証項目 line: "anyrender PaintScene 適合面" に inline annotate
  "(nzv.11 で verify 済、bridge trait 不要と確定)"
- §M0 内部 DAG + Tasks 依存順の 2 か所 feasibility-paintscene-adapter-compile-spike
  行にも同 annotate
- §4 dep 表直下: "Crate-local deps (not workspace-wide)" note 追加
  (precomputed-hash が raikiri-style 内 direct dep のみである旨)
- §4 dep 表直下: "taffy features 明示" note 追加 ([block_layout, flexbox,
  grid, content_size, calc, std] / taffy_tree OFF / Style !Send under
  calc feature caveat, m1.17 で invariant 決定予定)
- §M0 Tasks rust-toolchain-msrv 行: MSRV = 1.85 に "実 MSRV は 1.89 に drift、
  §13.0 参照" を inline annotate (既存記述は削除せず annotate に留める)

既存 spec narrative は削除せず annotate のみ (verify 履歴保存)。
コード / manifest / lock 変更なし。

Refs: raikiri-spike-m1.19
Claude-Session: https://claude.ai/code/session_012eq7kH2wgUjhJwgxvWjYHY
EOF
)"
```

Verify:
```bash
git log -1 --stat
```

Expected: 1 commit, 2 files changed (spec + plan), spec = additions のみ (deletions 0)。

---

## Self-Review

**1. Spec coverage**:

| feasibility-report source | plan task step | design doc target |
|---|---|---|
| §4.2 nzv.11 refute | Step 6/7/8 (b1/b2/b3 annotate) + Step 4 (drift log nzv.11 項) | §M0 3 か所 + §13.0 |
| §4.3 (1) precomputed-hash crate-local | Step 2 (Crate-local deps note) | §4 dep 表直下 |
| §4.3 (2) taffy features 明示 | Step 2 (taffy features 明示 note) | §4 dep 表直下 |
| §4.3 (3) Style !Send caveat | Step 2 (同 note 内 caveat 項) | §4 dep 表直下 |
| §4.3 (4) dev-only dep 配置 | **プランに含めず** — Style !Send caveat 内で raikiri-vrt を明示していないが、feasibility 側の(4) は "raikiri-vrt が M1 で `[dependencies]` に格上げする際 shim 不要" という将来の implementation note で、design doc に反映する urgency が無い。gap として認識、m1.19 の scope 外。 | (該当なし) |
| §4.4 MSRV drift | Step 4 (drift log MSRV bullet) | §13.0 |
| §4.4 nzv.8 verdict 遷移 | Step 4 (drift log nzv.8 項) | §13.0 |
| §4.4 raikiri-style M0 seed 化 | Step 4 (drift log raikiri-style seed 項) | §13.0 |
| §5.4 (=§4.4 再言及) | (cover 済) | §13.0 |
| recon 発見: §M0 Tasks の MSRV=1.85 | Step 10 (rust-toolchain-msrv 行 annotate) | §M0 Tasks line 3173 |

Gap: feasibility §4.3 (4) の dev-dep 配置は m1.19 scope 外 (upgrade note、design doc 反映 urgency 無し)。他は全て cover。

**2. Placeholder scan**: TBD / TODO / implement later / similar to Task N なし。全 edit の before/after content が本文に明記されている。

**3. Type consistency**: N/A (docs 変更、型なし)。ただし用語の一貫性チェック:
- "Crate-local deps" (Step 2) と "Crate-local deps note" (Step 4 の cross-ref) — 一致。
- "§13.0" 参照 (Step 4/6/10) — §13.0 は既存 section header と一致 (line 3092)。
- `raikiri-spike-m1.17` (Style !Send caveat 内で参照) — beads で ready 状態、存在 verify 済。

---

## Rejected alternatives (design 段階で却下)

- **(b) の記述丸ごと削除**: verify 履歴が読めなくなり、"なぜ 7 個中 6 個しか書かれていない?" と混乱を招く。annotate 採用。
- **(a) を §13.1 sub-section にする**: 既存 §13.0 は "protocol の抽象定義" で単一 section 完結、§13.1 は "M0" milestone plan (line 3133) と衝突する。§13.0 内の list として抱え込む方が構造的に整合。
- **(d) を §3 に書く**: §3 は "継承する dep の crate 名列挙" のみで詳細を持たない。features / caveat のような implementation detail は §4 (workspace layout の詳細) 側が semantically 正しい配置。
- **書き換え方式 (b の未来形 → 完了形置換)**: annotate より短いが verify 前の懸念内容が失われる。
- **worktree なし**: docs-only 1 commit なので main 直接も可だったが、user が EnterWorktree を選択 (blueprint:impl workflow 厳守)。

---

## Post-commit steps (task 完了後の handoff)

1. `bd close raikiri-spike-m1.19` (session close protocol)
2. worktree 完了処理:
   - ExitWorktree (action=keep) して main branch に戻る
   - main に merge するか PR を open するかは user 判断
3. 完了 handoff report (changed files, validation, next commands)
