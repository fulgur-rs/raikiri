# m1.9 raikiri-wpt Harness Skeleton — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** `raikiri-wpt` crate に 4 module (`runner` / `expectations` / `oracle` / `reftest`) + 5 種 expectations file parser + `validate-expectations` bin stub + workspace-root `expectations/` の 5 本 header-only 空ファイルを追加。M3 で実 WPT reftest 実行を plug-in できる Type-first skeleton を M1 で確立する。

**Architecture:** dev-only crate。lib API は 4 module に分割し実行系 method は `todo!("M3")`。expectations parser のみ実装済 (line-oriented text)。blitz-dom は M1 で smoke test 経由に link 確認 (dev-dep が実際にビルドできることを保証)。error 型は raikiri-traits `m1.2 pattern` (`thiserror` 不使用、hand-written `Display + std::error::Error + From`)。

**Tech Stack:** Rust 2024 edition、rust-version 1.89.0、std のみ。新規 workspace dep 追加なし (blitz-html/blitz-dom は既に dev-dep 配線済、tempfile は使わず `parse(&str)` API で unit test)。

## Global Constraints

- 対象 crate: `crates/raikiri-wpt` のみ (他 crate は触らない)
- 実行系 method は `todo!("M3")` で本体未実装、type と signature のみ確定
- workspace-root `expectations/` は 5 本の header-only 空ファイル (実 test id 記入は M3)
- `expectations/` は `.gitignore` されていないので git track される (crate に必要な baseline artifact)
- `#[non_exhaustive]` を全公開 error / enum に付与
- ExpectError は `Display + std::error::Error + From<std::io::Error>` を hand-written (m1.2 pattern、thiserror 不使用)
- rustc / clippy warnings 0 を維持
- `cargo test -p raikiri-wpt` 全 green
- Commit 単位は task ごと (最小 4 commit)

---

## File Structure

**Modify:**
- `crates/raikiri-wpt/Cargo.toml` — `[[bin]]` セクション追加
- `crates/raikiri-wpt/src/lib.rs` — M0 stub の 4 行を module 宣言 4 本 + crate doc に置換

**Create:**
- `crates/raikiri-wpt/src/expectations.rs` — 5 種 parser (実装済) + ExpectationSet + ExpectError
- `crates/raikiri-wpt/src/runner.rs` — WptRunner / TestExecution / TestOutcome (type stub)
- `crates/raikiri-wpt/src/oracle.rs` — BlitzOracle / OracleDiff / BlitzDocument alias + blitz-dom smoke test
- `crates/raikiri-wpt/src/reftest.rs` — ReftestKind / ReftestPair / ReftestResult (type stub)
- `crates/raikiri-wpt/src/bin/validate-expectations.rs` — 3-4 行 stub bin
- `expectations/tracked-wpt.txt` — header-only (§12.9)
- `expectations/known-issues.txt` — header-only (§12.9)
- `expectations/raikiri-baseline.txt` — header-only (§12.10)
- `expectations/quarantine.txt` — header-only (§12.10、8-col format 記述)
- `expectations/deprecated.txt` — header-only (§12.10 precedence 1)

**Test:** 全て module 内 `#[cfg(test)] mod tests` + `expectations::ExpectationSet::load_from_workspace_root` の doctest。

---

## Task 1: Scaffolding (Cargo bin + lib.rs + module skeletons + workspace expectations/ files)

**Files:**
- Modify: `crates/raikiri-wpt/Cargo.toml`
- Modify: `crates/raikiri-wpt/src/lib.rs`
- Create: `crates/raikiri-wpt/src/bin/validate-expectations.rs`
- Create: `crates/raikiri-wpt/src/expectations.rs` (module skeleton のみ)
- Create: `crates/raikiri-wpt/src/runner.rs` (module skeleton のみ)
- Create: `crates/raikiri-wpt/src/oracle.rs` (module skeleton のみ)
- Create: `crates/raikiri-wpt/src/reftest.rs` (module skeleton のみ)
- Create: `expectations/tracked-wpt.txt`
- Create: `expectations/known-issues.txt`
- Create: `expectations/raikiri-baseline.txt`
- Create: `expectations/quarantine.txt`
- Create: `expectations/deprecated.txt`

**Interfaces:**
- Consumes: 既存 `crates/raikiri-wpt/Cargo.toml` の `[dependencies]` / `[dev-dependencies]` (変更しない)
- Produces:
  - `raikiri_wpt::expectations` / `runner` / `oracle` / `reftest` の 4 pub module (中身は Task 2-4 で populate)
  - `cargo run -p raikiri-wpt --bin validate-expectations` が exit 0 で "M3 populate" と出力
  - workspace root に `expectations/*.txt` 5 本が存在

- [ ] **Step 1: Cargo.toml に `[[bin]]` セクション追加**

`crates/raikiri-wpt/Cargo.toml` の末尾 (`[lints]` の前) に追記:

```toml
[[bin]]
name = "validate-expectations"
path = "src/bin/validate-expectations.rs"
```

- [ ] **Step 2: lib.rs を module 宣言に置換**

`crates/raikiri-wpt/src/lib.rs` を以下で置換:

```rust
//! raikiri-wpt — WPT harness with blitz oracle (M1 skeleton).
//!
//! M1 scaffold: module structure + expectations parsers + type stubs.
//! Actual reftest execution and oracle diff aggregation land in M3
//! (see design spec §12.9 / §12.10).

pub mod expectations;
pub mod oracle;
pub mod reftest;
pub mod runner;
```

- [ ] **Step 3: 4 module の skeleton file を作成 (中身は Task 2-4 で populate)**

`crates/raikiri-wpt/src/expectations.rs`:

```rust
//! WPT expectations file parsers (spec §12.9, §12.10).
//! Populated in Task 2 of the m1.9 skeleton plan.
```

`crates/raikiri-wpt/src/runner.rs`:

```rust
//! WPT test runner dispatch (spec §12.6). Type stub only in M1.
//! Populated in Task 3 of the m1.9 skeleton plan.
```

`crates/raikiri-wpt/src/oracle.rs`:

```rust
//! Blitz baseline oracle (spec §12.4, T3 informational).
//! Populated in Task 4 of the m1.9 skeleton plan.
```

`crates/raikiri-wpt/src/reftest.rs`:

```rust
//! WPT reftest pair + result types. Type stub only in M1.
//! Populated in Task 3 of the m1.9 skeleton plan.
```

- [ ] **Step 4: bin stub を作成**

`crates/raikiri-wpt/src/bin/validate-expectations.rs`:

```rust
//! Validate expectations files (spec §12.10 line 3101).
//!
//! M1 stub. M3 populates malformed / duplicate / conflicting / expired
//! detection and wires this into CI.

fn main() {
    eprintln!(
        "validate-expectations: M3 will populate malformed / duplicate / conflicting / expired checks."
    );
}
```

- [ ] **Step 5: workspace root に `expectations/` ディレクトリ + 5 本の header-only ファイル作成**

`expectations/tracked-wpt.txt`:

```
# expectations/tracked-wpt.txt — WPT categories under tracking (spec §12.9).
#
# Format: one test_id per line. Lines starting with '#' are comments.
# Empty lines are ignored.
#
# Populated in M3.
```

`expectations/known-issues.txt`:

```
# expectations/known-issues.txt — WPT tests judged as "cannot pass, no practical impact"
# (spec §12.9).
#
# Format: <test_id_or_pattern> | <reason>
# Lines starting with '#' are comments. Empty lines are ignored.
#
# Populated in M3.
```

`expectations/raikiri-baseline.txt`:

```
# expectations/raikiri-baseline.txt — T2 gate: WPT tests raikiri currently passes
# (spec §12.10). Regression on these blocks PR merge.
#
# Format: one test_id per line. Lines starting with '#' are comments.
# Empty lines are ignored.
#
# Populated in M3. Additions/removals require 2-reviewer approval (§12.10 baseline migration).
```

`expectations/quarantine.txt`:

```
# expectations/quarantine.txt — Flaky test temporary shelf (spec §12.10, platform-aware).
#
# Format: test_id | platform | arch | renderer | tolerance | reason | issue_link | added_date
#
# platform:  linux / macos / windows / *
# arch:      x86_64 / aarch64 / *
# renderer:  vello_cpu / skia / tiny_skia / *
# tolerance: pixel-exact / low / medium / high / *
#
# Multiple entries per test_id (different platform combinations) are allowed.
# Lines starting with '#' are comments. Empty lines are ignored.
#
# Populated in M3.
```

`expectations/deprecated.txt`:

```
# expectations/deprecated.txt — WPT tests fully excluded from evaluation
# (spec §12.10 precedence 1; highest priority).
#
# Format: one test_id per line. Lines starting with '#' are comments.
# Empty lines are ignored.
#
# Populated in M3.
```

- [ ] **Step 6: build 検証**

```bash
cargo build -p raikiri-wpt --all-targets 2>&1 | tail -20
```

Expected: `Finished ... target(s)`。エラーなし。

- [ ] **Step 7: bin 実行検証**

```bash
cargo run -p raikiri-wpt --bin validate-expectations 2>&1 | tail -5
```

Expected stderr: `validate-expectations: M3 will populate malformed / duplicate / conflicting / expired checks.`
Expected exit code: 0

- [ ] **Step 8: Commit**

```bash
git add crates/raikiri-wpt/Cargo.toml \
        crates/raikiri-wpt/src/lib.rs \
        crates/raikiri-wpt/src/expectations.rs \
        crates/raikiri-wpt/src/runner.rs \
        crates/raikiri-wpt/src/oracle.rs \
        crates/raikiri-wpt/src/reftest.rs \
        crates/raikiri-wpt/src/bin/validate-expectations.rs \
        expectations/
git commit -m "$(cat <<'EOF'
feat(raikiri-wpt): scaffold module structure + validate-expectations bin (m1.9)

Populate 4 module skeleton files (expectations / runner / oracle / reftest),
declare validate-expectations bin as stub, and add workspace-root
expectations/ with 5 header-only files describing the format for each
(tracked-wpt / known-issues / raikiri-baseline / quarantine / deprecated,
per spec §12.9 and §12.10).

Contents (parsers, types, blitz smoke test) land in the follow-up commits
of this m1.9 plan.

Claude-Session: https://claude.ai/code/session_012eq7kH2wgUjhJwgxvWjYHY
EOF
)"
```

---

## Task 2: expectations module — 5 parsers + ExpectError + unit tests + doctest

**Files:**
- Modify: `crates/raikiri-wpt/src/expectations.rs` (Task 1 で作った skeleton を full impl に置換)

**Interfaces:**
- Consumes: `crates/raikiri-wpt/expectations/*.txt` (Task 1 で作成、workspace root)
- Produces:
  - `pub struct ExpectationSet { tracked, known_issues, baseline, quarantine, deprecated }`
  - `impl ExpectationSet { pub fn load_from(dir: &Path) -> Result<Self, ExpectError>; pub fn load_from_workspace_root() -> Result<Self, ExpectError> }`
  - `pub struct TrackedWpt { pub entries: Vec<String> }` — `parse(&str, &str) -> Result<_, _>`, `load(&Path)`, `is_empty()`
  - `pub struct KnownIssues { pub entries: Vec<(String, String)> }` — same 3 methods
  - `pub struct Baseline { pub entries: HashSet<String> }` — same 3 methods
  - `pub struct Deprecated { pub entries: HashSet<String> }` — same 3 methods
  - `pub struct Quarantine { pub entries: Vec<QuarantineEntry> }` — same 3 methods
  - `pub struct QuarantineEntry { test_id, platform, arch, renderer, tolerance, reason, issue_link, added_date }`
  - `pub enum PlatformFilter { Linux, MacOs, Windows, Any }` (+ ArchFilter / RendererFilter / ToleranceFilter, 4 enums total、`#[non_exhaustive]`)
  - `pub enum ExpectError { Io(io::Error), MalformedLine{..}, UnknownEnum{..} }` — `#[non_exhaustive]` + Display + std::error::Error + From<io::Error>

- [ ] **Step 1: 失敗する empty-file test を書く**

`crates/raikiri-wpt/src/expectations.rs` の末尾 (module doc の後) に:

```rust
#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn empty_content_yields_empty_sets() {
        let tracked = TrackedWpt::parse("# only a comment\n", "tracked-wpt.txt").unwrap();
        assert!(tracked.is_empty());

        let ki = KnownIssues::parse("", "known-issues.txt").unwrap();
        assert!(ki.is_empty());

        let base = Baseline::parse("# header\n\n", "raikiri-baseline.txt").unwrap();
        assert!(base.is_empty());

        let dep = Deprecated::parse("", "deprecated.txt").unwrap();
        assert!(dep.is_empty());

        let q = Quarantine::parse("# 8-col format follows\n", "quarantine.txt").unwrap();
        assert!(q.is_empty());
    }
}
```

- [ ] **Step 2: test を run して fail 確認**

```bash
cargo test -p raikiri-wpt empty_content_yields_empty_sets 2>&1 | tail -20
```

Expected: build error (`TrackedWpt` etc. not defined)。

- [ ] **Step 3: expectations.rs に parser + types + error を実装**

`crates/raikiri-wpt/src/expectations.rs` を以下で置換 (Step 1 の tests module も含めた最終形):

```rust
//! WPT expectations file parsers (spec §12.9, §12.10).
//!
//! 5 files under workspace-root `expectations/`:
//! - `tracked-wpt.txt`     — tracking categories (informational, T3)
//! - `known-issues.txt`    — tests explicitly waived
//! - `raikiri-baseline.txt` — T2 gate (regression blocks merge)
//! - `quarantine.txt`      — flaky tests, platform-aware
//! - `deprecated.txt`      — fully excluded from evaluation
//!
//! Precedence (highest → lowest): deprecated > quarantine > baseline > default.
//!
//! Parsers are `parse(content, file_name)` for unit-testable strings, plus
//! `load(path)` for filesystem I/O. `ExpectationSet::load_from_workspace_root`
//! walks the workspace-root `expectations/` directory.

use std::collections::HashSet;
use std::fmt;
use std::path::{Path, PathBuf};

// ── ExpectationSet ─────────────────────────────────────────────────────────

/// All expectations files combined.
///
/// ```no_run
/// use raikiri_wpt::expectations::ExpectationSet;
/// let set = ExpectationSet::load_from_workspace_root().unwrap();
/// assert!(set.baseline.is_empty()); // M1 skeleton: workspace files are header-only
/// ```
#[non_exhaustive]
pub struct ExpectationSet {
    pub tracked: TrackedWpt,
    pub known_issues: KnownIssues,
    pub baseline: Baseline,
    pub quarantine: Quarantine,
    pub deprecated: Deprecated,
}

impl ExpectationSet {
    /// Load all 5 files from `<workspace root>/expectations/`.
    ///
    /// Workspace root is resolved via `CARGO_MANIFEST_DIR/../..` (this crate
    /// sits at `crates/raikiri-wpt/`).
    pub fn load_from_workspace_root() -> Result<Self, ExpectError> {
        Self::load_from(&workspace_expectations_dir())
    }

    /// Load all 5 files from an arbitrary directory. Used for unit tests
    /// via `parse(&str, &str)` on individual files and for future
    /// integration tests that stage fixture directories.
    pub fn load_from(dir: &Path) -> Result<Self, ExpectError> {
        Ok(Self {
            tracked: TrackedWpt::load(&dir.join("tracked-wpt.txt"))?,
            known_issues: KnownIssues::load(&dir.join("known-issues.txt"))?,
            baseline: Baseline::load(&dir.join("raikiri-baseline.txt"))?,
            quarantine: Quarantine::load(&dir.join("quarantine.txt"))?,
            deprecated: Deprecated::load(&dir.join("deprecated.txt"))?,
        })
    }
}

fn workspace_expectations_dir() -> PathBuf {
    PathBuf::from(env!("CARGO_MANIFEST_DIR"))
        .join("..")
        .join("..")
        .join("expectations")
}

// ── Parser helpers ─────────────────────────────────────────────────────────

fn iter_data_lines(content: &str) -> impl Iterator<Item = (usize, &str)> {
    content
        .lines()
        .enumerate()
        .map(|(i, line)| (i + 1, line.trim()))
        .filter(|(_, line)| !line.is_empty() && !line.starts_with('#'))
}

fn read_file(path: &Path) -> Result<String, ExpectError> {
    std::fs::read_to_string(path).map_err(ExpectError::Io)
}

// ── TrackedWpt (§12.9) ─────────────────────────────────────────────────────

#[derive(Debug, Default, Clone)]
#[non_exhaustive]
pub struct TrackedWpt {
    pub entries: Vec<String>,
}

impl TrackedWpt {
    pub fn parse(content: &str, _file_name: &str) -> Result<Self, ExpectError> {
        Ok(Self {
            entries: iter_data_lines(content).map(|(_, l)| l.to_owned()).collect(),
        })
    }

    pub fn load(path: &Path) -> Result<Self, ExpectError> {
        let content = read_file(path)?;
        Self::parse(&content, &path.display().to_string())
    }

    pub fn is_empty(&self) -> bool {
        self.entries.is_empty()
    }
}

// ── KnownIssues (§12.9) ────────────────────────────────────────────────────

#[derive(Debug, Default, Clone)]
#[non_exhaustive]
pub struct KnownIssues {
    /// `(pattern, reason)` pairs.
    pub entries: Vec<(String, String)>,
}

impl KnownIssues {
    pub fn parse(content: &str, file_name: &str) -> Result<Self, ExpectError> {
        let mut entries = Vec::new();
        for (line_no, line) in iter_data_lines(content) {
            let mut parts = line.splitn(2, '|').map(str::trim);
            match (parts.next(), parts.next()) {
                (Some(pat), Some(reason)) if !pat.is_empty() && !reason.is_empty() => {
                    entries.push((pat.to_owned(), reason.to_owned()));
                }
                _ => {
                    return Err(ExpectError::MalformedLine {
                        file: file_name.to_owned(),
                        line_no,
                        reason: "expected `test_id_or_pattern | reason`".to_owned(),
                    });
                }
            }
        }
        Ok(Self { entries })
    }

    pub fn load(path: &Path) -> Result<Self, ExpectError> {
        let content = read_file(path)?;
        Self::parse(&content, &path.display().to_string())
    }

    pub fn is_empty(&self) -> bool {
        self.entries.is_empty()
    }
}

// ── Baseline (§12.10, T2 gate) ─────────────────────────────────────────────

#[derive(Debug, Default, Clone)]
#[non_exhaustive]
pub struct Baseline {
    pub entries: HashSet<String>,
}

impl Baseline {
    pub fn parse(content: &str, _file_name: &str) -> Result<Self, ExpectError> {
        Ok(Self {
            entries: iter_data_lines(content).map(|(_, l)| l.to_owned()).collect(),
        })
    }

    pub fn load(path: &Path) -> Result<Self, ExpectError> {
        let content = read_file(path)?;
        Self::parse(&content, &path.display().to_string())
    }

    pub fn is_empty(&self) -> bool {
        self.entries.is_empty()
    }
}

// ── Deprecated (§12.10, precedence 1) ──────────────────────────────────────

#[derive(Debug, Default, Clone)]
#[non_exhaustive]
pub struct Deprecated {
    pub entries: HashSet<String>,
}

impl Deprecated {
    pub fn parse(content: &str, _file_name: &str) -> Result<Self, ExpectError> {
        Ok(Self {
            entries: iter_data_lines(content).map(|(_, l)| l.to_owned()).collect(),
        })
    }

    pub fn load(path: &Path) -> Result<Self, ExpectError> {
        let content = read_file(path)?;
        Self::parse(&content, &path.display().to_string())
    }

    pub fn is_empty(&self) -> bool {
        self.entries.is_empty()
    }
}

// ── Quarantine (§12.10, 8-col platform-aware) ──────────────────────────────

#[derive(Debug, Default, Clone)]
#[non_exhaustive]
pub struct Quarantine {
    pub entries: Vec<QuarantineEntry>,
}

#[derive(Debug, Clone)]
#[non_exhaustive]
pub struct QuarantineEntry {
    pub test_id: String,
    pub platform: PlatformFilter,
    pub arch: ArchFilter,
    pub renderer: RendererFilter,
    pub tolerance: ToleranceFilter,
    pub reason: String,
    pub issue_link: String,
    /// Kept as `String` in M1; chrono/time integration lands with the M3
    /// `validate-expectations` expired-check logic (bd follow-up filed at
    /// m1.9 close).
    pub added_date: String,
}

#[derive(Debug, Clone, Copy, PartialEq, Eq)]
#[non_exhaustive]
pub enum PlatformFilter { Linux, MacOs, Windows, Any }

#[derive(Debug, Clone, Copy, PartialEq, Eq)]
#[non_exhaustive]
pub enum ArchFilter { X86_64, Aarch64, Any }

#[derive(Debug, Clone, Copy, PartialEq, Eq)]
#[non_exhaustive]
pub enum RendererFilter { VelloCpu, Skia, TinySkia, Any }

#[derive(Debug, Clone, Copy, PartialEq, Eq)]
#[non_exhaustive]
pub enum ToleranceFilter { PixelExact, Low, Medium, High, Any }

impl PlatformFilter {
    fn parse(s: &str) -> Option<Self> {
        Some(match s {
            "linux" => Self::Linux,
            "macos" => Self::MacOs,
            "windows" => Self::Windows,
            "*" => Self::Any,
            _ => return None,
        })
    }
}

impl ArchFilter {
    fn parse(s: &str) -> Option<Self> {
        Some(match s {
            "x86_64" => Self::X86_64,
            "aarch64" => Self::Aarch64,
            "*" => Self::Any,
            _ => return None,
        })
    }
}

impl RendererFilter {
    fn parse(s: &str) -> Option<Self> {
        Some(match s {
            "vello_cpu" => Self::VelloCpu,
            "skia" => Self::Skia,
            "tiny_skia" => Self::TinySkia,
            "*" => Self::Any,
            _ => return None,
        })
    }
}

impl ToleranceFilter {
    fn parse(s: &str) -> Option<Self> {
        Some(match s {
            "pixel-exact" => Self::PixelExact,
            "low" => Self::Low,
            "medium" => Self::Medium,
            "high" => Self::High,
            "*" => Self::Any,
            _ => return None,
        })
    }
}

impl Quarantine {
    pub fn parse(content: &str, file_name: &str) -> Result<Self, ExpectError> {
        let mut entries = Vec::new();
        for (line_no, line) in iter_data_lines(content) {
            let cols: Vec<&str> = line.split('|').map(str::trim).collect();
            if cols.len() != 8 {
                return Err(ExpectError::MalformedLine {
                    file: file_name.to_owned(),
                    line_no,
                    reason: format!("expected 8 pipe-delimited columns, got {}", cols.len()),
                });
            }
            let platform = PlatformFilter::parse(cols[1]).ok_or_else(|| ExpectError::UnknownEnum {
                file: file_name.to_owned(),
                line_no,
                field: "platform",
                value: cols[1].to_owned(),
            })?;
            let arch = ArchFilter::parse(cols[2]).ok_or_else(|| ExpectError::UnknownEnum {
                file: file_name.to_owned(),
                line_no,
                field: "arch",
                value: cols[2].to_owned(),
            })?;
            let renderer = RendererFilter::parse(cols[3]).ok_or_else(|| ExpectError::UnknownEnum {
                file: file_name.to_owned(),
                line_no,
                field: "renderer",
                value: cols[3].to_owned(),
            })?;
            let tolerance = ToleranceFilter::parse(cols[4]).ok_or_else(|| ExpectError::UnknownEnum {
                file: file_name.to_owned(),
                line_no,
                field: "tolerance",
                value: cols[4].to_owned(),
            })?;
            entries.push(QuarantineEntry {
                test_id: cols[0].to_owned(),
                platform,
                arch,
                renderer,
                tolerance,
                reason: cols[5].to_owned(),
                issue_link: cols[6].to_owned(),
                added_date: cols[7].to_owned(),
            });
        }
        Ok(Self { entries })
    }

    pub fn load(path: &Path) -> Result<Self, ExpectError> {
        let content = read_file(path)?;
        Self::parse(&content, &path.display().to_string())
    }

    pub fn is_empty(&self) -> bool {
        self.entries.is_empty()
    }
}

// ── ExpectError (m1.2 pattern: hand-written Display + Error + From) ────────

#[derive(Debug)]
#[non_exhaustive]
pub enum ExpectError {
    /// Underlying filesystem I/O error.
    Io(std::io::Error),
    /// A line failed the shape expected by the file format.
    MalformedLine {
        file: String,
        line_no: usize,
        reason: String,
    },
    /// A pipe-delimited column carried a value not in the accepted enum.
    UnknownEnum {
        file: String,
        line_no: usize,
        field: &'static str,
        value: String,
    },
}

impl fmt::Display for ExpectError {
    fn fmt(&self, f: &mut fmt::Formatter<'_>) -> fmt::Result {
        match self {
            Self::Io(e) => write!(f, "expectations I/O error: {e}"),
            Self::MalformedLine { file, line_no, reason } => {
                write!(f, "{file}:{line_no}: malformed line ({reason})")
            }
            Self::UnknownEnum { file, line_no, field, value } => {
                write!(f, "{file}:{line_no}: unknown {field} value {value:?}")
            }
        }
    }
}

impl std::error::Error for ExpectError {
    fn source(&self) -> Option<&(dyn std::error::Error + 'static)> {
        match self {
            Self::Io(e) => Some(e),
            _ => None,
        }
    }
}

impl From<std::io::Error> for ExpectError {
    fn from(e: std::io::Error) -> Self {
        Self::Io(e)
    }
}

// ── Tests ──────────────────────────────────────────────────────────────────

#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn empty_content_yields_empty_sets() {
        let tracked = TrackedWpt::parse("# only a comment\n", "tracked-wpt.txt").unwrap();
        assert!(tracked.is_empty());

        let ki = KnownIssues::parse("", "known-issues.txt").unwrap();
        assert!(ki.is_empty());

        let base = Baseline::parse("# header\n\n", "raikiri-baseline.txt").unwrap();
        assert!(base.is_empty());

        let dep = Deprecated::parse("", "deprecated.txt").unwrap();
        assert!(dep.is_empty());

        let q = Quarantine::parse("# 8-col format follows\n", "quarantine.txt").unwrap();
        assert!(q.is_empty());
    }

    #[test]
    fn tracked_parses_test_ids_and_skips_comments() {
        let content = "# comment\n\
                       css/css-page/page-margin-boxes-001\n\
                       \n\
                       css/css-fragmentation/break-before-001\n";
        let tracked = TrackedWpt::parse(content, "t.txt").unwrap();
        assert_eq!(
            tracked.entries,
            vec![
                "css/css-page/page-margin-boxes-001".to_owned(),
                "css/css-fragmentation/break-before-001".to_owned(),
            ]
        );
    }

    #[test]
    fn known_issues_parses_pattern_and_reason() {
        let content = "css/css-transitions/* | non-goal, interactive\n\
                       html/interaction/* | non-goal, interactive\n";
        let ki = KnownIssues::parse(content, "k.txt").unwrap();
        assert_eq!(
            ki.entries,
            vec![
                ("css/css-transitions/*".to_owned(), "non-goal, interactive".to_owned()),
                ("html/interaction/*".to_owned(), "non-goal, interactive".to_owned()),
            ]
        );
    }

    #[test]
    fn known_issues_rejects_missing_reason() {
        let content = "css/foo | \n";
        let err = KnownIssues::parse(content, "k.txt").unwrap_err();
        match err {
            ExpectError::MalformedLine { line_no, .. } => assert_eq!(line_no, 1),
            other => panic!("expected MalformedLine, got {other:?}"),
        }
    }

    #[test]
    fn baseline_deduplicates_via_hashset() {
        let content = "css/foo/bar-001\ncss/foo/bar-001\ncss/foo/baz-002\n";
        let base = Baseline::parse(content, "b.txt").unwrap();
        assert_eq!(base.entries.len(), 2);
    }

    #[test]
    fn quarantine_parses_8_cols_and_enum_values() {
        let content = "css/css-page/page-margin-boxes-001 | macos | aarch64 | vello_cpu | pixel-exact | Intermittent 1-pixel diff | https://example/issues/123 | 2026-08-01\n";
        let q = Quarantine::parse(content, "q.txt").unwrap();
        assert_eq!(q.entries.len(), 1);
        let e = &q.entries[0];
        assert_eq!(e.test_id, "css/css-page/page-margin-boxes-001");
        assert_eq!(e.platform, PlatformFilter::MacOs);
        assert_eq!(e.arch, ArchFilter::Aarch64);
        assert_eq!(e.renderer, RendererFilter::VelloCpu);
        assert_eq!(e.tolerance, ToleranceFilter::PixelExact);
        assert_eq!(e.reason, "Intermittent 1-pixel diff");
        assert_eq!(e.issue_link, "https://example/issues/123");
        assert_eq!(e.added_date, "2026-08-01");
    }

    #[test]
    fn quarantine_accepts_wildcards() {
        let content = "css/foo | * | * | * | * | reason | issue | 2026-08-05\n";
        let q = Quarantine::parse(content, "q.txt").unwrap();
        let e = &q.entries[0];
        assert_eq!(e.platform, PlatformFilter::Any);
        assert_eq!(e.arch, ArchFilter::Any);
        assert_eq!(e.renderer, RendererFilter::Any);
        assert_eq!(e.tolerance, ToleranceFilter::Any);
    }

    #[test]
    fn quarantine_rejects_wrong_col_count() {
        let content = "css/foo | linux | x86_64\n"; // 3 cols
        let err = Quarantine::parse(content, "q.txt").unwrap_err();
        match err {
            ExpectError::MalformedLine { line_no, reason, .. } => {
                assert_eq!(line_no, 1);
                assert!(reason.contains("expected 8"));
            }
            other => panic!("expected MalformedLine, got {other:?}"),
        }
    }

    #[test]
    fn quarantine_rejects_unknown_platform() {
        let content = "css/foo | plan9 | x86_64 | vello_cpu | low | r | i | 2026-08-01\n";
        let err = Quarantine::parse(content, "q.txt").unwrap_err();
        match err {
            ExpectError::UnknownEnum { field, value, .. } => {
                assert_eq!(field, "platform");
                assert_eq!(value, "plan9");
            }
            other => panic!("expected UnknownEnum, got {other:?}"),
        }
    }

    #[test]
    fn expect_error_display_and_source_chain() {
        // Io variant delegates source to the wrapped io::Error.
        let io_err = std::io::Error::new(std::io::ErrorKind::NotFound, "missing");
        let err: ExpectError = io_err.into();
        assert!(std::error::Error::source(&err).is_some());
        assert!(format!("{err}").contains("missing"));

        let err = ExpectError::MalformedLine {
            file: "q.txt".to_owned(),
            line_no: 42,
            reason: "bad".to_owned(),
        };
        assert!(std::error::Error::source(&err).is_none());
        assert_eq!(format!("{err}"), "q.txt:42: malformed line (bad)");
    }

    #[test]
    fn load_from_workspace_root_reads_the_header_only_files() {
        // Integration-style: relies on Task 1 having created the workspace
        // expectations/ directory with header-only files.
        let set = ExpectationSet::load_from_workspace_root().expect("workspace expectations/ should be present");
        assert!(set.tracked.is_empty());
        assert!(set.known_issues.is_empty());
        assert!(set.baseline.is_empty());
        assert!(set.quarantine.is_empty());
        assert!(set.deprecated.is_empty());
    }
}
```

- [ ] **Step 4: test 全通過を確認**

```bash
cargo test -p raikiri-wpt --lib 2>&1 | tail -30
```

Expected: 11 tests passed, 0 failed。

- [ ] **Step 5: doctest 通過を確認**

```bash
cargo test -p raikiri-wpt --doc 2>&1 | tail -20
```

Expected: 1 doctest passed (`ExpectationSet::load_from_workspace_root` の doctest は `no_run` なので compile-only pass)。

- [ ] **Step 6: clippy 検証**

```bash
cargo clippy -p raikiri-wpt --all-targets -- -D warnings 2>&1 | tail -20
```

Expected: `Finished ...`、warnings/errors なし。

- [ ] **Step 7: Commit**

```bash
git add crates/raikiri-wpt/src/expectations.rs
git commit -m "$(cat <<'EOF'
feat(raikiri-wpt): expectations file parsers (m1.9)

5 file parsers per spec §12.9 and §12.10:
- tracked-wpt.txt   — 1-col test_id list
- known-issues.txt  — pipe-separated `pattern | reason`
- raikiri-baseline.txt — 1-col HashSet (T2 gate)
- quarantine.txt    — 8-col platform-aware
- deprecated.txt    — 1-col HashSet (precedence 1)

ExpectError follows the raikiri-traits m1.2 pattern (hand-written
Display + std::error::Error + From<io::Error>, no thiserror). All public
types + enum values are #[non_exhaustive].

Actual test ids land in M3 (workspace-root expectations/*.txt currently
header-only).

Claude-Session: https://claude.ai/code/session_012eq7kH2wgUjhJwgxvWjYHY
EOF
)"
```

---

## Task 3: runner + reftest modules (type stubs)

**Files:**
- Modify: `crates/raikiri-wpt/src/runner.rs` (Task 1 skeleton を full stub 定義に置換)
- Modify: `crates/raikiri-wpt/src/reftest.rs` (同上)

**Interfaces:**
- Consumes: `crates::expectations::ExpectationSet` (Task 2 で確定した型)
- Produces:
  - `pub struct WptRunner` (private field)
  - `pub struct TestExecution { pub test_id: String, pub outcome: TestOutcome }`
  - `pub enum TestOutcome { Pass, Fail(String), Skip(String), Quarantined }` — `#[non_exhaustive]`
  - `impl WptRunner { pub fn new(expectations: ExpectationSet) -> Self; pub fn run_test(&self, test_id: &str) -> TestExecution }` — 実行系は `todo!("M3")`
  - `pub enum ReftestKind { Match, Mismatch }` — `#[non_exhaustive]`
  - `pub struct ReftestPair { pub test: PathBuf, pub reference: PathBuf, pub kind: ReftestKind }`
  - `pub struct ReftestResult { pub pair_test_id: String, pub outcome: TestOutcome }`

- [ ] **Step 1: 失敗する type-shape test を書く**

`crates/raikiri-wpt/src/runner.rs` の doc の後に:

```rust
#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn test_outcome_variants_are_constructible() {
        let _ = TestOutcome::Pass;
        let _ = TestOutcome::Fail("boom".to_owned());
        let _ = TestOutcome::Skip("non-goal".to_owned());
        let _ = TestOutcome::Quarantined;
    }
}
```

- [ ] **Step 2: test を run して fail 確認**

```bash
cargo test -p raikiri-wpt test_outcome_variants_are_constructible 2>&1 | tail -10
```

Expected: build error (`TestOutcome` not defined)。

- [ ] **Step 3: runner.rs を full stub で置換**

`crates/raikiri-wpt/src/runner.rs`:

```rust
//! WPT test runner dispatch (spec §12.6). Type stubs; run logic lands in M3.

use crate::expectations::ExpectationSet;

/// Dispatches WPT tests and records outcomes.
///
/// In M1 the constructor stores the loaded `ExpectationSet`; `run_test`
/// is a `todo!()` populated in M3 alongside the tree-walker and reftest
/// runner (currently stubs in [`crate::reftest`]).
#[non_exhaustive]
pub struct WptRunner {
    #[allow(dead_code)] // field held for M3 populate; silence M1 warning.
    expectations: ExpectationSet,
}

impl WptRunner {
    pub fn new(expectations: ExpectationSet) -> Self {
        Self { expectations }
    }

    /// Run one WPT test by id. **M1 stub**: panics via `todo!()`.
    pub fn run_test(&self, _test_id: &str) -> TestExecution {
        todo!("M3: walk the WPT submodule tree and dispatch to reftest::run_pair")
    }
}

/// The record produced by executing a single WPT test.
#[non_exhaustive]
pub struct TestExecution {
    pub test_id: String,
    pub outcome: TestOutcome,
}

/// Outcome of a single WPT test execution.
#[derive(Debug, Clone, PartialEq, Eq)]
#[non_exhaustive]
pub enum TestOutcome {
    Pass,
    Fail(String),
    Skip(String),
    Quarantined,
}

#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn test_outcome_variants_are_constructible() {
        let _ = TestOutcome::Pass;
        let _ = TestOutcome::Fail("boom".to_owned());
        let _ = TestOutcome::Skip("non-goal".to_owned());
        let _ = TestOutcome::Quarantined;
    }
}
```

- [ ] **Step 4: reftest.rs を full stub で置換**

`crates/raikiri-wpt/src/reftest.rs`:

```rust
//! WPT reftest pair + result types (spec §12.9). Stubs; execution in M3.

use std::path::PathBuf;

use crate::runner::TestOutcome;

/// Whether a reftest requires visual match or explicit mismatch.
#[derive(Debug, Clone, Copy, PartialEq, Eq)]
#[non_exhaustive]
pub enum ReftestKind {
    /// Test and reference must rasterize to the same pixels.
    Match,
    /// Test and reference must rasterize to *different* pixels.
    Mismatch,
}

/// One (test, reference) pair discovered under the WPT submodule.
#[non_exhaustive]
pub struct ReftestPair {
    pub test: PathBuf,
    pub reference: PathBuf,
    pub kind: ReftestKind,
}

/// Result of executing a `ReftestPair`.
#[non_exhaustive]
pub struct ReftestResult {
    pub pair_test_id: String,
    pub outcome: TestOutcome,
}

#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn reftest_kind_roundtrip() {
        let m = ReftestKind::Match;
        let mm = ReftestKind::Mismatch;
        assert_ne!(m, mm);
    }
}
```

- [ ] **Step 5: test 通過確認**

```bash
cargo test -p raikiri-wpt --lib 2>&1 | tail -30
```

Expected: 13 tests passed (Task 2 の 11 + Task 3 の 2)、0 failed。

- [ ] **Step 6: clippy 検証**

```bash
cargo clippy -p raikiri-wpt --all-targets -- -D warnings 2>&1 | tail -20
```

Expected: warnings/errors なし。

- [ ] **Step 7: Commit**

```bash
git add crates/raikiri-wpt/src/runner.rs crates/raikiri-wpt/src/reftest.rs
git commit -m "$(cat <<'EOF'
feat(raikiri-wpt): runner + reftest type stubs (m1.9)

WptRunner / TestExecution / TestOutcome (runner.rs) and
ReftestPair / ReftestResult / ReftestKind (reftest.rs) with M1
type-first API. Execution paths are todo!("M3").

Claude-Session: https://claude.ai/code/session_012eq7kH2wgUjhJwgxvWjYHY
EOF
)"
```

---

## Task 4: oracle module (type stub + blitz smoke test)

**Files:**
- Modify: `crates/raikiri-wpt/src/oracle.rs` (Task 1 skeleton を full stub + smoke test に置換)

**Interfaces:**
- Consumes: `blitz_dom::{BaseDocument, DocumentConfig}`、`blitz_html::HtmlDocument` (dev-dep 既配線)
- Produces:
  - `pub struct BlitzOracle`
  - `pub struct OracleDiff` — `#[non_exhaustive]`
  - `pub type BlitzDocument = blitz_dom::BaseDocument`
  - `impl BlitzOracle { pub fn parse_html(html: &str) -> BlitzDocument /* todo!() */; pub fn diff(...) -> OracleDiff /* todo!() */ }`

- [ ] **Step 1: 失敗する blitz smoke test を書く**

`crates/raikiri-wpt/src/oracle.rs` の doc の後に:

```rust
#[cfg(test)]
mod tests {
    use super::*;

    /// Confirms the blitz-html / blitz-dom dev-deps actually link in M1.
    /// Execution semantics land in M3.
    #[test]
    fn blitz_dev_dep_links_and_parses_trivial_html() {
        use blitz_dom::DocumentConfig;
        use blitz_html::HtmlDocument;

        let doc = HtmlDocument::from_html("<p>hello</p>", DocumentConfig::default());
        let _base: BlitzDocument = doc.into_inner();
    }
}
```

- [ ] **Step 2: test を run して fail 確認**

```bash
cargo test -p raikiri-wpt blitz_dev_dep_links_and_parses_trivial_html 2>&1 | tail -10
```

Expected: build error (`BlitzDocument` not defined)。

- [ ] **Step 3: oracle.rs を full stub で置換 (Step 1 の tests module も含めた最終形)**

`crates/raikiri-wpt/src/oracle.rs`:

```rust
//! Blitz baseline oracle (spec §12.4, T3 informational).
//!
//! In M1 the module provides type stubs plus a smoke test that exercises
//! the blitz-html / blitz-dom dev-deps end-to-end (parse `<p>hello</p>`).
//! Actual raikiri-vs-blitz diff aggregation lands in M3.

/// Type alias so downstream code (M3) can name the oracle's document type
/// without a direct `blitz_dom::*` import.
pub type BlitzDocument = blitz_dom::BaseDocument;

/// Runs blitz-html / blitz-dom parses side-by-side with raikiri to record
/// pass/fail deltas. **M1 stub**: no live diffing.
#[non_exhaustive]
pub struct BlitzOracle;

/// The recorded delta between one raikiri outcome and the corresponding
/// blitz outcome for a single test. **M1 stub**: field set is
/// intentionally empty until M3 populates it.
#[non_exhaustive]
pub struct OracleDiff {}

impl BlitzOracle {
    /// Parse `html` via blitz-html and return the underlying document.
    ///
    /// **M1 stub**: panics via `todo!()`. The smoke test below drives the
    /// blitz API directly to confirm the dev-dep links.
    pub fn parse_html(_html: &str) -> BlitzDocument {
        todo!("M3: wire blitz_html::HtmlDocument::from_html and return into_inner()")
    }

    /// Compute the informational delta between a raikiri outcome and a
    /// blitz outcome. **M1 stub**.
    pub fn diff(_raikiri: &(), _blitz: &()) -> OracleDiff {
        todo!("M3: compare TestOutcome tuples and record blitz-only-pass entries")
    }
}

#[cfg(test)]
mod tests {
    use super::*;

    /// Confirms the blitz-html / blitz-dom dev-deps actually link in M1.
    /// Execution semantics land in M3.
    #[test]
    fn blitz_dev_dep_links_and_parses_trivial_html() {
        use blitz_dom::DocumentConfig;
        use blitz_html::HtmlDocument;

        let doc = HtmlDocument::from_html("<p>hello</p>", DocumentConfig::default());
        let _base: BlitzDocument = doc.into_inner();
    }
}
```

- [ ] **Step 4: test 通過確認**

```bash
cargo test -p raikiri-wpt --lib 2>&1 | tail -30
```

Expected: 14 tests passed (Task 2 の 11 + Task 3 の 2 + Task 4 の 1)、0 failed。

- [ ] **Step 5: doctest + workspace 影響なし確認**

```bash
cargo test -p raikiri-wpt 2>&1 | tail -15
echo "--- workspace check ---"
cargo check --workspace --all-targets 2>&1 | tail -5
```

Expected: unit tests 14 pass, doc-tests 1 pass、workspace check clean。

- [ ] **Step 6: clippy 検証 (`--all-targets` で bin も含む)**

```bash
cargo clippy -p raikiri-wpt --all-targets -- -D warnings 2>&1 | tail -20
```

Expected: warnings/errors なし。

- [ ] **Step 7: Commit**

```bash
git add crates/raikiri-wpt/src/oracle.rs
git commit -m "$(cat <<'EOF'
feat(raikiri-wpt): oracle module stub + blitz dev-dep smoke test (m1.9)

BlitzOracle / OracleDiff type stubs and BlitzDocument alias per spec
§12.4 (T3 informational). The smoke test drives blitz_html::HtmlDocument
against `<p>hello</p>` to confirm the blitz-html / blitz-dom dev-deps
actually link in this workspace; live diffing lands in M3.

Claude-Session: https://claude.ai/code/session_012eq7kH2wgUjhJwgxvWjYHY
EOF
)"
```

---

## Self-Review Notes (author's own; do not check off)

- **Spec coverage**: §12.9 (tracked / known-issues / expectations format) → Task 1 files + Task 2 parsers. §12.10 (baseline / quarantine 8-col / deprecated / precedence / validate-expectations bin) → Task 1 files + Task 2 parsers + Task 1 bin stub. §12.4 (blitz oracle T3) → Task 4 module + smoke test. §12.6 CI table → out of scope for m1.9 (m1.12 concern). §4 line 1135 (dev-only, WPT runner + blitz oracle) → matches all task deliverables.
- **Placeholder scan**: no TBD/TODO/"add appropriate error handling"; every code block is complete.
- **Type consistency**: `TestOutcome` referenced in `reftest::ReftestResult` matches `runner::TestOutcome`. `ExpectationSet` field types match parser return types. `BlitzDocument` alias used consistently.
- **Follow-up bd 化** (m1.9 close 時): (1) validate-expectations の実 validation logic (M3), (2) expectations/ ファイルへの実 test id populate (M3), (3) `added_date` の chrono/time 化 (M3)。
