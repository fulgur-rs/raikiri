# m1.6 Layout Single-Page — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** raikiri-dom に `layout::layout_single_page(&mut Document, &CascadeResult, PageBox) -> Result<(), LayoutError>` を実装、`<body>` を layout root として PageBox 幅高に固定した taffy 計算 + parley 最小統合による text intrinsic size 測定を wire する。raikiri-traits::PageBox に width / height / `A4` const を populate。M1 hello-world (`<p style="color:red">Hi</p>`) を layout まで通す (paint 側は m1.7)。

**Architecture:** 3 段: (1) raikiri-traits::PageBox に width/height + `A4` const 追加、(2) raikiri-dom Node に `text_layout: Option<parley::Layout<()>>` field 追加 + taffy leaf closure で cached parley Layout の intrinsic size を返す、(3) raikiri-dom::layout module 新設で 5 step の in-place driver (text clear → computed→style bridge (M1.4 no-op) → parley pre-shape → body 発見 + PageBox 幅高セット → compute_root_layout)。全 helper は crate-private、pub は `layout_single_page` と `raikiri-traits::PageBox` 追加 field のみ。

**Tech Stack:** Rust 2024 edition、rust-version 1.89.0、taffy (workspace)、parley 0.10 (workspace)、raikiri-traits / raikiri-style / raikiri-dom (workspace crates)、smol_str 0.3。

## Global Constraints

- 対象 crate: `crates/raikiri-traits` (PageBox 拡張)、`crates/raikiri-dom` (Node.text_layout / layout module / taffy leaf closure)
- Cleanroom 原則: raikiri-traits の pub API に parley / taffy 型を漏らさない (m1.2/m1.4 と同じ責務境界)
- raikiri-dom の pub API には parley::Layout 型を漏らさない (`Node.text_layout` は crate-private、taffy leaf 経由でのみ利用)
- 全新 pub type / field に `#[non_exhaustive]` 維持 — `PageBox { width, height, ..PageBox::A4 }` で external consumer 構築可能 (M1 acceptance)
- Font source: `parley::FontContext::new()` の OS 検索 default (M1.6 では OK、byte-identical strategy は m1.13 で font pinning に置換)
- Layout invalidation: `layout_single_page` は呼び出し毎に全 `Node.text_layout` を clear + 再走 (granular invalidation は M2+)
- 再帰の stack overflow 回避: `find_body` は iterative `Vec` stack (cascade §deep_nesting の pattern と一貫)
- rustc / clippy warnings 0 を維持 (`-D warnings` gate)
- 既存 test の regression 禁止 (build_document / all_three_display_modes_layout_non_degenerate / cascade tests など、workspace 全体)
- 各 task 完了時に commit (frequent commits 原則)
- タスク間の commit message は `<verb>(<scope>): <what>` 形式、末尾に `(raikiri-spike-m1.6)` を含めて追跡容易にする

---

## File Structure

**Modify (raikiri-traits):**
- `crates/raikiri-traits/src/page.rs:31-50` — `PageBox` struct field populate (`width: f32`, `height: f32`) + `pub const A4: PageBox` + `impl Default::default() -> Self::A4` (現在は空 struct + `#[derive(Default)]`)
- `crates/raikiri-traits/src/lib.rs` — 追加 `PageBox` 単体 test (fields readable、A4 sensible values、`#[non_exhaustive]` external constructable 想定)

**Modify (raikiri-dom):**
- `crates/raikiri-dom/src/node.rs:32-59` — `Node` struct に `text_layout: Option<parley::Layout<()>>` field 追加、`#[derive(Debug)]` を手動 impl に降格 (parley::Layout の Debug 有無に応じて選択)
- `crates/raikiri-dom/src/node.rs:60-115` — 3 constructor (`new_document` / `new_element` / `new_text`) を text_layout = None で初期化
- `crates/raikiri-dom/src/taffy_impl.rs:85-119` — `compute_child_layout` の leaf closure を Node.text_layout の intrinsic size を返す形に更新
- `crates/raikiri-dom/src/lib.rs` — `pub mod layout;` 宣言 + `pub use layout::layout_single_page;`

**Create (raikiri-dom):**
- `crates/raikiri-dom/src/layout.rs` — 本タスクの本体。以下を保持:
  - crate-private `find_body(doc: &Document) -> Option<usize>` (iterative DFS)
  - crate-private `apply_computed_to_style(doc: &mut Document, cascade: &CascadeResult)` (M1.4 no-op site)
  - crate-private `apply_page_box_to_body(doc: &mut Document, body_id: usize, page_box: PageBox)`
  - crate-private `preshape_text(doc: &mut Document, cascade: &CascadeResult, fonts: &mut parley::FontContext, layout_cx: &mut parley::LayoutContext<()>, max_advance: f32) -> Result<(), LayoutError>`
  - pub `layout_single_page(...)` driver
  - `#[cfg(test)] mod tests` に 8 unit test

---

## Task 1: raikiri-traits::PageBox に width/height field と `A4` const を populate

**Files:**
- Modify: `crates/raikiri-traits/src/page.rs:31-50`
- Modify: `crates/raikiri-traits/src/lib.rs` (`#[cfg(test)] mod tests` 末尾に追記)

**Interfaces:**
- Consumes: 既存 `PageBox { /* empty */ }` placeholder (`#[non_exhaustive]` + `Default::default()` + `pub fn new()`)
- Produces:
  - `pub struct PageBox { pub width: f32, pub height: f32 }` (`#[non_exhaustive]` 維持)
  - `impl PageBox { pub const A4: PageBox = PageBox { width: 595.0, height: 842.0 }; pub fn new() -> Self { Self::default() } }`
  - `impl Default for PageBox { fn default() -> Self { Self::A4 } }`
  - 追加 test 3 個 (fields readable、Default = A4、external constructable pattern)

**Steps:**

- [ ] **Step 1: 現状確認** — `crates/raikiri-traits/src/page.rs` を Read で開き、既存の `#[derive(Debug, Default, Clone)]` と empty struct 実装を把握。既存 test の `PageBox::default()` 呼び出し箇所 (lib.rs:92) を grep で確認、breaking を回避するため Default 動作を保つ。

- [ ] **Step 2: 失敗する test を lib.rs に追加**

`crates/raikiri-traits/src/lib.rs` の `#[cfg(test)] mod tests` に以下を追加:

```rust
#[test]
fn page_box_a4_has_expected_dimensions() {
    let a4 = PageBox::A4;
    assert_eq!(a4.width, 595.0);
    assert_eq!(a4.height, 842.0);
}

#[test]
fn page_box_default_is_a4() {
    let default = PageBox::default();
    assert_eq!(default, PageBox::A4);
}

#[test]
fn page_box_external_constructable_via_struct_update_from_a4() {
    // #[non_exhaustive] pub struct の external constructable pattern (M1 acceptance)。
    // `..PageBox::A4` を base に width だけ変える。
    let landscape_a4 = PageBox { width: 842.0, height: 595.0, ..PageBox::A4 };
    assert_eq!(landscape_a4.width, 842.0);
    assert_eq!(landscape_a4.height, 595.0);
}
```

- [ ] **Step 3: test の失敗を確認**

Run: `cargo test -p raikiri-traits --lib -- page_box_a4_has_expected_dimensions page_box_default_is_a4 page_box_external_constructable_via_struct_update_from_a4`

Expected: 3 test 全て compile error (`PageBox::A4` 未定義 / `width` field 未定義)

- [ ] **Step 4: 実装**

`crates/raikiri-traits/src/page.rs` の `PageBox` block を以下に置換:

```rust
/// PageBox — @page rule 解決結果 (size, margins, margin box slots)。
/// M1.6 layout-single-page で width / height + `A4` const を populate。
/// margins / margin_boxes は M4 で populate。
#[allow(missing_docs)]
#[derive(Debug, Clone, Copy, PartialEq)]
#[non_exhaustive]
pub struct PageBox {
    /// Page 幅 (CSS pt = 1/72 inch)。M1.6 populate。
    pub width: f32,
    /// Page 高 (CSS pt = 1/72 inch)。M1.6 populate。
    pub height: f32,
    // M4 で populate:
    //   pub margins: Margins,
    //   pub margin_boxes: [Option<MarginBox>; 16],
}

impl PageBox {
    /// A4 portrait (595 × 842 pt)。CSS Paged Media default page size。
    pub const A4: PageBox = PageBox { width: 595.0, height: 842.0 };

    /// Construct a `PageBox` = `A4`。`#[non_exhaustive]` の下でも安定した
    /// zero-arg constructor を残すため保持。
    pub fn new() -> Self {
        Self::default()
    }
}

impl Default for PageBox {
    fn default() -> Self {
        Self::A4
    }
}
```

- [ ] **Step 5: workspace ビルド + 全 test 実行**

Run:
```bash
cargo build --workspace 2>&1 | tail -5
cargo test  --workspace 2>&1 | grep -E "^(test result|FAILED)"
```

Expected: build green、全 test PASS (既存の `let _ = PageBox::default();` @ lib.rs:92 は Default::A4 になるが assert なしなので影響なし)

- [ ] **Step 6: clippy 確認**

Run: `cargo clippy -p raikiri-traits --all-targets -- -D warnings 2>&1 | tail -5`

Expected: 0 warnings

- [ ] **Step 7: Commit**

```bash
git add crates/raikiri-traits/src/page.rs crates/raikiri-traits/src/lib.rs
git commit -m "feat(raikiri-traits): populate PageBox with width/height + A4 const (raikiri-spike-m1.6)

- PageBox に pub width: f32 / pub height: f32 field を populate
- pub const A4: PageBox = PageBox { width: 595.0, height: 842.0 } を追加
- impl Default::default() = Self::A4 に変更 (既存の #[derive(Default)] は削除)
- Clone/Copy/PartialEq derive 追加 (layout API で by-value 渡し + assert 用)
- #[non_exhaustive] 維持、\`..PageBox::A4\` struct update で external consumer 構築可能"
```

---

## Task 2: raikiri-dom::Node に `text_layout: Option<parley::Layout<()>>` field を追加

**Files:**
- Modify: `crates/raikiri-dom/src/node.rs`

**Interfaces:**
- Consumes:
  - Task 1 の変更 (`raikiri-traits::PageBox` はまだ使わない)
  - `parley::Layout<T>` (parley 0.10 workspace dep、Cargo.toml は既に `parley = { workspace = true }`)
- Produces:
  - `Node { ..., pub(crate) text_layout: Option<parley::Layout<()>> }`
  - 3 constructor (`new_document` / `new_element` / `new_text`) が `text_layout: None` で初期化
  - `#[derive(Debug)]` は parley::Layout の Debug 有無に応じて維持 or 手動 impl に降格

**Steps:**

- [ ] **Step 1: parley::Layout の Debug 有無を確認**

Run:
```bash
cargo rustc -p raikiri-dom --lib -- --emit=metadata 2>/dev/null
# 一時 debug check file
cat > /tmp/debug_check.rs << 'EOF'
use parley::Layout;
fn assert_debug<T: std::fmt::Debug>() {}
fn main() { assert_debug::<Layout<()>>(); }
EOF
# 実際は下記 test を Node に inline で追加して確認する方が簡便:
```

代わりに `crates/raikiri-dom/src/node.rs` 末尾に一時 test を追加:

```rust
#[cfg(test)]
mod debug_check {
    use parley::Layout;
    #[test]
    fn parley_layout_has_debug() {
        fn assert_debug<T: std::fmt::Debug>() {}
        assert_debug::<Layout<()>>();
    }
}
```

Run: `cargo test -p raikiri-dom --lib debug_check::parley_layout_has_debug -- --nocapture 2>&1 | tail -10`

Expected: PASS (parley 0.10 の Layout は Debug を実装) or compile error。もし error なら次 Step の代替パスを使用。

- [ ] **Step 2: text_layout field 追加**

`crates/raikiri-dom/src/node.rs:32-59` の Node struct に追加:

```rust
#[derive(Debug)]
pub(crate) struct Node {
    // ... 既存 field 全て ...
    pub(crate) attributes: Vec<Attr>,
    /// Text node の pre-shaped parley Layout。Element / Document は常に None。
    ///
    /// - Populated by [`crate::layout::preshape_text`] (M1.6)
    /// - Consumed by taffy leaf measure closure (intrinsic size) と m1.7 paint
    ///   (glyph 位置)
    /// - Brush type `()` は M1.6 の choice: color / decoration は持たせない
    ///   (paint 段で ComputedValues.color を別途拾う)。M3 で `peniko::Brush`
    ///   等に昇格予定
    /// - Invalidation: `layout_single_page` 呼び出し毎に全 None にクリア +
    ///   再走。granular invalidation は M2+
    pub(crate) text_layout: Option<parley::Layout<()>>,
}
```

Step 1 で parley::Layout が Debug を実装しないと判明した場合は、`#[derive(Debug)]` を外し以下の手動 Debug impl を追加 (末尾に):

```rust
impl std::fmt::Debug for Node {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        f.debug_struct("Node")
            .field("kind", &self.kind)
            .field("tag_name", &self.tag_name)
            .field("text_content", &self.text_content)
            .field("children", &self.children)
            .field("inline_style", &self.inline_style)
            .field("namespace", &self.namespace)
            .field("attributes", &self.attributes)
            .field("style", &"<taffy::Style>")
            .field("cache", &"<taffy::Cache>")
            .field("unrounded_layout", &self.unrounded_layout)
            .field("text_layout", &self.text_layout.as_ref().map(|_| "<parley::Layout>"))
            .finish()
    }
}
```

- [ ] **Step 3: 3 constructor の初期化を追加**

`new_document` / `new_element` / `new_text` の 3 か所すべての Node struct literal に `text_layout: None,` を追加 (各 Self { ... } の末尾フィールドとして)。

Step 1 で追加した debug_check module は削除。

- [ ] **Step 4: workspace ビルド + test**

Run:
```bash
cargo build -p raikiri-dom 2>&1 | tail -5
cargo test -p raikiri-dom 2>&1 | grep -E "^(test result|FAILED)" | tail -20
```

Expected: build green、全既存 test PASS (Node 内部変更なので DOM/layout 系 test は影響なし)

- [ ] **Step 5: clippy 確認**

Run: `cargo clippy -p raikiri-dom --all-targets -- -D warnings 2>&1 | tail -5`

Expected: 0 warnings

- [ ] **Step 6: Commit**

```bash
git add crates/raikiri-dom/src/node.rs
git commit -m "feat(raikiri-dom): add Node.text_layout: Option<parley::Layout<()>> field (raikiri-spike-m1.6)

- Text node の pre-shaped parley Layout を cache する field を追加
- Element / Document node は常に None
- 3 constructor (new_document / new_element / new_text) を text_layout: None で初期化
- Debug impl は parley::Layout の Debug 有無に応じて derive 維持 or 手動降格
- M1.6 preshape_text pass が populate、taffy leaf closure が intrinsic size を消費、
  m1.7 paint が glyph 位置として再利用"
```

---

## Task 3: raikiri-dom::layout module 新設 + `find_body` iterative DFS

**Files:**
- Create: `crates/raikiri-dom/src/layout.rs`
- Modify: `crates/raikiri-dom/src/lib.rs`

**Interfaces:**
- Consumes:
  - `crate::document::Document` (index-based arena)
  - `raikiri_traits::NodeKind`
- Produces:
  - crate-private `fn find_body(doc: &Document) -> Option<usize>` (iterative DFS)
  - `#[cfg(test)] mod tests` に 3 個の test (basic / missing / deep-nesting)

**Steps:**

- [ ] **Step 1: layout.rs skeleton を作成**

`crates/raikiri-dom/src/layout.rs` を新規作成:

```rust
//! Single-page layout driver — `layout_single_page` を pub 提供。
//!
//! Pipeline: cascade (raikiri-style) 出力 + Document arena + PageBox から
//! taffy compute_root_layout を駆動し、text intrinsic size は parley 0.10 の
//! 最小統合で pre-shape する。M1.6 scope: 単一 A4 ページ、ASCII Latin、
//! parley system font default (byte-identical cross-machine は m1.13 で font pinning)。
//!
//! 全 helper は crate-private、pub 型は [`layout_single_page`] のみ。

use raikiri_traits::NodeKind;

use crate::document::Document;

/// Document arena を DFS で walk し、最初の `<body>` element の arena index を返す。
///
/// iterative `Vec` stack で実装 (cascade §deep_nesting の pattern と一貫、
/// deep DOM で stack overflow を回避)。fragment parse (no `<body>`) では
/// `None`、caller が `LayoutError::Internal` に昇格させる。
pub(crate) fn find_body(doc: &Document) -> Option<usize> {
    let mut stack: Vec<usize> = vec![doc.root];
    while let Some(node_idx) = stack.pop() {
        let node = &doc.nodes[node_idx];
        if node.kind == NodeKind::Element
            && node.tag_name.as_deref() == Some("body")
        {
            return Some(node_idx);
        }
        // children を reverse push すると document order で pop される
        for &child in node.children.iter().rev() {
            stack.push(child);
        }
    }
    None
}

#[cfg(test)]
mod tests {
    use super::*;
    use taffy::Style;

    #[test]
    fn find_body_returns_index_when_present() {
        let mut doc = Document::new();
        let html = doc.append_element(Some(0), "html", Style::default(), None::<&str>);
        let _head = doc.append_element(Some(html), "head", Style::default(), None::<&str>);
        let body = doc.append_element(Some(html), "body", Style::default(), None::<&str>);
        assert_eq!(find_body(&doc), Some(body));
    }

    #[test]
    fn find_body_returns_none_when_absent() {
        // Fragment 相当: <p> を Document root 直下に append、<body> なし
        let mut doc = Document::new();
        let _p = doc.append_element(Some(0), "p", Style::default(), None::<&str>);
        assert_eq!(find_body(&doc), None);
    }

    #[test]
    fn find_body_iterative_no_stack_overflow_on_deep_dom() {
        // 5000 深さで stack overflow を起こさず None を返す。
        // cascade §deep_nesting_5000_cascade_no_overflow と同水準の regression pin。
        let mut doc = Document::new();
        let mut parent = 0usize;
        for _ in 0..5000 {
            parent = doc.append_element(Some(parent), "div", Style::default(), None::<&str>);
        }
        assert_eq!(find_body(&doc), None);
    }

    #[test]
    fn find_body_returns_first_body_in_document_order() {
        // 2 個の <body> がある病理的なケースでは最初の document order の <body> を返す
        // (html5ever は 1 個しか作らない想定だが、defensive contract を pin)
        let mut doc = Document::new();
        let html = doc.append_element(Some(0), "html", Style::default(), None::<&str>);
        let body1 = doc.append_element(Some(html), "body", Style::default(), None::<&str>);
        let _body2 = doc.append_element(Some(html), "body", Style::default(), None::<&str>);
        assert_eq!(find_body(&doc), Some(body1));
    }
}
```

- [ ] **Step 2: lib.rs で layout module を宣言**

`crates/raikiri-dom/src/lib.rs` の module 宣言セクション (現在 `pub mod document; pub mod dom_impl; pub mod taffy_impl;` の block) に追加:

```rust
pub mod document;
pub mod dom_impl;
pub mod layout;  // ← 追加
pub mod taffy_impl;
```

- [ ] **Step 3: test 実行**

Run: `cargo test -p raikiri-dom --lib layout::tests -- --nocapture 2>&1 | tail -20`

Expected: 4 test 全て PASS

- [ ] **Step 4: workspace 全体で regression 確認**

Run:
```bash
cargo build --workspace 2>&1 | tail -3
cargo test --workspace 2>&1 | grep -E "^test result" | tail -30
```

Expected: 全 test PASS

- [ ] **Step 5: clippy 確認**

Run: `cargo clippy -p raikiri-dom --all-targets -- -D warnings 2>&1 | tail -5`

Expected: 0 warnings

- [ ] **Step 6: Commit**

```bash
git add crates/raikiri-dom/src/layout.rs crates/raikiri-dom/src/lib.rs
git commit -m "feat(raikiri-dom): new layout module + find_body iterative DFS (raikiri-spike-m1.6)

- crates/raikiri-dom/src/layout.rs を新設 (単一ページ layout driver 用)
- crate-private fn find_body(doc: &Document) -> Option<usize> を追加
- iterative Vec stack 実装 (cascade §deep_nesting の pattern と一貫、
  deep DOM で stack overflow を回避)
- 4 unit test: basic body 発見 / body 不在 / 5000 深 nesting / document order tie-break"
```

---

## Task 4: `apply_page_box_to_body` と `apply_computed_to_style` (M1.4 no-op bridge) を追加

**Files:**
- Modify: `crates/raikiri-dom/src/layout.rs`

**Interfaces:**
- Consumes:
  - `crate::document::Document`
  - `raikiri_traits::PageBox` (Task 1 の産物)
  - `raikiri_style::CascadeResult` (existing、m1.4 の産物)
- Produces:
  - crate-private `fn apply_page_box_to_body(doc, body_id, page_box)`
  - crate-private `fn apply_computed_to_style(doc, cascade)` (M1.4 では no-op body、Element ごとに future property を merge する site)

**Steps:**

- [ ] **Step 1: 失敗する test を追加**

`crates/raikiri-dom/src/layout.rs` の `#[cfg(test)] mod tests` に追加:

```rust
#[test]
fn apply_page_box_to_body_sets_body_style_size_to_page_dimensions() {
    use raikiri_traits::PageBox;
    use taffy::{Dimension, Size};

    let mut doc = Document::new();
    let html = doc.append_element(Some(0), "html", Style::default(), None::<&str>);
    let body = doc.append_element(Some(html), "body", Style::default(), None::<&str>);

    apply_page_box_to_body(&mut doc, body, PageBox::A4);

    let size: Size<Dimension> = doc.nodes[body].style.size;
    assert_eq!(size.width, Dimension::length(595.0));
    assert_eq!(size.height, Dimension::length(842.0));
}

#[test]
fn apply_computed_to_style_is_noop_at_m1_4() {
    // M1.4 の 4 property (color / font-family / font-size / font-weight) は
    // 全て inherited かつ text-only、taffy::Style を変えない。この test は
    // M4 で display / margin / width / height 等の layout property が入った時に
    // ここの assertion が「変える」に反転する: bridge site が正しく機能している
    // かの regression pin。
    use raikiri_style::{build_rule_tree, cascade};

    let mut doc = Document::new();
    let html = doc.append_element(Some(0), "html", Style::default(), None::<&str>);
    let body = doc.append_element(Some(html), "body", Style::default(), Some("color:red"));
    let rules = build_rule_tree(&doc);
    let cr = cascade(&doc, &rules).expect("cascade Ok");

    let before = doc.nodes[body].style.clone();
    apply_computed_to_style(&mut doc, &cr);
    let after = doc.nodes[body].style.clone();

    // M1.4 では size / margin / padding など layout に効く field は変えない
    assert_eq!(before.size, after.size, "size unchanged at M1.4");
    assert_eq!(before.display, after.display, "display unchanged at M1.4");
    assert_eq!(before.margin, after.margin, "margin unchanged at M1.4");
    assert_eq!(before.padding, after.padding, "padding unchanged at M1.4");
}
```

- [ ] **Step 2: test の失敗を確認**

Run: `cargo test -p raikiri-dom --lib layout::tests::apply_ 2>&1 | tail -10`

Expected: compile error (関数 `apply_page_box_to_body` / `apply_computed_to_style` 未定義)

- [ ] **Step 3: 実装追加**

`crates/raikiri-dom/src/layout.rs` の `find_body` の下に追加 (imports は上部に追加):

```rust
use raikiri_style::CascadeResult;
use raikiri_traits::PageBox;
use taffy::{Dimension, Size};

/// `<body>` の taffy::Style.size を PageBox の width / height (CSS pt) に強制する。
///
/// CSS Paged Media の initial containing block = @page size。M1 は @page 非対応
/// のため body.style.size に直接注入する妥協。M4 で @page cascade + per-page
/// PageBox を導入時に `<html>` root style に site を昇格予定。
pub(crate) fn apply_page_box_to_body(
    doc: &mut Document,
    body_id: usize,
    page_box: PageBox,
) {
    doc.nodes[body_id].style.size = Size {
        width: Dimension::length(page_box.width),
        height: Dimension::length(page_box.height),
    };
}

/// ComputedValues → taffy::Style bridge の site。
///
/// M1.4 property (color / font-family / font-size / font-weight) は全て
/// inherited text-only property で taffy::Style を変えないため、M1.6 では
/// 本体 no-op。M4 で display / margin / padding / width / height 等の
/// layout property が加わった時、ここに merge ロジックを追加する。
/// (この関数の存在自体が M1.6 の site 確立の遺産。)
pub(crate) fn apply_computed_to_style(
    _doc: &mut Document,
    _cascade: &CascadeResult,
) {
    // M1.4 では no-op。M4 で ComputedValues に display / size / margin / padding
    // 等が加わった時、下記のような per-element loop を追加:
    //
    // for idx in 0..doc.nodes.len() {
    //     if doc.nodes[idx].kind != NodeKind::Element { continue; }
    //     let cv = &cascade.computed[idx];
    //     merge_layout_properties(&mut doc.nodes[idx].style, cv);
    // }
}
```

- [ ] **Step 4: test を再走**

Run: `cargo test -p raikiri-dom --lib layout::tests -- --nocapture 2>&1 | tail -20`

Expected: 6 test 全 PASS (Task 3 の 4 + 追加 2)

- [ ] **Step 5: clippy 確認**

Run: `cargo clippy -p raikiri-dom --all-targets -- -D warnings 2>&1 | tail -5`

Expected: 0 warnings

- [ ] **Step 6: Commit**

```bash
git add crates/raikiri-dom/src/layout.rs
git commit -m "feat(raikiri-dom): add apply_page_box_to_body + no-op apply_computed_to_style bridge (raikiri-spike-m1.6)

- apply_page_box_to_body: body.style.size を PageBox.{width,height} の
  Dimension::length で強制上書き (initial containing block = @page size 相当)
- apply_computed_to_style: M1.4 では no-op、M4 で display/margin/padding が
  ComputedValues に入った時に merge site として拡張予定。今この site を
  確立することが本 task の遺産
- 単体 test 2 個追加 (body.size 適用検証、M1.4 bridge no-op regression pin)"
```

---

## Task 5: `preshape_text` — parley 0.10 で全 Text node を pre-shape

**Files:**
- Modify: `crates/raikiri-dom/src/layout.rs`

**Interfaces:**
- Consumes:
  - `crate::document::Document`
  - `raikiri_style::CascadeResult`
  - `parley::{FontContext, LayoutContext, FontStack, StyleProperty, FontWeight, Alignment, AlignmentOptions, Layout}`
  - `raikiri_traits::{LayoutError, NodeKind}`
- Produces:
  - crate-private `fn preshape_text(doc, cascade, fonts, layout_cx, max_advance) -> Result<(), LayoutError>`
  - 各 Text node の `Node.text_layout` が `Some(parley::Layout<()>)` に populate される

**Steps:**

- [ ] **Step 1: parley 0.10 の実 API を確認**

Run:
```bash
cargo doc -p parley --no-deps 2>&1 | tail -5
# open target/doc/parley/index.html locally (optional)
# 主に確認する型: FontContext, LayoutContext, RangedBuilder, FontStack,
# StyleProperty::FontSize / FontWeight, Layout<T>::break_all_lines /
# align / width / height
```

もし後の実装で下記 pseudo-code の API 名が異なった場合 (RangedBuilder::push_default 引数型、FontStack のバリアント等)、コンパイラエラーメッセージに従い最小差分で修正する。design 全体構造は変更なし。

- [ ] **Step 2: 失敗する test を追加**

`crates/raikiri-dom/src/layout.rs` の tests module に追加:

```rust
#[test]
fn preshape_text_populates_text_layout_for_text_nodes() {
    use parley::{FontContext, LayoutContext};
    use raikiri_style::{build_rule_tree, cascade};

    let mut doc = Document::new();
    let html = doc.append_element(Some(0), "html", Style::default(), None::<&str>);
    let body = doc.append_element(Some(html), "body", Style::default(), None::<&str>);
    let p = doc.append_element(Some(body), "p", Style::default(), None::<&str>);
    let text = doc.append_text(p, "Hi");

    let rules = build_rule_tree(&doc);
    let cr = cascade(&doc, &rules).expect("cascade Ok");

    let mut fonts = FontContext::new();
    let mut layout_cx = LayoutContext::<()>::new();
    preshape_text(&mut doc, &cr, &mut fonts, &mut layout_cx, 595.0).expect("preshape Ok");

    assert!(
        doc.nodes[text].text_layout.is_some(),
        "text node's text_layout must be populated"
    );
    let layout = doc.nodes[text].text_layout.as_ref().unwrap();
    assert!(layout.width() > 0.0, "text 'Hi' must have non-zero width");
    assert!(layout.height() > 0.0, "text 'Hi' must have non-zero line height");

    // Element / Document は None のまま
    assert!(doc.nodes[html].text_layout.is_none(), "html element is not text");
    assert!(doc.nodes[body].text_layout.is_none(), "body element is not text");
    assert!(doc.nodes[p].text_layout.is_none(), "p element is not text");
    assert!(doc.nodes[0].text_layout.is_none(), "document root is not text");
}

#[test]
fn preshape_text_respects_computed_font_size() {
    use parley::{FontContext, LayoutContext};
    use raikiri_style::{build_rule_tree, cascade};

    fn shape_text_height_at_font_size(px: &str) -> f32 {
        let mut doc = Document::new();
        let html = doc.append_element(Some(0), "html", Style::default(), None::<&str>);
        let body = doc.append_element(Some(html), "body", Style::default(), None::<&str>);
        let inline = format!("font-size:{}", px);
        let p = doc.append_element(Some(body), "p", Style::default(), Some(inline.as_str()));
        let text = doc.append_text(p, "Hi");
        let rules = build_rule_tree(&doc);
        let cr = cascade(&doc, &rules).unwrap();
        let mut fonts = FontContext::new();
        let mut layout_cx = LayoutContext::<()>::new();
        preshape_text(&mut doc, &cr, &mut fonts, &mut layout_cx, 595.0).unwrap();
        doc.nodes[text].text_layout.as_ref().unwrap().height()
    }

    let small = shape_text_height_at_font_size("8px");
    let large = shape_text_height_at_font_size("32px");
    assert!(
        large > small,
        "font-size:32px must produce taller text than 8px (cascade→shape inheritance regression pin, got small={small}, large={large})"
    );
}
```

- [ ] **Step 3: test の失敗を確認**

Run: `cargo test -p raikiri-dom --lib layout::tests::preshape_ 2>&1 | tail -10`

Expected: compile error (関数 `preshape_text` 未定義)

- [ ] **Step 4: 実装追加**

`crates/raikiri-dom/src/layout.rs` の imports に追加:

```rust
use parley::{
    Alignment, AlignmentOptions, FontContext, FontStack, FontWeight, Layout,
    LayoutContext, StyleProperty,
};
use raikiri_style::property::Length;
use raikiri_traits::LayoutError;
```

`apply_computed_to_style` の下に `preshape_text` 追加:

```rust
/// 全 Text node を parley で pre-shape、結果を `Node.text_layout` に格納する。
///
/// 呼び出し側 (`layout_single_page`) は事前に全 `Node.text_layout = None` に
/// clear 済であることを前提とする (re-entrance safety)。
///
/// Font stack / size / weight は `cascade.computed[idx]` (親から inherit 済) を消費。
/// `max_advance` は行折り返し境界で、通常 `page_box.width`。
///
/// # Errors
/// - `LayoutError::Internal` — parley shape が想定外の状態で失敗した場合
///   (M1 ASCII 前提では発生想定なし、defensive)
pub(crate) fn preshape_text(
    doc: &mut Document,
    cascade: &CascadeResult,
    fonts: &mut FontContext,
    layout_cx: &mut LayoutContext<()>,
    max_advance: f32,
) -> Result<(), LayoutError> {
    for idx in 0..doc.nodes.len() {
        if doc.nodes[idx].kind != NodeKind::Text {
            continue;
        }
        let text: String = match &doc.nodes[idx].text_content {
            Some(s) if !s.is_empty() => s.as_str().to_string(),
            _ => continue,
        };
        // cascade は Text node 位置にも ComputedValues を populate する
        // (親から inherit)。M1.4 test `text_node_inherits_from_element_parent`
        // で確認済。
        let cv = &cascade.computed[idx];

        // font-family: Vec<Atom> → parley::FontStack。Atom は SmolStr newtype
        // なので as_str() で &str に落として parley に食わせる。
        let family_str: String = cv
            .font_family
            .iter()
            .map(|a| a.0.as_str())
            .collect::<Vec<_>>()
            .join(", ");
        let font_stack = FontStack::from(family_str.as_str());

        let font_size_px = match cv.font_size {
            Length::Px(v) => v,
        };

        let mut builder = layout_cx.ranged_builder(fonts, &text, 1.0, true);
        builder.push_default(font_stack);
        builder.push_default(StyleProperty::FontSize(font_size_px));
        builder.push_default(StyleProperty::FontWeight(FontWeight::new(
            cv.font_weight as f32,
        )));
        let mut layout: Layout<()> = builder.build(&text);
        layout.break_all_lines(Some(max_advance));
        layout.align(Some(max_advance), Alignment::Start, AlignmentOptions::default());

        doc.nodes[idx].text_layout = Some(layout);
    }
    Ok(())
}
```

**API 差分の対処ガイド**:
- `FontStack::from(&str)` が無ければ `FontStack::Single(FontFamily::Named(family_str.into()))` 相当を使う (parley 0.10 の実 enum を doc から確認)
- `ranged_builder(fonts, &text, 1.0, true)` の 3 引数目は scale、4 引数目は `quantize`。API 差があれば `ranged_builder(fonts, &text, 1.0)` にフォールバック
- `StyleProperty::FontSize(f32)` / `StyleProperty::FontWeight(FontWeight)` は parley 0.10 で存在確認済 (feasibility nzv.5)
- `Layout::break_all_lines(Some(max_advance))` の返り型は () だが古い版で戻り値がある場合は捨てる
- `Alignment::Start` / `AlignmentOptions::default()` は parley 0.10 に存在

- [ ] **Step 5: test 実行**

Run: `cargo test -p raikiri-dom --lib layout::tests::preshape_ -- --nocapture 2>&1 | tail -30`

Expected: 2 test PASS。1 目 = Hi の shape が非零、Element / Document は None。2 目 = font-size 32px は 8px より tall。

- [ ] **Step 6: workspace 全体で regression 確認**

Run:
```bash
cargo test --workspace 2>&1 | grep -E "^test result" | tail -30
```

Expected: 全 test PASS

- [ ] **Step 7: clippy 確認**

Run: `cargo clippy -p raikiri-dom --all-targets -- -D warnings 2>&1 | tail -5`

Expected: 0 warnings

- [ ] **Step 8: Commit**

```bash
git add crates/raikiri-dom/src/layout.rs
git commit -m "feat(raikiri-dom): preshape_text pass with parley 0.10 minimal integration (raikiri-spike-m1.6)

- 全 Text node を parley で pre-shape、Node.text_layout に格納
- cascade で inherit された ComputedValues (font-family / font-size /
  font-weight) を parley StyleProperty に mapping
- max_advance = page_box.width で行折り返し
- FontContext は caller (layout_single_page) が per-call new()、
  determinism strategy は M2+ で Document 昇格 or Consumer inject に変更予定
- 2 unit test: text intrinsic size 非零 + font-size 反映 (cascade→shape
  inheritance regression pin)"
```

---

## Task 6: taffy leaf closure を Node.text_layout の intrinsic size を返す形に更新

**Files:**
- Modify: `crates/raikiri-dom/src/taffy_impl.rs:85-119`

**Interfaces:**
- Consumes: Task 2 で追加した `Node.text_layout: Option<parley::Layout<()>>`
- Produces: 現在 `Size::ZERO` を返す leaf measure closure が、text_layout がある場合はその width / height を返す

**Steps:**

- [ ] **Step 1: 現状の leaf closure を Read で把握**

`crates/raikiri-dom/src/taffy_impl.rs:85-119` を Read。既存の `is_leaf` 分岐の `compute_leaf_layout` 呼び出し (行 102-109) の measure closure `|_known, _avail| Size::ZERO` を対象に更新する。

- [ ] **Step 2: 失敗する test を追加**

`crates/raikiri-dom/src/lib.rs` の tests module に追加:

```rust
#[test]
fn taffy_leaf_measure_reads_pre_populated_text_layout() {
    // Node.text_layout に手動で parley Layout をセットして、taffy leaf closure が
    // その intrinsic size を返すことを直接検証する (layout_single_page 経由
    // ではなく leaf closure の pin として)。
    use parley::{FontContext, LayoutContext, Alignment, AlignmentOptions};
    use taffy::{AvailableSpace, NodeId as TaffyNodeId, Size};

    let mut doc = Document::new();
    let root = doc.append_element(
        Some(0),
        "root",
        Style {
            display: Display::Block,
            size: Size {
                width: Dimension::length(400.0),
                height: Dimension::auto(),
            },
            ..Default::default()
        },
        None::<&str>,
    );
    let text = doc.append_text(root, "Hi");

    // 手動 pre-shape
    let mut fonts = FontContext::new();
    let mut layout_cx = LayoutContext::<()>::new();
    let mut builder = layout_cx.ranged_builder(&mut fonts, "Hi", 1.0, true);
    let mut layout = builder.build("Hi");
    layout.break_all_lines(Some(400.0));
    layout.align(Some(400.0), Alignment::Start, AlignmentOptions::default());
    let expected_h = layout.height();
    doc.nodes[text].text_layout = Some(layout);

    compute_root_layout(
        &mut doc,
        TaffyNodeId::from(root),
        Size {
            width: AvailableSpace::Definite(800.0),
            height: AvailableSpace::Definite(600.0),
        },
    );

    let text_size = doc.nodes[text].unrounded_layout.size;
    assert!(
        text_size.width > 0.0,
        "text leaf must have non-zero width from parley layout (got {})",
        text_size.width
    );
    // root の block layout 経由で text leaf の高さが flow するはず
    let root_size = doc.nodes[root].unrounded_layout.size;
    assert!(
        (root_size.height - expected_h).abs() < 0.5,
        "root block should stack single text child at parley height {expected_h} (got {})",
        root_size.height
    );
}
```

- [ ] **Step 3: test の失敗を確認**

Run: `cargo test -p raikiri-dom --lib tests::taffy_leaf_measure_reads_pre_populated_text_layout -- --nocapture 2>&1 | tail -10`

Expected: FAIL — 現在 leaf closure が Size::ZERO を返すため root_size.height = 0

- [ ] **Step 4: leaf closure を更新**

`crates/raikiri-dom/src/taffy_impl.rs:100-109` を以下に置換:

```rust
let is_leaf = tree.nodes[idx].children.is_empty();
if is_leaf {
    let style = tree.nodes[idx].style.clone();
    let text_intrinsic: Option<Size<f32>> =
        tree.nodes[idx].text_layout.as_ref().map(|l| Size {
            width: l.width(),
            height: l.height(),
        });
    compute_leaf_layout(
        inputs,
        &style,
        |_val, _basis| 0.0,
        |known, _avail| {
            // taffy が style から算出した known.width / .height が Some なら
            // それを優先 (explicit size)、None なら parley intrinsic を使う、
            // 両方無ければ 0。
            Size {
                width: known.width.or(text_intrinsic.map(|s| s.width)).unwrap_or(0.0),
                height: known
                    .height
                    .or(text_intrinsic.map(|s| s.height))
                    .unwrap_or(0.0),
            }
        },
    )
}
```

- [ ] **Step 5: test 再走 + regression 確認**

Run:
```bash
cargo test -p raikiri-dom --lib tests::taffy_leaf_measure_reads_pre_populated_text_layout -- --nocapture 2>&1 | tail -10
cargo test -p raikiri-dom --lib 2>&1 | grep -E "^test result" | tail -10
```

Expected: 対象 test PASS + `all_three_display_modes_layout_non_degenerate` / `empty_display_none_leaf_produces_hidden_layout` / `layout_cache_invalidated_after_mutation` / `many_mutations_still_yield_correct_layout` など既存 layout test も全 PASS (element leaf は `text_layout = None` なので既存挙動 = Size::ZERO は保たれる)

- [ ] **Step 6: workspace 全体で regression 確認**

Run: `cargo test --workspace 2>&1 | grep -E "^test result" | tail -30`

Expected: 全 test PASS

- [ ] **Step 7: clippy 確認**

Run: `cargo clippy -p raikiri-dom --all-targets -- -D warnings 2>&1 | tail -5`

Expected: 0 warnings

- [ ] **Step 8: Commit**

```bash
git add crates/raikiri-dom/src/taffy_impl.rs crates/raikiri-dom/src/lib.rs
git commit -m "feat(raikiri-dom): taffy leaf closure reads Node.text_layout intrinsic size (raikiri-spike-m1.6)

- compute_child_layout の leaf 分岐 (compute_leaf_layout の measure closure)
  を Node.text_layout の parley::Layout width/height を返す形に更新
- 優先度: known (style explicit size) > text_intrinsic > 0
- element leaf (text_layout = None) は既存挙動 (Size::ZERO) を維持、
  regression なし
- pre-shape 済 parley::Layout を手動でセットして root block layout に flow
  することを検証する unit test を追加"
```

---

## Task 7: `layout_single_page` pub entrypoint + 4 integration test

**Files:**
- Modify: `crates/raikiri-dom/src/layout.rs`
- Modify: `crates/raikiri-dom/src/lib.rs`

**Interfaces:**
- Consumes: Task 1〜6 の全産物
- Produces:
  - `pub fn layout_single_page(document: &mut Document, cascade: &CascadeResult, page_box: PageBox) -> Result<(), LayoutError>`
  - `pub use layout::layout_single_page;` in lib.rs

**Steps:**

- [ ] **Step 1: 失敗する test を追加**

`crates/raikiri-dom/src/layout.rs` の tests module に追加:

```rust
fn hello_world_doc() -> (Document, raikiri_style::CascadeResult) {
    // <html><head></head><body><p style="color:red">Hi</p></body></html>
    // 相当 (parser の代わりに手動構築、raikiri-html 統合は m1.7+ で umbrella が担当)
    use raikiri_style::{build_rule_tree, cascade};
    let mut doc = Document::new();
    let html = doc.append_element(Some(0), "html", Style::default(), None::<&str>);
    let _head = doc.append_element(Some(html), "head", Style::default(), None::<&str>);
    let body = doc.append_element(Some(html), "body", Style::default(), None::<&str>);
    let p = doc.append_element(Some(body), "p", Style::default(), Some("color:red"));
    let _text = doc.append_text(p, "Hi");
    let rules = build_rule_tree(&doc);
    let cr = cascade(&doc, &rules).expect("cascade Ok");
    (doc, cr)
}

#[test]
fn layout_single_page_hello_world_produces_body_at_page_width() {
    use raikiri_traits::PageBox;
    let (mut doc, cr) = hello_world_doc();
    layout_single_page(&mut doc, &cr, PageBox::A4).expect("layout Ok");
    // body の layout size.width が A4 幅 (595) と一致
    let body_id = find_body(&doc).expect("body exists");
    let body_size = doc.nodes[body_id].unrounded_layout.size;
    assert!(
        (body_size.width - 595.0).abs() < 0.5,
        "body width should be A4.width (595), got {}",
        body_size.width
    );
    assert!(
        body_size.height > 0.0,
        "body height should be non-zero from block layout of <p>Hi</p>, got {}",
        body_size.height
    );
}

#[test]
fn layout_single_page_without_body_returns_error() {
    use raikiri_style::{build_rule_tree, cascade};
    use raikiri_traits::{LayoutError, PageBox};

    // <p> 直接 attach (fragment 相当)
    let mut doc = Document::new();
    let _p = doc.append_element(Some(0), "p", Style::default(), None::<&str>);
    let rules = build_rule_tree(&doc);
    let cr = cascade(&doc, &rules).unwrap();

    match layout_single_page(&mut doc, &cr, PageBox::A4) {
        Err(LayoutError::Internal { message }) => {
            assert!(
                message.contains("body"),
                "error message should mention <body>, got '{}'",
                message
            );
        }
        other => panic!("expected LayoutError::Internal, got {:?}", other),
    }
}

#[test]
fn layout_single_page_can_be_called_multiple_times() {
    use raikiri_traits::PageBox;
    let (mut doc, cr) = hello_world_doc();
    layout_single_page(&mut doc, &cr, PageBox::A4).expect("first call Ok");
    let body_id = find_body(&doc).expect("body exists");
    let first_size = doc.nodes[body_id].unrounded_layout.size;

    // 2 回目呼び出し — text_layout の re-entrance clear と layout の再走が
    // 同じ結果を返すことを pin (将来 incremental optimization が silent
    // regression を起こしても検出できる)
    layout_single_page(&mut doc, &cr, PageBox::A4).expect("second call Ok");
    let second_size = doc.nodes[body_id].unrounded_layout.size;

    assert!((first_size.width - second_size.width).abs() < 0.001);
    assert!((first_size.height - second_size.height).abs() < 0.001);
}

#[test]
fn layout_single_page_body_bridge_is_noop_at_m1_4() {
    // bridge site (apply_computed_to_style) が body.style の layout-affecting
    // field を変えないことを確認 (M4 で display / margin / padding が入る時に
    // ここが反転する)
    use raikiri_traits::PageBox;
    let (mut doc, cr) = hello_world_doc();
    let body_id = find_body(&doc).expect("body exists");
    let before = doc.nodes[body_id].style.display;
    layout_single_page(&mut doc, &cr, PageBox::A4).expect("layout Ok");
    let after = doc.nodes[body_id].style.display;
    assert_eq!(before, after, "display unchanged by M1.4 bridge");
    // apply_page_box_to_body により size は変わるので size は assert 対象外
}
```

- [ ] **Step 2: test の失敗を確認**

Run: `cargo test -p raikiri-dom --lib layout::tests::layout_single_page_ 2>&1 | tail -10`

Expected: compile error (`layout_single_page` 未定義)

- [ ] **Step 3: 実装追加**

`crates/raikiri-dom/src/layout.rs` の imports に追加:

```rust
use taffy::{AvailableSpace, NodeId as TaffyNodeId, compute_root_layout};
```

module 末尾に `layout_single_page` 追加:

```rust
/// 単一 A4 (or 指定 PageBox) ページに Document を layout する。
///
/// # 変更 (in-place)
/// - Node.text_layout を全 `None` にクリア (re-entrance safety)
/// - `apply_computed_to_style` で computed → taffy::Style bridge (M1.4 no-op)
/// - `preshape_text` で全 Text node を parley shape、Node.text_layout に格納
/// - `apply_page_box_to_body` で body.style.size = length(PageBox)
/// - `compute_root_layout` で taffy 計算、Node.unrounded_layout に書き込む
///
/// # Errors
/// - `LayoutError::Internal` — `<body>` element が見つからない (fragment
///   parse は M1 非対応) / parley shape が失敗 / taffy internal
///
/// # Non-goals in M1.6
/// - 同じ Document で複数回呼ぶことは safe (text_layout を毎回 clear) だが、
///   incremental (差分だけ再走) は M2+ で追加
/// - Consumer からの PageBox 上書きは M4 per-page PageBox で対応
/// - Fragment parse (no `<body>`) support は M2+
pub fn layout_single_page(
    document: &mut Document,
    cascade: &CascadeResult,
    page_box: PageBox,
) -> Result<(), LayoutError> {
    // Step 0: text_layout re-entrance clear
    for node in document.nodes.iter_mut() {
        node.text_layout = None;
    }

    // Step 1: ComputedValues → taffy::Style bridge (M1.4 no-op site)
    apply_computed_to_style(document, cascade);

    // Step 2: pre-shape all text with parley
    let mut fonts = FontContext::new();
    let mut layout_cx = LayoutContext::<()>::new();
    preshape_text(document, cascade, &mut fonts, &mut layout_cx, page_box.width)?;

    // Step 3: <body> lookup
    let body_id = find_body(document).ok_or_else(|| LayoutError::Internal {
        message: "no <body> element found (fragment parse not supported in M1)".to_string(),
    })?;

    // Step 4: body.style.size を PageBox に強制セット
    apply_page_box_to_body(document, body_id, page_box);

    // Step 5: taffy compute
    compute_root_layout(
        document,
        TaffyNodeId::from(body_id),
        taffy::Size {
            width: AvailableSpace::Definite(page_box.width),
            height: AvailableSpace::Definite(page_box.height),
        },
    );

    Ok(())
}
```

- [ ] **Step 4: lib.rs で pub re-export**

`crates/raikiri-dom/src/lib.rs` の re-export 群 (現在 `pub use document::Document;` 等の block) に追加:

```rust
pub use document::Document;
pub use dom_impl::{ChildIter, ElementRef, NodeRef};
pub use layout::layout_single_page;   // ← 追加
```

- [ ] **Step 5: test 実行**

Run: `cargo test -p raikiri-dom --lib layout::tests -- --nocapture 2>&1 | tail -30`

Expected: 全 test PASS (Task 3+4+5 + 追加 4 = 合計 10 test)

- [ ] **Step 6: workspace 全体で regression 確認**

Run: `cargo test --workspace 2>&1 | grep -E "^test result" | tail -30`

Expected: 全 test PASS

- [ ] **Step 7: clippy 確認**

Run: `cargo clippy -p raikiri-dom --all-targets -- -D warnings 2>&1 | tail -5`

Expected: 0 warnings

- [ ] **Step 8: Commit**

```bash
git add crates/raikiri-dom/src/layout.rs crates/raikiri-dom/src/lib.rs
git commit -m "feat(raikiri-dom): pub layout_single_page(&mut Document, &CascadeResult, PageBox) (raikiri-spike-m1.6)

- 5 step driver: text_layout clear → computed→style bridge → parley pre-shape →
  find_body / apply_page_box_to_body → compute_root_layout
- Fragment (no <body>) は LayoutError::Internal で fail-fast
- 同一 Document で複数回呼び出し safe (text_layout の re-entrance clear pin)
- 4 integration test: hello-world body size / no-body error / re-entrance /
  M1.4 bridge no-op regression
- pub use layout::layout_single_page で raikiri-dom re-export"
```

---

## Task 8: Determinism regression + acceptance final gate

**Files:**
- Modify: `crates/raikiri-dom/src/layout.rs`

**Interfaces:**
- Consumes: Task 7 の `layout_single_page`
- Produces: 10 連ラン byte-identical regression test + workspace final gate 確認

**Steps:**

- [ ] **Step 1: 10 連ラン determinism test を追加**

`crates/raikiri-dom/src/layout.rs` の tests module に追加:

```rust
#[test]
fn layout_single_page_deterministic_across_10_runs() {
    // M1 acceptance: 10 回連続実行で byte-identical。
    // 同一マシン上の determinism を pin (cross-machine は m1.13 で font
    // pinning に置き換わる)。
    use raikiri_traits::PageBox;

    fn one_run() -> Vec<taffy::Layout> {
        let (mut doc, cr) = hello_world_doc();
        layout_single_page(&mut doc, &cr, PageBox::A4).expect("layout Ok");
        doc.nodes.iter().map(|n| n.unrounded_layout).collect()
    }

    let baseline = one_run();
    for i in 1..10 {
        let run = one_run();
        assert_eq!(
            baseline.len(),
            run.len(),
            "run {i}: layout node count changed"
        );
        for (j, (b, r)) in baseline.iter().zip(run.iter()).enumerate() {
            // taffy::Layout の全 field を byte-identical で比較。
            // 浮動小数点の subnormal / NaN drift があると here が最も先に
            // 反応する (design doc §12.8 の NonFiniteFloat 検討の pin 相当)
            assert_eq!(
                b.size.width, r.size.width,
                "run {i} node {j}: size.width differs (baseline={} run={})",
                b.size.width, r.size.width
            );
            assert_eq!(b.size.height, r.size.height);
            assert_eq!(b.location.x, r.location.x);
            assert_eq!(b.location.y, r.location.y);
        }
    }
}
```

- [ ] **Step 2: test 実行**

Run: `cargo test -p raikiri-dom --lib layout::tests::layout_single_page_deterministic_across_10_runs -- --nocapture 2>&1 | tail -20`

Expected: PASS。

もし FAIL する場合:
- parley 側の非決定性が疑われる (HashMap iter 順、システム font 検索順が run-to-run で異なる可能性)
- **原因が parley::FontContext::new() の run-to-run 差** の場合、`layout_single_page` 内で `use std::sync::OnceLock; static FONTS: OnceLock<Mutex<FontContext>> = OnceLock::new();` の pattern に昇格して同一 process 内の 10 連ランで同じ context を再利用する。この変更は design の "M2+ で Document 昇格 or Consumer inject に変更予定" 節を M1.6 に前倒し。
- **原因が cascade の HashMap** の場合、raikiri-style::cascade の `HashMap<NodeId, ...>` を確認 (existing test `cascade_deterministic_across_10_runs` が pass しているのでほぼ大丈夫だが link 確認)

- [ ] **Step 3: final gate — workspace 全 test / clippy / build**

Run:
```bash
cargo build --workspace --all-targets 2>&1 | tail -3
cargo test  --workspace 2>&1 | grep -E "^(test result|FAILED)"
cargo clippy --workspace --all-targets -- -D warnings 2>&1 | tail -5
```

Expected:
- build: green
- test: 全 PASS (raikiri-dom 単体で 40+ test、workspace 全体で regression 無し)
- clippy: 0 warnings

- [ ] **Step 4: FontContext::new() の per-call cost を軽く測る (optional bench)**

以下を一時的に inline で実行 (cargo test 内で計測):

```rust
#[test]
#[ignore]  // 明示的に cargo test -- --ignored で実行
fn font_context_new_cost_is_reasonable() {
    let start = std::time::Instant::now();
    for _ in 0..10 {
        let _ = parley::FontContext::new();
    }
    let elapsed = start.elapsed();
    // 10 回 total で 5 秒未満なら M1.6 の per-call new() は許容
    // (10 連ラン determinism test が timeout しないため)
    assert!(elapsed.as_secs() < 5, "FontContext::new() too slow: 10x = {:?}", elapsed);
}
```

Run: `cargo test -p raikiri-dom --lib layout::tests::font_context_new_cost_is_reasonable -- --ignored --nocapture 2>&1 | tail -10`

**判断**:
- 5 秒未満 → M1.6 の per-call new() を維持
- 5 秒以上 → design doc §Open questions の "M2+ で OnceLock 昇格" を M1.6 に前倒し (Step 2 の失敗対応と同じ変更を適用)

このオプショナル bench 自体は最終 commit に含める必要は無い (`#[ignore]` で普段は実行されない)。含めるかは executor の判断。

- [ ] **Step 5: Commit (determinism test は必須、bench は optional)**

```bash
git add crates/raikiri-dom/src/layout.rs
git commit -m "test(raikiri-dom): layout_single_page 10-run byte-identical determinism regression (raikiri-spike-m1.6)

- M1 acceptance の 10 連ラン byte-identical を raikiri-dom 単体でも pin
- 全 Node.unrounded_layout の size / location を byte-identical 比較
- 万一 float drift が起きた場合 here で最先端検知 (design §12.8
  NonFiniteFloat 検討の pin 相当)
- Cross-machine byte-identical は m1.13 で font pinning (WPT Ahem) に置換予定"
```

---

## Self-Review Checklist (writing-plans skill 準拠、planner が pre-flight で確認)

**1. Spec coverage** (design 8 セクションと task の対応):

- ✅ Section 1 (data flow) — Task 3〜7 で step-by-step 実装
- ✅ Section 2 (API surface: PageBox / layout_single_page) — Task 1 + Task 7
- ✅ Section 3 (parley integration + Node.text_layout) — Task 2 + Task 5 + Task 6
- ✅ Section 4 (<body> lookup + PageBox wiring) — Task 3 + Task 4
- ✅ Section 5 (testing 8 tests) — 全 Task 内訳: Task 3 (4) + Task 4 (2) + Task 5 (2) + Task 6 (1) + Task 7 (4) + Task 8 (1) = 合計 14 test (design の 8 test を上回るが overhead でない、各 task が独立検証可能)
- ✅ Section 6 (defer list) — CLAUDE.md / design 内で明示、変更なし
- ✅ Section 7 (open questions) — Task 8 Step 4 で FontContext::new() cost bench、判断分岐 documented
- ✅ Section 8 (rejected alternatives) — plan には含めない (design に残す)

**2. Placeholder scan**: TBD / TODO / "similar to Task N" / 空 assertion なし。全 code block は完全形。

**3. Type consistency**:
- `PageBox` field: `width: f32` / `height: f32` (Task 1) — Task 4 / 7 で同じ使用
- `Node.text_layout: Option<parley::Layout<()>>` (Task 2) — Task 5 で populate、Task 6 で read
- `layout_single_page(&mut Document, &CascadeResult, PageBox) -> Result<(), LayoutError>` (Task 7) — Task 8 test で同じ signature
- `find_body(&Document) -> Option<usize>` (Task 3) — Task 7 driver で consume
- `apply_page_box_to_body(&mut Document, usize, PageBox)` (Task 4) — Task 7 driver で consume
- `apply_computed_to_style(&mut Document, &CascadeResult)` (Task 4) — Task 7 driver で consume
- `preshape_text(&mut Document, &CascadeResult, &mut FontContext, &mut LayoutContext<()>, f32) -> Result<(), LayoutError>` (Task 5) — Task 7 driver で consume

**4. Global constraints coverage**:
- `#[non_exhaustive]` 保持: Task 1 で明示、struct update pattern test 追加
- Cleanroom: raikiri-traits に parley / taffy 型を漏らさず (PageBox は素の struct)
- Iterative DFS: Task 3 で明示 + 5000 nesting regression test
- clippy warnings 0: 全 task の Step で確認
- Frequent commits: 各 task が独立 commit
- Byte-identical: Task 8 で 10 連ラン regression + bench 判断分岐

Plan は self-review 完了。実装に進む準備 OK。
