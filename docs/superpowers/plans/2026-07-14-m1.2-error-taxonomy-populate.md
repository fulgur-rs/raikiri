# m1.2 Error Taxonomy Populate — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** raikiri-traits の `ParseError` / `CascadeError` / `LayoutError` uninhabited enum に variant を populate し、`Display` / `std::error::Error` / `From` impl + `RenderStatus::Aborted` contract test を追加する (m1.3 / m1.4 unblock)。

**Architecture:** raikiri-traits crate 内で closed に完結。Stylo 責務境界に従い、CSS / HTML / layout の実装固有型 (cssparser / html5ever / taffy) は raikiri-traits に持ち込まない。ParseError は真の boundary error (`Io` / `Encoding`) のみ、CascadeError と LayoutError は `Internal { message }` 1-variant の opaque enum。

**Tech Stack:** Rust 2024 edition、rust-version 1.89.0、std のみ (upstream dep 追加なし)。

## Global Constraints

- 対象 crate: `crates/raikiri-traits` のみ (他 crate は触らない)
- Cleanroom 原則: raikiri-traits に upstream crate (cssparser / html5ever / taffy 等) の dep を追加しない
- 既存 test 19 件を regression させない
- `#[non_exhaustive]` を全 error enum に保持 (新規追加も同様)
- Detail message は `String` (`Cow<'static, str>` 化は将来判断)
- test 追加先: 既存パターンに従い `crates/raikiri-traits/src/lib.rs` の `#[cfg(test)] mod tests` に追記
- rustc / clippy warnings 0 を維持

---

## File Structure

**Modify:**
- `crates/raikiri-traits/src/error.rs` — ParseError / CascadeError / LayoutError の variant populate、Display / Error / From impl 追加、RenderError の source() delegate 更新
- `crates/raikiri-traits/src/lib.rs` — `#[cfg(test)] mod tests` block に新 test 6 件追記 (トレイト実装 assertion、source chain、? propagation、Aborted contract)

**Create:** なし

**Test:** 新 test は上記 `lib.rs` 内の tests module 内 (既存パターン踏襲、file 分割しない)。

---

## Task 1: ParseError populate (Io + Encoding variants)

**Files:**
- Modify: `crates/raikiri-traits/src/error.rs:281-292`
- Test: `crates/raikiri-traits/src/lib.rs` (tests module 末尾)

**Interfaces:**
- Consumes: 既存 `pub enum ParseError { }` (uninhabited)
- Produces:
  - `ParseError::Io(std::io::Error)`
  - `ParseError::Encoding { label: String, reason: String }`
  - `impl Display for ParseError`
  - `impl std::error::Error for ParseError` (source() は Io variant 経由で delegate)
  - `impl From<std::io::Error> for ParseError`

---

- [ ] **Step 1: Write failing test — ParseError implements std::error::Error and Display**

`crates/raikiri-traits/src/lib.rs` の tests module 末尾に追加:

```rust
    // ── Sub-error trait bounds (M1.2) ───────────────────────────

    #[test]
    fn parse_error_is_error_and_display() {
        fn _assert_error<T: std::error::Error>() {}
        fn _assert_display<T: std::fmt::Display>() {}
        _assert_error::<ParseError>();
        _assert_display::<ParseError>();
    }
```

- [ ] **Step 2: Run test to verify it fails**

```bash
cargo test -p raikiri-traits parse_error_is_error_and_display 2>&1 | tail -20
```

Expected: FAIL with "the trait bound `error::ParseError: std::error::Error` is not satisfied" (or `Display` not satisfied)。

- [ ] **Step 3: Populate ParseError variants + implement Display + Error**

`crates/raikiri-traits/src/error.rs:281-292` を以下で置換:

```rust
/// HTML parse 段階の terminal error (raikiri-html crate 内で発生)。
///
/// **Stylo/blitz と同じ責務境界**: html5ever tokenizer 由来の non-fatal
/// parse error (recoverable な malformed HTML) は raikiri-html 内で
/// [`RenderWarning`] として summary に集約し、この enum には含めない。
/// この enum の variant は rendering を halt させる真の terminal error のみ。
///
/// M1.2 で `Io` / `Encoding` の 2 variant を populate。html5ever 固有の
/// error variant は raikiri-html 側で crate-private に扱い、必要になった
/// 時点で `#[non_exhaustive]` の恩恵で追加する。
#[non_exhaustive]
#[derive(Debug)]
pub enum ParseError {
    /// Input source (`std::io::Read`) からの read 失敗。
    Io(std::io::Error),
    /// Byte stream → text の変換に失敗 (encoding label 検出 or 変換 error)。
    Encoding {
        /// 検出または指定された encoding label (例: "utf-8", "shift_jis")。
        label: String,
        /// 失敗理由の人間可読な description。
        reason: String,
    },
}

impl std::fmt::Display for ParseError {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        match self {
            Self::Io(_) => write!(f, "HTML source read error"),
            Self::Encoding { label, reason } => {
                write!(f, "HTML source encoding error ({label}): {reason}")
            }
        }
    }
}

impl std::error::Error for ParseError {
    fn source(&self) -> Option<&(dyn std::error::Error + 'static)> {
        match self {
            Self::Io(e) => Some(e),
            Self::Encoding { .. } => None,
        }
    }
}

impl From<std::io::Error> for ParseError {
    fn from(e: std::io::Error) -> Self {
        Self::Io(e)
    }
}
```

- [ ] **Step 4: Run test to verify it passes**

```bash
cargo test -p raikiri-traits parse_error_is_error_and_display 2>&1 | tail -10
```

Expected: PASS。

- [ ] **Step 5: Add source() chain test**

`crates/raikiri-traits/src/lib.rs` の tests module 末尾に追加:

```rust
    #[test]
    fn parse_error_io_source_chain() {
        let io_err = std::io::Error::new(std::io::ErrorKind::UnexpectedEof, "eof");
        let pe: ParseError = io_err.into();
        use std::error::Error as _;
        let src = pe.source();
        assert!(src.is_some(), "ParseError::Io should expose inner io::Error via source()");
    }
```

- [ ] **Step 6: Run test to verify it passes**

```bash
cargo test -p raikiri-traits parse_error_io_source_chain 2>&1 | tail -10
```

Expected: PASS。

- [ ] **Step 7: Verify no clippy warnings**

```bash
cargo clippy -p raikiri-traits --all-targets -- -D warnings 2>&1 | tail -10
```

Expected: 出力なし (warnings 0)。

- [ ] **Step 8: Commit**

```bash
git add crates/raikiri-traits/src/error.rs crates/raikiri-traits/src/lib.rs
git commit -m "feat(raikiri-traits): populate ParseError with Io + Encoding variants (m1.2)"
```

---

## Task 2: CascadeError populate (Internal variant only)

**Files:**
- Modify: `crates/raikiri-traits/src/error.rs:294-304`
- Test: `crates/raikiri-traits/src/lib.rs` (tests module 末尾)

**Interfaces:**
- Consumes: 既存 `pub enum CascadeError { }` (uninhabited)
- Produces:
  - `CascadeError::Internal { message: String }`
  - `impl Display for CascadeError`
  - `impl std::error::Error for CascadeError` (source() は常に None)

---

- [ ] **Step 1: Write failing test**

`crates/raikiri-traits/src/lib.rs` の tests module 末尾に追加:

```rust
    #[test]
    fn cascade_error_is_error_and_display() {
        fn _assert_error<T: std::error::Error>() {}
        fn _assert_display<T: std::fmt::Display>() {}
        _assert_error::<CascadeError>();
        _assert_display::<CascadeError>();
    }
```

- [ ] **Step 2: Run test to verify it fails**

```bash
cargo test -p raikiri-traits cascade_error_is_error_and_display 2>&1 | tail -20
```

Expected: FAIL with trait bound error。

- [ ] **Step 3: Populate CascadeError + implement Display + Error**

`crates/raikiri-traits/src/error.rs:294-304` を以下で置換:

```rust
/// CSS parse / cascade 段階の terminal error (raikiri-style crate 内で発生)。
///
/// **Stylo/blitz と同じ責務境界**: CSS spec 準拠で invalid rule / value は
/// silently drop され error にならない。cssparser / selectors 固有の error
/// 型は raikiri-style 内部に閉じ込め、この enum は raikiri-style が明示的
/// に fail-hard を選択した場合の signal のみ露出する。
///
/// M1.2 で `Internal` variant のみ populate。CSS 実装詳細 (property /
/// value / source location 等) を trait layer に漏らさない。必要になった
/// 時点で `#[non_exhaustive]` の恩恵で追加する。
#[non_exhaustive]
#[derive(Debug)]
pub enum CascadeError {
    /// raikiri-style 内で回復不能な内部 error が発生した (bug 相当、または
    /// 明示的な strict mode で許容外の入力を受けた)。詳細メッセージは
    /// raikiri-style 内部で log + message として構成される。
    Internal {
        /// 人間可読な失敗詳細 (raikiri-style 内部で構成)。
        message: String,
    },
}

impl std::fmt::Display for CascadeError {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        match self {
            Self::Internal { message } => write!(f, "CSS cascade internal error: {message}"),
        }
    }
}

impl std::error::Error for CascadeError {}
```

- [ ] **Step 4: Run test to verify it passes**

```bash
cargo test -p raikiri-traits cascade_error_is_error_and_display 2>&1 | tail -10
```

Expected: PASS。

- [ ] **Step 5: Verify no clippy warnings**

```bash
cargo clippy -p raikiri-traits --all-targets -- -D warnings 2>&1 | tail -10
```

Expected: 出力なし。

- [ ] **Step 6: Commit**

```bash
git add crates/raikiri-traits/src/error.rs crates/raikiri-traits/src/lib.rs
git commit -m "feat(raikiri-traits): populate CascadeError with opaque Internal variant (m1.2)"
```

---

## Task 3: LayoutError populate (Internal variant only)

**Files:**
- Modify: `crates/raikiri-traits/src/error.rs:306-315`
- Test: `crates/raikiri-traits/src/lib.rs` (tests module 末尾)

**Interfaces:**
- Consumes: 既存 `pub enum LayoutError { }` (uninhabited)
- Produces:
  - `LayoutError::Internal { message: String }`
  - `impl Display for LayoutError`
  - `impl std::error::Error for LayoutError` (source() は常に None)

---

- [ ] **Step 1: Write failing test**

`crates/raikiri-traits/src/lib.rs` の tests module 末尾に追加:

```rust
    #[test]
    fn layout_error_is_error_and_display() {
        fn _assert_error<T: std::error::Error>() {}
        fn _assert_display<T: std::fmt::Display>() {}
        _assert_error::<LayoutError>();
        _assert_display::<LayoutError>();
    }
```

- [ ] **Step 2: Run test to verify it fails**

```bash
cargo test -p raikiri-traits layout_error_is_error_and_display 2>&1 | tail -20
```

Expected: FAIL with trait bound error。

- [ ] **Step 3: Populate LayoutError + implement Display + Error**

`crates/raikiri-traits/src/error.rs:306-315` を以下で置換:

```rust
/// Layout 段階の terminal error (raikiri-dom + taffy が発生源)。
///
/// **同じ責務境界**: taffy 固有 error 型は raikiri-dom 内部に閉じ込め、
/// この enum は raikiri-dom が明示的に fail-hard を選択した場合の signal のみ。
///
/// M1.2 で `Internal` variant のみ populate。taffy 実装詳細を trait layer
/// に漏らさない。必要になった時点で `#[non_exhaustive]` の恩恵で追加する。
#[non_exhaustive]
#[derive(Debug)]
pub enum LayoutError {
    /// raikiri-dom 内で回復不能な内部 error が発生した (taffy internal
    /// error 等)。詳細メッセージは raikiri-dom 内部で log + message として構成。
    Internal {
        /// 人間可読な失敗詳細 (raikiri-dom 内部で構成)。
        message: String,
    },
}

impl std::fmt::Display for LayoutError {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        match self {
            Self::Internal { message } => write!(f, "Layout internal error: {message}"),
        }
    }
}

impl std::error::Error for LayoutError {}
```

- [ ] **Step 4: Run test to verify it passes**

```bash
cargo test -p raikiri-traits layout_error_is_error_and_display 2>&1 | tail -10
```

Expected: PASS。

- [ ] **Step 5: Verify no clippy warnings**

```bash
cargo clippy -p raikiri-traits --all-targets -- -D warnings 2>&1 | tail -10
```

Expected: 出力なし。

- [ ] **Step 6: Commit**

```bash
git add crates/raikiri-traits/src/error.rs crates/raikiri-traits/src/lib.rs
git commit -m "feat(raikiri-traits): populate LayoutError with opaque Internal variant (m1.2)"
```

---

## Task 4: RenderError source() delegates to sub-errors

**Files:**
- Modify: `crates/raikiri-traits/src/error.rs:87-94` (RenderError の std::error::Error impl)

**Interfaces:**
- Consumes: `ParseError` / `CascadeError` / `LayoutError` の `std::error::Error` impl (Task 1-3 で追加済)
- Produces:
  - `RenderError::source()` が Parse / Cascade / Layout variant で inner sub-error を返すよう更新
  - 既存の Sink / Io variant delegate は維持

---

- [ ] **Step 1: Write failing test — RenderError::Parse source() returns inner ParseError**

`crates/raikiri-traits/src/lib.rs` の tests module 末尾に追加:

```rust
    #[test]
    fn render_error_parse_source_chain() {
        use std::error::Error as _;
        let io_err = std::io::Error::new(std::io::ErrorKind::UnexpectedEof, "eof");
        let re: RenderError = RenderError::Parse(ParseError::Io(io_err));
        let src = re.source();
        assert!(src.is_some(), "RenderError::Parse should expose inner ParseError via source()");
    }
```

- [ ] **Step 2: Run test to verify it fails**

```bash
cargo test -p raikiri-traits render_error_parse_source_chain 2>&1 | tail -10
```

Expected: FAIL — 現状 RenderError の source() は Parse variant で None を返す。

- [ ] **Step 3: Update RenderError::source() to delegate to sub-errors**

`crates/raikiri-traits/src/error.rs:87-94` を以下で置換:

```rust
impl std::error::Error for RenderError {
    fn source(&self) -> Option<&(dyn std::error::Error + 'static)> {
        match self {
            Self::Parse(e) => Some(e),
            Self::Cascade(e) => Some(e),
            Self::Layout(e) => Some(e),
            Self::Sink(e) | Self::Io(e) => Some(e),
            Self::Resolver(_)
            | Self::Network(_)
            | Self::Policy(_)
            | Self::LimitExceeded { .. }
            | Self::Configuration(_)
            | Self::TargetDidNotConverge { .. } => None,
        }
    }
}
```

- [ ] **Step 4: Run test to verify it passes**

```bash
cargo test -p raikiri-traits render_error_parse_source_chain 2>&1 | tail -10
```

Expected: PASS。

- [ ] **Step 5: Verify existing tests still pass (regression check)**

```bash
cargo test -p raikiri-traits 2>&1 | tail -10
```

Expected: 全 test PASS (regression なし)。

- [ ] **Step 6: Verify no clippy warnings**

```bash
cargo clippy -p raikiri-traits --all-targets -- -D warnings 2>&1 | tail -10
```

Expected: 出力なし。

- [ ] **Step 7: Commit**

```bash
git add crates/raikiri-traits/src/error.rs crates/raikiri-traits/src/lib.rs
git commit -m "feat(raikiri-traits): delegate RenderError::source() to Parse/Cascade/Layout sub-errors (m1.2)"
```

---

## Task 5: From<Sub-error> for RenderError impls + `?` propagation smoke test

**Files:**
- Modify: `crates/raikiri-traits/src/error.rs` (impl block 追加、既存 impl 群の後)
- Test: `crates/raikiri-traits/src/lib.rs` (tests module 末尾)

**Interfaces:**
- Consumes: `ParseError` / `CascadeError` / `LayoutError` (Task 1-3)
- Produces:
  - `impl From<ParseError> for RenderError`
  - `impl From<CascadeError> for RenderError`
  - `impl From<LayoutError> for RenderError`

---

- [ ] **Step 1: Write failing test — ? propagates io::Error through ParseError into RenderError**

`crates/raikiri-traits/src/lib.rs` の tests module 末尾に追加:

```rust
    #[test]
    fn question_mark_propagates_io_through_parse_to_render() {
        fn producer() -> Result<(), RenderError> {
            let io_err = std::io::Error::new(std::io::ErrorKind::UnexpectedEof, "eof");
            let pe: ParseError = io_err.into();
            Err(pe)?;
            Ok(())
        }
        let e = producer().expect_err("expected RenderError");
        match e {
            RenderError::Parse(ParseError::Io(_)) => (),
            other => panic!("expected RenderError::Parse(ParseError::Io(_)), got {other:?}"),
        }
    }
```

- [ ] **Step 2: Run test to verify it fails**

```bash
cargo test -p raikiri-traits question_mark_propagates_io_through_parse_to_render 2>&1 | tail -20
```

Expected: FAIL — compile error "the trait `From<ParseError>` is not implemented for `RenderError`"。

- [ ] **Step 3: Add From impls (place after existing impl blocks in error.rs)**

`crates/raikiri-traits/src/error.rs` の `impl std::error::Error for RenderError` block 直後に追加:

```rust
impl From<ParseError> for RenderError {
    fn from(e: ParseError) -> Self {
        Self::Parse(e)
    }
}

impl From<CascadeError> for RenderError {
    fn from(e: CascadeError) -> Self {
        Self::Cascade(e)
    }
}

impl From<LayoutError> for RenderError {
    fn from(e: LayoutError) -> Self {
        Self::Layout(e)
    }
}
```

- [ ] **Step 4: Run test to verify it passes**

```bash
cargo test -p raikiri-traits question_mark_propagates_io_through_parse_to_render 2>&1 | tail -10
```

Expected: PASS。

- [ ] **Step 5: Verify no clippy warnings**

```bash
cargo clippy -p raikiri-traits --all-targets -- -D warnings 2>&1 | tail -10
```

Expected: 出力なし。

- [ ] **Step 6: Commit**

```bash
git add crates/raikiri-traits/src/error.rs crates/raikiri-traits/src/lib.rs
git commit -m "feat(raikiri-traits): add From<Sub-error> for RenderError + ? propagation test (m1.2)"
```

---

## Task 6: RenderStatus::Aborted partial_pages contract test

**Files:**
- Test: `crates/raikiri-traits/src/lib.rs` (tests module 末尾)

**Interfaces:**
- Consumes: 既存 `RenderStatus::Aborted { partial_pages: u32 }` (m1.1 populate 済)
- Produces: `render_status_aborted_carries_partial_pages` test — partial_pages が round-trip する contract を documentation として固定する

---

- [ ] **Step 1: Write test (即 pass する contract test)**

`crates/raikiri-traits/src/lib.rs` の tests module 末尾に追加:

```rust
    // ── RenderStatus::Aborted contract (M1.2、実 semantic は M6c) ─

    /// Type-level contract test。`RenderStatus::Aborted` の `partial_pages`
    /// field が Consumer から観測可能で、round-trip することを固定する。
    /// 実 render pipeline 経由での partial_pages 追跡は M6c
    /// (`abort-signal-integration` / `renderstatus-aborted-impl`) で verify。
    #[test]
    fn render_status_aborted_carries_partial_pages() {
        let s = RenderStatus::Aborted { partial_pages: 7 };
        match s {
            RenderStatus::Aborted { partial_pages } => assert_eq!(partial_pages, 7),
            RenderStatus::Completed(_) => panic!("expected Aborted"),
        }
    }
```

- [ ] **Step 2: Run test to verify it passes**

```bash
cargo test -p raikiri-traits render_status_aborted_carries_partial_pages 2>&1 | tail -10
```

Expected: PASS。

- [ ] **Step 3: Verify full test suite passes**

```bash
cargo test -p raikiri-traits 2>&1 | tail -10
```

Expected: 全 test PASS (元 19 件 + 新 7 件 = 26 件)。

- [ ] **Step 4: Verify no clippy warnings**

```bash
cargo clippy -p raikiri-traits --all-targets -- -D warnings 2>&1 | tail -10
```

Expected: 出力なし。

- [ ] **Step 5: Verify rustdoc builds cleanly**

```bash
cargo doc -p raikiri-traits --no-deps 2>&1 | tail -10
```

Expected: warnings なし。

- [ ] **Step 6: Commit**

```bash
git add crates/raikiri-traits/src/lib.rs
git commit -m "test(raikiri-traits): add RenderStatus::Aborted partial_pages contract test (m1.2)"
```

---

## Post-implementation verification checklist

- [ ] `cargo test -p raikiri-traits` all pass (26 tests expected: 元 19 + 新 7)
- [ ] `cargo clippy -p raikiri-traits --all-targets -- -D warnings` clean
- [ ] `cargo doc -p raikiri-traits --no-deps` warnings なし
- [ ] `cargo test` (workspace 全体) が regression なし — 他 crate が re-export 経由で影響を受けないことを確認
- [ ] `git log --oneline` で 6 コミットが順序通り並んでいる

## Follow-up notes (out of scope for this task)

- m1.3 着手時: `WarningKind` に `HtmlParseError { message }` variant 追加、html5ever `TreeSink::parse_error` callback を routing
- m1.4 着手時: cssparser 由来 error を CascadeError::Internal 経由で降格する境界 code を raikiri-style 内に配置
- 将来的に detail message 型を `Cow<'static, str>` にすると Static message で allocation 削減可能 (M2 以降で判断)
