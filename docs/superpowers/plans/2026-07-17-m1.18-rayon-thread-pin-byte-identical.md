# m1.18 — anyrender_vello_cpu rayon 1v4 byte-identical test — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** raikiri-vrt に、`vello_cpu::RenderSettings::num_threads` を 1 と 4 で pin した rayon dispatcher の出力が byte-identical であることを確認する targeted test を追加。M0 spike (nzv.7) が default multi-thread config での byte-identity を確認済みの follow-up (spec §5.2)。VRT/T1 の pixel-exact baseline は「rayon の work-stealing 非決定性が pixel に leak しない」ことを前提にしており、この test がその前提を M1 で pin する。

**Architecture:** `anyrender_vello_cpu::VelloCpuImageRenderer` は `RenderSettings::num_threads` を surface しないため、`vello_cpu 0.0.9` を raikiri-vrt の dev-dep に直接追加し、`RenderContext::new_with(w, h, RenderSettings { num_threads, ..default() })` で thread count を pin する。scene は既存の `draw_red_rect` (100×100 canvas、10..90 赤矩形) と同一。`anyrender_vello_cpu::VelloCpuScenePainter` (pub 型、pub fields) を直接構築して production の `VelloCpuImageRenderer::render` と同じ code path (`draw_fn → flush → render_to_buffer(OptimizeSpeed)`) を通す。

**Tech Stack:** Rust 2024 edition、rust-version 1.89.0、`vello_cpu = "0.0.9"` (Cargo.lock で解決済み)、`anyrender = 0.11`、`anyrender_vello_cpu = "0.14"` (features = ["multithreading"])、`kurbo`、`peniko`。

## Global Constraints

- 対象 crate: `crates/raikiri-vrt` (test 追加)、workspace root `Cargo.toml` (workspace dep 追加)
- 新規 workspace dep は既存 `anyrender_vello_cpu` の transitive 依存 (Cargo.lock 解決済み v0.0.9) を直接指定するだけで、新たな dep tree 拡張は無い
- `vello_cpu` は `[dev-dependencies]` に限定 (prod code は依然 anyrender surface のみ経由)
- rustc / clippy warnings 0 を維持 (`cargo clippy --workspace --all-targets -- -D warnings` を通す)
- `cargo test --workspace --all-targets` が pass する状態を維持 (既存 test の regression 禁止)
- test は `crates/raikiri-vrt/src/lib.rs` の既存 `#[cfg(test)] mod tests` module に追加 (現行 test と code path・scene を共有できる位置)
- scene は既存 `draw_red_rect` と 100% 同一 (Color=css::RED、Rect (10.0, 10.0, 90.0, 90.0)、Fill::NonZero、Affine::IDENTITY) — 既存 test 群との cross-reference 可能性を保つ
- thread count は 1 と 4 のみ (spec §5.2 の要求どおり、両方とも `MultiThreadedDispatcher` path)。`num_threads=0` (SingleThreadedDispatcher、非 rayon path) の比較は scope 外
- test failure 時は既存 test と同じ diagnostic 形式 (`"N of M bytes differ"`) で出力
- commit は conventional form: `test(raikiri-vrt): pin rayon 1v4 byte-identical (m1.18)`
- 既存 CI / hooks / lints をすべて satisfy させた上で commit
- beads issue `raikiri-spike-m1.18` は task 完了時に `bd close` する

---

## File Structure

**Modify:**
- `Cargo.toml` (workspace root) — `[workspace.dependencies]` の Dev-only section に `vello_cpu = "0.0.9"` を追加
- `crates/raikiri-vrt/Cargo.toml` — `[dev-dependencies]` に `vello_cpu = { workspace = true }` を追加
- `crates/raikiri-vrt/src/lib.rs` — 既存 `#[cfg(test)] mod tests` の末尾に helper + 2 new test を追加

**Not modified:**
- `crates/raikiri-feasibility/src/anyrender_byte_identical.rs` — M0 spike の evidence として保持 (raikiri-spike-m1.16 で crate 全体が disposal 対象、m1.18 の scope 外)

---

## Task 1: workspace / crate dependency wiring

**Files:**
- Modify: `Cargo.toml` (workspace root、`[workspace.dependencies]` の Dev-only section 末尾)
- Modify: `crates/raikiri-vrt/Cargo.toml` (`[dev-dependencies]` block)

**Interfaces:**
- Consumes: 既存 workspace の `anyrender_vello_cpu = { version = "0.14", features = ["multithreading"] }` が transitive に `vello_cpu 0.0.9` を pull 済み (Cargo.lock 参照)
- Produces: raikiri-vrt の test target から `vello_cpu::{RenderContext, RenderSettings, RenderMode, Resources}` が use できる状態

### Steps

- [ ] **Step 1: workspace root Cargo.toml に vello_cpu dep を追加**

`Cargo.toml` の `[workspace.dependencies]` の既存 `# ── Dev-only: VRT rasterizer / snapshot / oracle ──────────────────────` section (現行 line 70-77) の `anyrender_vello_cpu = { version = "0.14", features = ["multithreading"] }` (line 71) の**直下**に、以下 1 行を追加:

```toml
vello_cpu           = "0.0.9"                    # m1.18 thread-pin test; RenderContext::new_with(RenderSettings::num_threads)
```

書式:
- `=` の位置を既存の右揃え (`anyrender_vello_cpu` に合わせて `vello_cpu           =`) に合わせる (space 数 = 11)
- inline comment はコメント理由の短縮版のみ (長い explanation は plan doc 内に留める、Cargo.toml は簡潔に保つ)

編集後の該当 block (期待形):
```toml
# ── Dev-only: VRT rasterizer / snapshot / oracle ───────────────────────
anyrender_vello_cpu = { version = "0.14", features = ["multithreading"] }
vello_cpu           = "0.0.9"                    # m1.18 thread-pin test; RenderContext::new_with(RenderSettings::num_threads)
tiny-skia           = "0.11"
insta               = "1"
blitz-traits        = "=0.3.0-beta.1"
blitz-html          = "=0.3.0-beta.1"
blitz-dom           = "=0.3.0-beta.1"
blitz-paint         = "=0.3.0-beta.1"
```

- [ ] **Step 2: raikiri-vrt Cargo.toml の dev-dependencies に vello_cpu を追加**

`crates/raikiri-vrt/Cargo.toml` の `[dev-dependencies]` block 末尾に以下を追加:

```toml
vello_cpu           = { workspace = true }  # m1.18 rayon thread-pin test only; production path stays anyrender-only.
```

既存 dev-deps の書式 (右揃え `=` の pattern) に合わせる。

- [ ] **Step 3: dep resolution を確認**

Run:
```bash
cd /home/mitz/worktrees/github.com/mitsuru/raikiri-spike/worktree-raikiri-spike-m1.18
cargo build -p raikiri-vrt --tests
```

Expected: `Compiling raikiri-vrt v0.1.0 ...` → `Finished` で warning / error 無し。`vello_cpu` の再 build は起きない (transitive として既に compile 済み)。

- [ ] **Step 4: commit**

```bash
cd /home/mitz/worktrees/github.com/mitsuru/raikiri-spike/worktree-raikiri-spike-m1.18
git add Cargo.toml crates/raikiri-vrt/Cargo.toml
git commit -m "$(cat <<'EOF'
build(raikiri-vrt): add vello_cpu dev-dep for m1.18 thread-pin test

VelloCpuImageRenderer does not surface RenderSettings::num_threads, so the
m1.18 rayon 1v4 byte-identical test needs vello_cpu as a direct dev-dep to
call RenderContext::new_with. spec §5.2 option (a). Prod code path stays
anyrender-only; vello_cpu remains dev-only.

raikiri-spike-m1.18
EOF
)"
```

---

## Task 2: rayon thread pin byte-identical test

**Files:**
- Modify: `crates/raikiri-vrt/src/lib.rs` (末尾、既存 `#[cfg(test)] mod tests` block 内)

**Interfaces:**
- Consumes: Task 1 で追加した `vello_cpu` dev-dep 経由の `RenderContext / RenderSettings / RenderMode / Resources`、既存 `use` 済みの `anyrender::PaintScene / anyrender_vello_cpu::VelloCpuImageRenderer / kurbo / peniko` 型
- Produces: 2 test functions:
  - `rayon_thread_pin_1_vs_4_is_byte_identical` — main byte-identity assertion
  - `rayon_thread_pin_produces_expected_buffer_shape` — sanity check (non-empty buffer, 中央 pixel non-transparent) — silent no-op 検出用

### Steps

- [ ] **Step 1: 失敗する test の追加 (TDD の "red")**

`crates/raikiri-vrt/src/lib.rs` の既存 `#[cfg(test)] mod tests { ... }` block の末尾 (`render_to_buffer_leaves_untouched_pixels_transparent` test の直後) に以下を追加。既存 `use` (`anyrender::PaintScene`、`anyrender_vello_cpu::VelloCpuImageRenderer`、`kurbo::{Affine, Rect}`、`peniko::{Color, Fill, color::palette::css}`) は既に import 済み。Task 1 で追加した `vello_cpu` から必要な型を追加 import:

```rust
    /// M1 spec §5.2 — pinning rayon worker count and verifying byte-identity.
    ///
    /// `anyrender_vello_cpu::VelloCpuImageRenderer` internally constructs
    /// `vello_cpu::RenderContext::new(w, h)` which uses `RenderSettings::default()`
    /// (num_threads = `min(available_parallelism - 1, 8)`). To pin the worker count
    /// we bypass `VelloCpuImageRenderer::new` and construct `RenderContext::new_with`
    /// directly, then wrap it in `VelloCpuScenePainter` (whose fields are `pub`) to
    /// stay on the exact same production render path (`draw_fn → flush →
    /// render_to_buffer(OptimizeSpeed)`) that `VelloCpuImageRenderer::render` uses.
    ///
    /// Both `num_threads: 1` and `num_threads: 4` route through
    /// `MultiThreadedDispatcher` (only `num_threads == 0` selects
    /// `SingleThreadedDispatcher`), so this test verifies rayon work-stealing
    /// determinism, not scalar-vs-rayon parity.
    fn render_with_threads(num_threads: u16) -> Vec<u8> {
        use anyrender_vello_cpu::VelloCpuScenePainter;
        use vello_cpu::{RenderContext, RenderMode, RenderSettings, Resources};

        let settings = RenderSettings {
            num_threads,
            ..Default::default()
        };
        let render_ctx = RenderContext::new_with(W as u16, H as u16, settings);
        let mut scene = VelloCpuScenePainter {
            render_ctx,
            resources: Resources::new(),
        };
        draw_red_rect(&mut scene);
        scene.render_ctx.flush();
        let mut buf = vec![0u8; (W as usize) * (H as usize) * 4];
        let (w, h) = (scene.render_ctx.width(), scene.render_ctx.height());
        scene.render_ctx.render_to_buffer(
            &mut scene.resources,
            &mut buf,
            w,
            h,
            RenderMode::OptimizeSpeed,
        );
        buf
    }

    /// Sanity: pinned-thread render still produces a non-empty buffer with the
    /// red rectangle drawn. Guards against silent no-op regressions in the
    /// pinned path (e.g. flush order, dispatcher initialization).
    #[test]
    fn rayon_thread_pin_produces_expected_buffer_shape() {
        let buf = render_with_threads(4);
        assert_eq!(buf.len(), (W as usize) * (H as usize) * 4);
        let idx = (50 * (W as usize) + 50) * 4;
        assert_ne!(
            &buf[idx..idx + 4],
            &[0, 0, 0, 0],
            "pinned num_threads=4 render left (50,50) transparent — scene did not render",
        );
    }

    /// spec §5.2 — pinning rayon at 1 and 4 workers must give byte-identical
    /// output. Both paths use `MultiThreadedDispatcher`; a divergence would mean
    /// rayon scheduling non-determinism leaks into pixel output, invalidating
    /// the VRT/T1 pixel-exact baseline (design doc §12.8).
    #[test]
    fn rayon_thread_pin_1_vs_4_is_byte_identical() {
        let a = render_with_threads(1);
        let b = render_with_threads(4);
        assert_eq!(a.len(), b.len());
        assert!(
            a == b,
            "rayon 1 vs 4 threads diverged: {} of {} bytes differ",
            a.iter().zip(&b).filter(|(x, y)| x != y).count(),
            a.len(),
        );
    }
```

**注意点:**
- `use anyrender_vello_cpu::VelloCpuScenePainter` と `use vello_cpu::{RenderContext, RenderMode, RenderSettings, Resources}` は helper function 内に局所化 (mod tests の他の test が既に `VelloCpuImageRenderer` を使っており、mod-level import に混ぜると intent が不明瞭になるため)
- `draw_red_rect(&mut scene)` は既存の `fn draw_red_rect<S: PaintScene>(scene: &mut S)` を Rust の型推論経由で呼ぶ (`S = VelloCpuScenePainter` に単一化)。既存 test の HRTB closure (`|scene| draw_red_rect(scene)`) は `anyrender::render_to_buffer` の generic 制約回避のためで、直接呼び出しでは不要
- `#[allow(clippy::redundant_closure)]` は今回不要 (closure を使っていない)
- `let (w, h) = (scene.render_ctx.width(), scene.render_ctx.height());` を先に取得するのは、`scene.render_ctx.render_to_buffer(&mut scene.resources, ..., scene.render_ctx.width(), ..)` の borrow が衝突するため

- [ ] **Step 2: test を run し、結果を確認**

Run:
```bash
cd /home/mitz/worktrees/github.com/mitsuru/raikiri-spike/worktree-raikiri-spike-m1.18
cargo test -p raikiri-vrt --lib rayon_thread_pin -- --nocapture
```

Expected (成功シナリオ):
```
running 2 tests
test tests::rayon_thread_pin_produces_expected_buffer_shape ... ok
test tests::rayon_thread_pin_1_vs_4_is_byte_identical ... ok

test result: ok. 2 passed; 0 failed; ...
```

Failure シナリオ 2 通り:
- Compile error → import / 型不一致 (worktree_dir の cargo build を先に走らせて解決)
- Test failure "rayon 1 vs 4 threads diverged: N of M bytes differ" → **重要な発見**: rayon scheduling が pixel に leak している。この場合 task を止め、user に報告 (VRT/T1 前提の見直しが必要、m1.18 は再設計 / 別 issue 起票が要る)

- [ ] **Step 3: full workspace test run で regression が無いことを確認**

Run:
```bash
cd /home/mitz/worktrees/github.com/mitsuru/raikiri-spike/worktree-raikiri-spike-m1.18
cargo test --workspace --all-targets
```

Expected: すべての既存 test + 新規 2 test が pass。既存 test は 200+ 本ある想定 (直近の m1.22 commit で 213 test)。

- [ ] **Step 4: clippy warning check (`-D warnings` gate)**

Run:
```bash
cd /home/mitz/worktrees/github.com/mitsuru/raikiri-spike/worktree-raikiri-spike-m1.18
cargo clippy --workspace --all-targets -- -D warnings
```

Expected: no warnings, no errors。もし `clippy::redundant_closure` や dead_code 等が出た場合は、既存 test に合わせて `#[allow(...)]` を追加 (ただし step 1 の設計上は不要な想定)。

- [ ] **Step 5: commit**

```bash
cd /home/mitz/worktrees/github.com/mitsuru/raikiri-spike/worktree-raikiri-spike-m1.18
git add crates/raikiri-vrt/src/lib.rs
git commit -m "$(cat <<'EOF'
test(raikiri-vrt): pin rayon 1v4 byte-identical (m1.18)

spec §5.2 follow-up to nzv.7 (default multi-thread byte-identity). Constructs
vello_cpu::RenderContext::new_with(RenderSettings { num_threads: 1|4, .. })
and wraps in VelloCpuScenePainter to stay on the production render path
(draw_fn → flush → render_to_buffer(OptimizeSpeed)) that VelloCpuImageRenderer
uses. Both thread counts route through MultiThreadedDispatcher; this test
pins that rayon work-stealing is deterministic w.r.t. pixel output — the
premise VRT/T1's pixel-exact baseline depends on (design doc §12.8).

Also adds a shape sanity test to guard against silent no-op regressions in
the pinned path.

raikiri-spike-m1.18
EOF
)"
```

---

## Task 3: bd close + hand-off

**Files:** (none — beads state + git handoff only)

**Interfaces:**
- Consumes: Task 1 + 2 commits
- Produces: closed `raikiri-spike-m1.18` beads issue、user への hand-off report

### Steps

- [ ] **Step 1: git log で 2 commits の shape を確認**

Run:
```bash
cd /home/mitz/worktrees/github.com/mitsuru/raikiri-spike/worktree-raikiri-spike-m1.18
git log --oneline main..HEAD
```

Expected: 2 commits (`test(raikiri-vrt): ... (m1.18)`, `build(raikiri-vrt): ... m1.18`) が main tip の直上に並ぶ。

- [ ] **Step 2: beads issue を close**

Run:
```bash
bd close raikiri-spike-m1.18 --reason "m1.18 test landed: render_with_threads pin 1|4 → byte-identical via VelloCpuScenePainter on prod render path; regression-safe."
```

Expected: `✓ Closed issue: raikiri-spike-m1.18` (or equivalent)

- [ ] **Step 3: user への hand-off**

以下を user に報告:
- 変更ファイル: `Cargo.toml`, `crates/raikiri-vrt/Cargo.toml`, `crates/raikiri-vrt/src/lib.rs`
- 追加 test: `rayon_thread_pin_1_vs_4_is_byte_identical` + `rayon_thread_pin_produces_expected_buffer_shape`
- 検証: `cargo test --workspace --all-targets` + `cargo clippy --workspace --all-targets -- -D warnings` 両 pass
- worktree branch: `worktree-raikiri-spike-m1.18` (2 commits ahead of main)
- beads: `raikiri-spike-m1.18` closed
- 次 command 候補 (CLAUDE.md conservative profile — 自動 push しない):
  ```
  git switch main
  git merge --ff-only worktree-raikiri-spike-m1.18
  git push
  gwq remove -b worktree-raikiri-spike-m1.18
  ```
