# m1.7 Paint Basic — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** raikiri-paint に `pub fn paint_single_page(scene: &mut impl PaintScene, document: &Document, cascade: &CascadeResult, page_box: PageBox)` を実装、m1.6 post-layout Document (arena に taffy Layout + parley Layout を populate 済) を DFS walk して text glyphs を anyrender::PaintScene に emit する。nzv.11 refute (docs/feasibility-report.md §3.4) に従い bridge trait を挟まない。canvas fill site は M1.4 では no-op (M4 で background-color cascade が入る時に発火)。infallible、単一 A4 ページ前提、hello-world (`<p style="color:red">Hi</p>`) の text glyph 描画を成立させる。

**Architecture:** 2 crate に跨る 5 task 構成。(1) raikiri-dom の Node を crate-private → pub struct に昇格し必要 field 5 個 (children / unrounded_layout / kind / tag_name / text_layout) を pub 化 + Document に paint 用 3 accessor (get_node / node_count / root_index) を追加、(2) raikiri-paint の Cargo.toml + module skeleton (walk / text private module) + pub entry stub、(3) walk module: paint_canvas_background (no-op site) + find_body (paint 側自前) + paint_document/element/node の 3-fn 分離 (blitz-mirror `render_element`/`render_node` の separation)、(4) text module: parley Layout.lines()→GlyphRun を anyrender::draw_glyphs に pipe、brush は cascade で inherit された color を peniko::Color 変換、(5) full 8 test suite + verification (clippy / workspace test)。

**Tech Stack:** Rust 2024 edition、rust-version 1.89.0、anyrender 0.11 (workspace)、peniko 0.6 (workspace)、kurbo 0.13 (workspace)、parley 0.10 (workspace)、raikiri-traits / raikiri-dom / raikiri-style (workspace)。テストは `anyrender::Scene` (RenderCommand 記録型) を painter として使用、raster backend (anyrender_vello_cpu) は dev-dep に入れない (raster 経由 test は m1.13/m1.14 の責務)。

## Global Constraints

- 対象 crate: `crates/raikiri-dom` (Node pub 化 + Document accessor 3 個)、`crates/raikiri-paint` (skeleton + walk + text + tests)
- Cleanroom 原則: raikiri-traits の pub API に変更なし (page_box は既存 pub、cascade / dom 型は既存 workspace crate)
- blitz-paint / blitz-dom の source / doc comment を参照しない (memory `raikiri-implementation-independence` §"blitz code の扱い" 準拠、blitz は挙動 oracle のみ、code inspiration は分離)
- Node の `pub(crate)` field は gradual exposure — paint に必要な 5 field のみ pub、他 (style / cache / text_content / inline_style / namespace / attributes) は `pub(crate)` 維持
- raikiri-paint は infallible (raikiri-traits::RenderError に Paint variant なし、m1.6 で pre-shape / layout 済の Document 消費が原理的 infallible)
- test は `#[cfg(test)]` module in `raikiri-paint/src/lib.rs` (doctest は crate cycle 回避、integration は Scene.commands 掘れないので unit で)
- `anyrender::Scene` (dev-dep 追加不要、anyrender 本体に含まれる) を painter として使用、`RenderCommand::GlyphRun(_)` pattern match で assert
- rustc warnings 0 (`-D warnings` gate)、clippy workspace lint 準拠
- 既存 test regression 禁止 (workspace 全体、raikiri-dom 70+44 tests 等)
- 各 task 完了時 commit (frequent commits 原則)、message は `<verb>(<scope>): <what> (raikiri-spike-m1.7)` 形式

---

## File Structure

**Modify (raikiri-dom):**

- `crates/raikiri-dom/src/node.rs:32-70` — `Node` を `pub(crate) struct` → `pub struct` に昇格、5 field を pub 化 (children / unrounded_layout / kind / tag_name / text_layout)、5 field は `pub(crate)` 維持 (style / cache / text_content / inline_style / namespace / attributes)、doc comment 更新
- `crates/raikiri-dom/src/lib.rs:15-24` — `pub use node::Node;` を追加 (raikiri-paint が `&Node` を type name として持てる)
- `crates/raikiri-dom/src/document.rs:29-229` — impl Document block 内に 3 pub method 追加: `get_node(id: usize) -> Option<&Node>` / `node_count() -> usize` / `root_index() -> usize`

**Create (raikiri-paint):**

- `crates/raikiri-paint/src/walk.rs` — DFS walker: `paint_canvas_background` (no-op site) / `paint_document` (body 発見 + start) / `paint_element` (背景 site + children iter) / `paint_node` (kind dispatch) / `find_body` (iterative DFS)
- `crates/raikiri-paint/src/text.rs` — Text 描画: `draw_text_node` (parley Layout iterator → anyrender::draw_glyphs) / `to_anyrender_glyph` (parley::Glyph → anyrender::Glyph) / `css_color_to_peniko` (raikiri_style CssColor → peniko::Color)

**Modify (raikiri-paint):**

- `crates/raikiri-paint/src/lib.rs` — module 宣言 (`mod walk; mod text;`) + `pub fn paint_single_page(...)` 定義 + `#[cfg(test)] mod tests` の 8 unit test
- `crates/raikiri-paint/Cargo.toml` — dependencies 追加 (raikiri-dom / raikiri-style / parley)、dev-dependencies 追加 (raikiri-dom / raikiri-style)

**No change (但し依存関係で確認):**

- `crates/raikiri-traits/src/page.rs` — `PageBox` は既存 pub (m1.6 で populate 済)、変更なし
- `crates/raikiri-style/src/{cascade,computed,property}.rs` — 既存 pub 型 (CascadeResult / ComputedValues / CssColor) をそのまま消費、変更なし

---

## Task 1: raikiri-dom pub 化 (Node + Document accessors)

**Files:**
- Modify: `crates/raikiri-dom/src/node.rs:1-127` (`Node` struct visibility + field visibility + doc comment)
- Modify: `crates/raikiri-dom/src/lib.rs:15-25` (`pub use node::Node;` 追加)
- Modify: `crates/raikiri-dom/src/document.rs:29-229` (`impl Document` block に 3 pub method 追加)

**Interfaces:**
- Consumes: 既存 `Node` struct 定義 (m1.6 raikiri-spike-blg 時点の 11 field)、`Document::nodes` field (`pub(crate)` のまま)、`Document::root` field
- Produces:
  - `pub struct raikiri_dom::Node` (`use raikiri_dom::Node;` で外部 crate から type name 参照可)
  - `Node::children: Vec<usize>` (pub read/write)
  - `Node::unrounded_layout: taffy::Layout` (pub read)
  - `Node::kind: raikiri_traits::NodeKind` (pub read)
  - `Node::tag_name: Option<smol_str::SmolStr>` (pub read)
  - `Node::text_layout: Option<parley::Layout<()>>` (pub read)
  - `Document::get_node(&self, id: usize) -> Option<&Node>` — arena index → node ref, None if out of range
  - `Document::node_count(&self) -> usize` — arena length
  - `Document::root_index(&self) -> usize` — arena index of Document root (always 0)

- [ ] **Step 1: 現状の Node struct を open**

Run: `sed -n '30,70p' crates/raikiri-dom/src/node.rs`
Expected: `#[derive(Debug)]\npub(crate) struct Node { ... }` が見え、11 field 全部が `pub(crate)`

- [ ] **Step 2: Node struct を pub、5 field を pub 化**

Edit `crates/raikiri-dom/src/node.rs`:

```rust
/// Arena node。paint に必要な 5 field は pub、他は crate-private (gradual
/// exposure)。M4 で cascade property 追加時に必要分を pub 化する。
///
/// M1.5 では `style` を Consumer が taffy::Style 直接構築する形。M1.6
/// layout-single-page で ComputedValues → taffy::Style 変換 layer が入る予定。
/// M1.4 で `inline_style` field を追加 (HTML `style="..."` 属性の生 string を保持、
/// raikiri-style::cascade が declaration-list として parse する)。
/// raikiri-spike-blg で `namespace` / `attributes` field を追加
/// (raikiri-html sink が finish 時に side-table から wire)。
/// raikiri-spike-m1.7 で `Node` を pub struct に昇格、paint に必要な field 5 個
/// (children / unrounded_layout / kind / tag_name / text_layout) を pub 化。
#[derive(Debug)]
pub struct Node {
    /// Taffy layout style。
    pub(crate) style: Style,
    /// Child arena indices (`Document::nodes` の usize)。
    pub children: Vec<usize>,
    /// Taffy layout cache (per-node)。
    pub(crate) cache: Cache,
    /// Taffy layout 結果 (compute_root_layout が populate)。
    pub unrounded_layout: Layout,
    /// Node kind (Element / Text / Document)。
    pub kind: NodeKind,
    /// Element tag name (kind == Element 時のみ populate、他は `None`)。
    pub tag_name: Option<SmolStr>,
    /// Text character data (kind == Text 時のみ populate、他は `None`)。
    pub(crate) text_content: Option<SmolStr>,
    /// HTML `style="..."` attribute の生 string (kind == Element 時のみ populate、
    /// 他は `None`)。M1.4 raikiri-style::cascade が消費。
    pub(crate) inline_style: Option<SmolStr>,
    /// Element namespace URI (kind == Element かつ non-HTML の場合のみ `Some`、
    /// HTML default namespace は `None` を fast path とする)。
    /// 例: `Some("http://www.w3.org/2000/svg")`。
    pub(crate) namespace: Option<SmolStr>,
    /// null-namespace attribute list (kind == Element 時のみ populate、他は空)。
    /// 順序保持 (html5ever の source order、cascade tie-breaking で使う想定)。
    /// `style` attribute は [`Node::inline_style`] に分離済のためここには含めない。
    pub(crate) attributes: Vec<Attr>,
    /// Text node の pre-shaped parley Layout。Element / Document は常に None。
    ///
    /// - Populated by [`crate::layout::preshape_text`] (M1.6)
    /// - Consumed by taffy leaf measure closure (intrinsic size) と m1.7 paint
    ///   (glyph 位置)
    /// - Brush type `()` は M1.6 の choice: color / decoration は持たせない
    ///   (paint 段で ComputedValues.color を別途拾う)。M3 で `peniko::Brush`
    ///   等に昇格予定
    /// - Invalidation: `layout_single_page` 呼び出し毎に全 None にクリア +
    ///   再走。granular invalidation は M2+
    pub text_layout: Option<parley::Layout<()>>,
}
```

(constructor `impl Node { ... }` 部分は変更なし、field visibility 変更のみ)

- [ ] **Step 3: raikiri-dom lib.rs に `pub use Node`**

Edit `crates/raikiri-dom/src/lib.rs` の `pub use` block:

```rust
pub use document::Document;
pub use dom_impl::{ChildIter, ElementRef, NodeRef};
pub use layout::layout_single_page;
pub use node::Node;
```

(既存 3 行 + `pub use node::Node;` 1 行追加)

- [ ] **Step 4: Document に 3 pub method を追加**

Edit `crates/raikiri-dom/src/document.rs` の `impl Document { ... }` block 末尾 (invalidate_layout_cache の直前) に挿入:

```rust
    /// arena index `id` の node への借用参照。範囲外 index は `None`。
    ///
    /// blitz-dom の `BaseDocument::get_node` 相当。raikiri-paint が walk 中に
    /// per-node で呼ぶ hot path なので O(1) の `Vec::get` を wrap。
    pub fn get_node(&self, id: usize) -> Option<&Node> {
        self.nodes.get(id)
    }

    /// arena 内の総 node 数 (Document root を含む)。
    ///
    /// raikiri-paint / caller が `cascade.computed.len() == doc.node_count()`
    /// の contract violation を early に検出する目的 + doctest / smoke test で消費。
    pub fn node_count(&self) -> usize {
        self.nodes.len()
    }

    /// Document root の arena index (常に 0)。
    ///
    /// `Dom::root_id()` trait method の inherent 版。trait import せず
    /// `&Document` から直接呼べる。
    /// 命名: `root_index` (type は `usize` で trait method の `NodeId` newtype と区別)
    pub fn root_index(&self) -> usize {
        self.root
    }
```

- [ ] **Step 5: Build を通す**

Run: `cargo build -p raikiri-dom`
Expected: `Finished` (warning 0)。もし parley::Layout の Debug impl 不在で fail した場合、`#[derive(Debug)]` を手動 impl (`impl std::fmt::Debug for Node { ... }`) に降格。m1.6 で `#[derive(Debug)]` が既に通っている実績があるので通常は問題なし。

- [ ] **Step 6: 既存 test で regression チェック**

Run: `cargo test -p raikiri-dom 2>&1 | tail -30`
Expected: 全 test pass (`70 passed; 0 failed`, `44 passed; 0 failed` 等の既存カウント維持)。もし fail が出た場合、pub 化変更の side effect (visibility 変更で move/borrow が変わる) を疑い、compile error を先に修正。

- [ ] **Step 7: 新 pub API の smoke test を追加**

Edit `crates/raikiri-dom/src/lib.rs` の `#[cfg(test)] mod tests` 末尾に追加:

```rust
    // ── raikiri-spike-m1.7 pub 化 smoke test ─────────────

    #[test]
    fn document_get_node_returns_some_for_valid_id_and_none_for_out_of_range() {
        let mut doc = Document::new();
        let p = doc.append_element(Some(0), "p", Style::default(), None::<&str>);
        // valid: root + p の 2 個存在
        assert!(doc.get_node(0).is_some());
        assert!(doc.get_node(p).is_some());
        // out of range: node_count 以上
        assert!(doc.get_node(doc.node_count()).is_none());
        assert!(doc.get_node(doc.node_count() + 100).is_none());
    }

    #[test]
    fn document_node_count_grows_with_appends() {
        let mut doc = Document::new();
        let n0 = doc.node_count(); // root only = 1
        assert_eq!(n0, 1);
        let _e = doc.append_element(Some(0), "e", Style::default(), None::<&str>);
        let _t = doc.append_text(0, "hi");
        assert_eq!(doc.node_count(), n0 + 2);
    }

    #[test]
    fn document_root_index_is_zero_and_matches_get_node_kind() {
        let doc = Document::new();
        assert_eq!(doc.root_index(), 0);
        let root = doc.get_node(doc.root_index()).expect("root exists");
        assert_eq!(root.kind, NodeKind::Document);
    }

    #[test]
    fn node_pub_fields_are_readable_from_external_call_site() {
        // pub 化した 5 field (children / unrounded_layout / kind / tag_name / text_layout)
        // が super::* から見えることを regression pin。
        let mut doc = Document::new();
        let e = doc.append_element(Some(0), "div", Style::default(), None::<&str>);
        let t = doc.append_text(e, "hi");
        let node = doc.get_node(e).unwrap();
        let _ = &node.children;
        let _ = &node.unrounded_layout;
        let _ = &node.kind;
        let _ = &node.tag_name;
        let _ = &node.text_layout;
        // text node kind
        let tn = doc.get_node(t).unwrap();
        assert_eq!(tn.kind, NodeKind::Text);
    }
```

- [ ] **Step 8: Test を run**

Run: `cargo test -p raikiri-dom -- pub_fields document_get_node document_node_count document_root_index 2>&1 | tail -15`
Expected: 4 new test 全 pass、`test result: ok. 4 passed`。

- [ ] **Step 9: workspace 全体 regression 確認**

Run: `cargo build --workspace 2>&1 | tail -5 && cargo test --workspace 2>&1 | grep -E '^test result|FAILED' | tail -30`
Expected: workspace build green (0 warning)、全 test suite `ok`、FAILED なし。

- [ ] **Step 10: Commit**

```bash
git add crates/raikiri-dom/src/node.rs crates/raikiri-dom/src/lib.rs crates/raikiri-dom/src/document.rs
git commit -m "$(cat <<'EOF'
refactor(raikiri-dom): promote Node to pub struct + Document paint accessors (raikiri-spike-m1.7)

Node struct becomes pub with 5 fields also promoted (children / unrounded_layout /
kind / tag_name / text_layout) to support raikiri-paint DFS walk without a bridge
trait (nzv.11 refute). Remaining 5 fields (style / cache / text_content /
inline_style / namespace / attributes) stay pub(crate) — gradual exposure, M4
promotes further as cascade properties join.

Document gains 3 pub accessors: get_node(id) / node_count() / root_index() —
blitz-dom BaseDocument::get_node analogue for hot per-node lookup, plus
contract-check helpers.

Claude-Session: https://claude.ai/code/session_01Xzb6RqUhDy9rgT65ZF1R5V
EOF
)"
```

---

## Task 2: raikiri-paint skeleton (Cargo.toml + pub entry stub + module structure)

**Files:**
- Modify: `crates/raikiri-paint/Cargo.toml` (deps + dev-deps 追加)
- Modify: `crates/raikiri-paint/src/lib.rs` (module 宣言 + `pub fn paint_single_page` stub + tests mod)
- Create: `crates/raikiri-paint/src/walk.rs` (empty stub module — 各 pub(crate) 関数を no-op 実装)
- Create: `crates/raikiri-paint/src/text.rs` (empty stub module — draw_text_node を no-op 実装)

**Interfaces:**
- Consumes: Task 1 の `pub use raikiri_dom::Node`, `Document::{get_node, node_count, root_index}`, `raikiri_style::CascadeResult`, `raikiri_traits::PageBox`, `anyrender::PaintScene`
- Produces:
  - `pub fn raikiri_paint::paint_single_page(scene: &mut impl PaintScene, document: &Document, cascade: &CascadeResult, page_box: PageBox)` — infallible
  - `pub(crate) fn walk::paint_canvas_background(scene, doc, cascade, page_box)` — no-op stub
  - `pub(crate) fn walk::paint_document(scene, doc, cascade)` — no-op stub (Task 3 で実装)
  - `pub(crate) fn text::draw_text_node(scene, node, cascade, node_id, abs_x, abs_y)` — no-op stub (Task 4 で実装)

- [ ] **Step 1: Cargo.toml に dependencies を追加**

Edit `crates/raikiri-paint/Cargo.toml`:

```toml
[package]
name = "raikiri-paint"
version = "0.1.0"
description = "PageFragment → anyrender::PaintScene walker for raikiri (VRT and debug output)"
edition.workspace = true
rust-version.workspace = true
license.workspace = true

[dependencies]
raikiri-traits = { workspace = true }
raikiri-dom    = { workspace = true }
raikiri-style  = { workspace = true }
anyrender      = { workspace = true }
peniko         = { workspace = true }
kurbo          = { workspace = true }
parley         = { workspace = true }

[dev-dependencies]
raikiri-dom   = { workspace = true }
raikiri-style = { workspace = true }

[lints]
workspace = true
```

(既存 M0 stub `raikiri-traits / anyrender / peniko / kurbo` に加えて `raikiri-dom / raikiri-style / parley` の 3 dep を追加、dev-deps に `raikiri-dom / raikiri-style`)

- [ ] **Step 2: walk.rs stub を新規作成**

Create `crates/raikiri-paint/src/walk.rs`:

```rust
//! DOM walker — Document arena を DFS で walk し PaintScene に emit する。
//!
//! `paint_document` / `paint_element` / `paint_node` の 3 関数分離は
//! separation of concerns — paint_element = element 描画責任 (背景/border/
//! children walk、M4 で肉付け)、paint_node = kind dispatch のみ。M3 で inline
//! formatting context を実装する時に paint_element を変えずに paint_node の
//! Text 分岐だけ unreachable 化できる。
//!
//! find_body は raikiri-dom::layout::find_body と重複するが、5 行の helper
//! を crate 境界越境で pub 化するよりも paint 側で持つ方が clean。

use anyrender::PaintScene;
use raikiri_dom::Document;
use raikiri_style::CascadeResult;
use raikiri_traits::PageBox;

pub(crate) fn paint_canvas_background(
    _scene: &mut impl PaintScene,
    _document: &Document,
    _cascade: &CascadeResult,
    _page_box: PageBox,
) {
    // Task 3 で実装
}

pub(crate) fn paint_document(
    _scene: &mut impl PaintScene,
    _document: &Document,
    _cascade: &CascadeResult,
) {
    // Task 3 で実装
}
```

- [ ] **Step 3: text.rs stub を新規作成**

Create `crates/raikiri-paint/src/text.rs`:

```rust
//! Text glyph draw — parley Layout の GlyphRun を anyrender::draw_glyphs に pipe。
//!
//! m1.6 で `Node.text_layout: Option<parley::Layout<()>>` に pre-shape 済 Layout
//! を格納する design に依拠。paint は line iteration + GlyphRun.positioned_glyphs()
//! を per-run 変換 (parley::Glyph → anyrender::Glyph) して scene に送るだけ。

use anyrender::PaintScene;
use raikiri_dom::Node;
use raikiri_style::CascadeResult;

pub(crate) fn draw_text_node(
    _scene: &mut impl PaintScene,
    _node: &Node,
    _cascade: &CascadeResult,
    _node_id: usize,
    _abs_x: f32,
    _abs_y: f32,
) {
    // Task 4 で実装
}
```

- [ ] **Step 4: lib.rs に module 宣言 + pub 関数 + doc header**

Overwrite `crates/raikiri-paint/src/lib.rs`:

```rust
//! raikiri-paint — Document + CascadeResult → anyrender::PaintScene walker.
//!
//! nzv.11 の refute 結果 (docs/feasibility-report.md §3.4) に従い、bridge trait
//! を挟まず `impl anyrender::PaintScene` を直接消費する。M1 は単一 A4 ページ +
//! text glyphs のみ、element background / border / decoration は M4+ に defer。
//!
//! ## Contract
//!
//! - `document` は m1.6 `layout_single_page` を呼び終えた post-layout 状態を前提
//!   (Node.unrounded_layout / Node.text_layout populate 済)
//! - `cascade.computed.len() == document.node_count()` を前提 (caller 責任)
//! - `scene.reset()` の呼び出しは caller 責任 (blitz-paint と同じ convention)
//! - infallible — raikiri-traits::RenderError に Paint variant はない
//!   (m1.6 で pre-shape / layout 済の Document 消費が原理的 infallible)

use anyrender::PaintScene;
use raikiri_dom::Document;
use raikiri_style::CascadeResult;
use raikiri_traits::PageBox;

mod walk;
mod text;

/// 単一 A4 (or 指定 PageBox) ページに Document + CascadeResult を paint する。
///
/// # 呼び出し順序
/// 1. `walk::paint_canvas_background` — canvas 背景 fill site (M1.4 no-op、M4 で発火)
/// 2. `walk::paint_document` — body から始まる DFS walk、Text node で glyph draw
///
/// # Non-goals (M1.7 scope)
/// - Multi-page pagination → M2
/// - Element background-color / border / box-shadow → M4+
/// - Text decoration (underline / line-through) → M3
/// - z-index / stacking context → M4+
/// - CSS transform (rotate/scale/skew) → M4+
/// - DPI scaling → m1.14 or m8 (`paint_single_page_scaled` 別関数で拡張)
pub fn paint_single_page(
    scene: &mut impl PaintScene,
    document: &Document,
    cascade: &CascadeResult,
    page_box: PageBox,
) {
    walk::paint_canvas_background(scene, document, cascade, page_box);
    walk::paint_document(scene, document, cascade);
}

#[cfg(test)]
mod tests {
    // Task 3 以降で unit test を追加。ここでは compile pass の smoke test のみ。
    use super::*;
    use anyrender::Scene;
    use raikiri_dom::Document;
    use raikiri_style::{build_rule_tree, cascade};
    use raikiri_traits::PageBox;

    #[test]
    fn paint_single_page_compiles_and_returns_unit() {
        let doc = Document::new();
        let rules = build_rule_tree(&doc);
        let cr = cascade(&doc, &rules).expect("cascade Ok");
        let mut scene = Scene::new();
        paint_single_page(&mut scene, &doc, &cr, PageBox::A4);
        // stub なので commands は積まれない
        assert!(scene.commands.is_empty(), "stub paint_single_page should emit no commands");
    }
}
```

- [ ] **Step 5: Build を通す**

Run: `cargo build -p raikiri-paint 2>&1 | tail -10`
Expected: `Finished` (warning 0)。`unused imports` があれば fix (Task 3/4 で消費するので `#[allow(unused_imports)]` は不要、stub の署名 params が使わない `_` prefix になっていれば OK)。

- [ ] **Step 6: Compile smoke test を run**

Run: `cargo test -p raikiri-paint -- compiles 2>&1 | tail -10`
Expected: `paint_single_page_compiles_and_returns_unit ... ok`、`1 passed`。

- [ ] **Step 7: Commit**

```bash
git add crates/raikiri-paint/Cargo.toml crates/raikiri-paint/src/lib.rs crates/raikiri-paint/src/walk.rs crates/raikiri-paint/src/text.rs
git commit -m "$(cat <<'EOF'
feat(raikiri-paint): add crate skeleton + pub paint_single_page stub (raikiri-spike-m1.7)

Populate module structure (walk / text private modules) and pub entry function.
Both submodules are no-op stubs; Task 3-4 will fill in walker + text draw. Adds
raikiri-dom / raikiri-style / parley to [dependencies] and raikiri-dom /
raikiri-style to [dev-dependencies] for the test scene builder.

Contract is documented on paint_single_page: post-layout Document + matching
CascadeResult, caller-owned scene reset (blitz convention), infallible.

Claude-Session: https://claude.ai/code/session_01Xzb6RqUhDy9rgT65ZF1R5V
EOF
)"
```

---

## Task 3: walk module 実装 (canvas fill site + find_body + paint_document/element/node)

**Files:**
- Modify: `crates/raikiri-paint/src/walk.rs` (5 pub(crate) 関数を stub → 実装)
- Modify: `crates/raikiri-paint/src/lib.rs:tests` (3 unit test 追加: canvas no-op / fragment early return / zero-size subtree skip)

**Interfaces:**
- Consumes: `raikiri_dom::{Document, Node}`, `raikiri_traits::{PageBox, NodeKind}`, `anyrender::PaintScene`, `raikiri_style::CascadeResult`, `crate::text::draw_text_node`
- Produces:
  - `pub(crate) fn walk::paint_canvas_background(...)` — M1.4 no-op、M4 で blitz-paint render.rs:120-162 と同じ html→body 伝播ロジック実装 (CSS Backgrounds L3 §2.11.2)
  - `pub(crate) fn walk::paint_document(scene, doc, cascade)` — find_body で body 発見 → paint_element(body, 0.0, 0.0) start
  - `pub(crate) fn walk::paint_element(scene, doc, cascade, node_id, parent_abs_x, parent_abs_y)` — zero-size skip + cumulative offset + children iter で paint_node dispatch
  - `pub(crate) fn walk::paint_node(scene, doc, cascade, node_id, parent_abs_x, parent_abs_y)` — kind dispatch: Element → paint_element、Text → text::draw_text_node、Document → return
  - `fn walk::find_body(doc) -> Option<usize>` — iterative DFS (raikiri-dom::layout::find_body と同ロジックだが crate 境界越境なしで自前実装)

- [ ] **Step 1: walk.rs を full 実装で置換**

Overwrite `crates/raikiri-paint/src/walk.rs`:

```rust
//! DOM walker — Document arena を DFS で walk し PaintScene に emit する。
//!
//! `paint_document` / `paint_element` / `paint_node` の 3 関数分離は
//! separation of concerns — paint_element = element 描画責任 (背景/border/
//! children walk、M4 で肉付け)、paint_node = kind dispatch のみ。M3 で inline
//! formatting context を実装する時に paint_element を変えずに paint_node の
//! Text 分岐だけ unreachable 化できる。
//!
//! find_body は raikiri-dom::layout::find_body と重複するが、5 行の helper
//! を crate 境界越境で pub 化するよりも paint 側で持つ方が clean。

use anyrender::PaintScene;
use raikiri_dom::Document;
use raikiri_style::CascadeResult;
use raikiri_traits::{NodeKind, PageBox};

use crate::text;

/// Canvas 背景 fill site。M1.4 では cascade に background-color 無しで実質 no-op、
/// future-proof pin として存在。M4 で CSS Backgrounds L3 §2.11.2 "canvas
/// propagation" (html の background-color を取得、TRANSPARENT なら body に
/// fallback、Some なら PageBox 全域を fill) を実装する。
pub(crate) fn paint_canvas_background(
    _scene: &mut impl PaintScene,
    _document: &Document,
    _cascade: &CascadeResult,
    _page_box: PageBox,
) {
    // M1.4: no-op site。M4 で発火。
}

/// Document arena を body から DFS walk する。fragment (no `<body>`) case は
/// silent return (m1.6 layout_single_page が Err を返すので paint 呼び出し
/// 前に検出済のはず、defensive)。
pub(crate) fn paint_document(
    scene: &mut impl PaintScene,
    document: &Document,
    cascade: &CascadeResult,
) {
    let Some(body_id) = find_body(document) else { return; };
    paint_element(scene, document, cascade, body_id, 0.0, 0.0);
}

/// Element node の描画。M1.7 では background site を no-op で置く + children
/// を document order で walk。M4 で element background-color / border /
/// box-shadow の描画をここに追加。zero-size subtree は skip
/// (display:none 相当、taffy が LayoutOutput::HIDDEN で size=0 を出す)。
pub(crate) fn paint_element(
    scene: &mut impl PaintScene,
    document: &Document,
    cascade: &CascadeResult,
    node_id: usize,
    parent_abs_x: f32,
    parent_abs_y: f32,
) {
    let Some(node) = document.get_node(node_id) else { return; };
    let layout = node.unrounded_layout;
    if layout.size.width <= 0.0 || layout.size.height <= 0.0 {
        return;
    }
    let abs_x = parent_abs_x + layout.location.x;
    let abs_y = parent_abs_y + layout.location.y;
    // paint_element_background(scene, node, abs_x, abs_y, cascade) — M4 でここに挿入
    for &child in &node.children {
        paint_node(scene, document, cascade, child, abs_x, abs_y);
    }
}

/// Kind dispatch。M1.7 は Element / Text の 2 分岐、Document は通常通らない。
/// M3 で AnonymousBlock (inline wrapper) 等が加わる、M3 以降で inline formatting
/// context を実装する時は Text 分岐が unreachable 化する予定。
pub(crate) fn paint_node(
    scene: &mut impl PaintScene,
    document: &Document,
    cascade: &CascadeResult,
    node_id: usize,
    parent_abs_x: f32,
    parent_abs_y: f32,
) {
    let Some(node) = document.get_node(node_id) else { return; };
    match node.kind {
        NodeKind::Element => paint_element(scene, document, cascade, node_id, parent_abs_x, parent_abs_y),
        NodeKind::Text => {
            let layout = node.unrounded_layout;
            let abs_x = parent_abs_x + layout.location.x;
            let abs_y = parent_abs_y + layout.location.y;
            text::draw_text_node(scene, node, cascade, node_id, abs_x, abs_y);
        }
        NodeKind::Document => {
            // paint_document が body から start するので通常来ない。
            // Document node は children を持ちうる (未 attach <html>) が M1.7 では扱わない。
        }
    }
}

/// Document arena を DFS で walk し、最初の `<body>` element の arena index を返す。
///
/// iterative `Vec` stack で実装 (cascade §deep_nesting の pattern と一貫、
/// 深 DOM で stack overflow を回避)。fragment parse (no `<body>`) では `None`。
fn find_body(doc: &Document) -> Option<usize> {
    let mut stack: Vec<usize> = vec![doc.root_index()];
    while let Some(id) = stack.pop() {
        let node = doc.get_node(id)?;
        if node.kind == NodeKind::Element && node.tag_name.as_deref() == Some("body") {
            return Some(id);
        }
        for &c in node.children.iter().rev() {
            stack.push(c);
        }
    }
    None
}
```

- [ ] **Step 2: Build を通す (compile check)**

Run: `cargo build -p raikiri-paint 2>&1 | tail -10`
Expected: `Finished` (warning 0)。text::draw_text_node は Task 2 の stub のまま (Task 4 で実装)。

- [ ] **Step 3: 3 unit test を lib.rs に追加**

Edit `crates/raikiri-paint/src/lib.rs` の `#[cfg(test)] mod tests` を以下で置換 (既存 `paint_single_page_compiles_and_returns_unit` は残す):

```rust
#[cfg(test)]
mod tests {
    use super::*;
    use anyrender::Scene;
    use anyrender::recording::RenderCommand;
    use raikiri_dom::{Document, layout_single_page};
    use raikiri_style::{build_rule_tree, cascade};
    use raikiri_traits::PageBox;
    use taffy::{Dimension, Display, Size, Style};

    /// hello-world (`<html><head></head><body><p style="color:red">Hi</p></body></html>`) を
    /// m1.6 layout_single_page まで完了させた Document + CascadeResult を返す。
    /// m1.7 test 全ての共通 setup。
    fn hello_world_paint_setup() -> (Document, raikiri_style::CascadeResult) {
        let mut doc = Document::new();
        let html = doc.append_element(Some(0), "html", Style::default(), None::<&str>);
        let _head = doc.append_element(Some(html), "head", Style::default(), None::<&str>);
        let body = doc.append_element(Some(html), "body", Style::default(), None::<&str>);
        let p = doc.append_element(Some(body), "p", Style::default(), Some("color:red"));
        let _t = doc.append_text(p, "Hi");
        let rules = build_rule_tree(&doc);
        let cr = cascade(&doc, &rules).expect("cascade Ok");
        layout_single_page(&mut doc, &cr, PageBox::A4).expect("layout Ok");
        (doc, cr)
    }

    #[test]
    fn paint_single_page_compiles_and_returns_unit() {
        let doc = Document::new();
        let rules = build_rule_tree(&doc);
        let cr = cascade(&doc, &rules).expect("cascade Ok");
        let mut scene = Scene::new();
        paint_single_page(&mut scene, &doc, &cr, PageBox::A4);
        // fragment (no body) なので paint_document は早期 return、canvas は no-op site
        assert!(scene.commands.is_empty(), "empty Document should emit no commands");
    }

    #[test]
    fn paint_single_page_canvas_background_site_is_noop_at_m1_4() {
        // canvas fill site が M1.4 で命令を積まないことを pin。
        // M4 で background-color が cascade に入った時にこの test が反転する
        // ("commands should be non-empty" で fail する)、その時に M4 実装完了の合図。
        let (doc, cr) = hello_world_paint_setup();
        let mut scene = Scene::new();
        // canvas fill 単体を呼ぶ相当の効果を得るため、paint_single_page 全体 - text glyph の
        // 差分で判定 (canvas site が emit すれば Fill command が glyph より前に来る)。
        // Task 4 完了までは text も stub なので、全体 commands 0 でも OK。
        // Task 4 完了後にこの test は "GlyphRun のみ、Fill (canvas) は無い" に精緻化予定。
        paint_single_page(&mut scene, &doc, &cr, PageBox::A4);
        let fill_commands: Vec<_> = scene.commands.iter()
            .filter(|c| matches!(c, RenderCommand::Fill(_)))
            .collect();
        assert!(fill_commands.is_empty(), "M1.4 canvas site should emit no Fill, got {:?}", fill_commands.len());
    }

    #[test]
    fn paint_single_page_without_body_returns_early() {
        // fragment (Document → <p> 直子、no <body>) → paint は何も emit しない。
        // m1.6 layout_single_page は fragment で Err を返すが、paint 側は
        // defensive に silent return する契約 (early return without touching scene)。
        let mut doc = Document::new();
        let p = doc.append_element(Some(0), "p", Style::default(), None::<&str>);
        let _t = doc.append_text(p, "Hi");
        let rules = build_rule_tree(&doc);
        let cr = cascade(&doc, &rules).expect("cascade Ok");
        // layout_single_page はここでは呼ばない (Err になる)、paint 単体で silent
        // return するかを test。
        let mut scene = Scene::new();
        paint_single_page(&mut scene, &doc, &cr, PageBox::A4);
        assert!(scene.commands.is_empty(), "fragment (no body) should emit no commands");
    }

    #[test]
    fn paint_single_page_skips_zero_size_subtree() {
        // display:none 相当を模した zero-size element (display: None, size 明示) が
        // walk されないことを pin。paint_element の early return が正しく発火する
        // regression pin (m1.6 の layout side でも empty_display_none_leaf... で pin 済)。
        let mut doc = Document::new();
        let html = doc.append_element(Some(0), "html", Style::default(), None::<&str>);
        let body = doc.append_element(Some(html), "body", Style::default(), None::<&str>);
        // hidden element を body 直下に置く (display: None、size 0)
        let hidden_style = Style {
            display: Display::None,
            size: Size {
                width: Dimension::length(100.0),
                height: Dimension::length(50.0),
            },
            ..Default::default()
        };
        let hidden = doc.append_element(Some(body), "hidden", hidden_style, None::<&str>);
        let _child_of_hidden = doc.append_text(hidden, "should not be painted");
        let rules = build_rule_tree(&doc);
        let cr = cascade(&doc, &rules).expect("cascade Ok");
        layout_single_page(&mut doc, &cr, PageBox::A4).expect("layout Ok");
        // hidden の layout size は 0 になっているはず (taffy LayoutOutput::HIDDEN)
        let hidden_layout = doc.get_node(hidden).unwrap().unrounded_layout;
        assert_eq!(
            hidden_layout.size.width, 0.0,
            "display:none should have zero size after taffy layout"
        );
        let mut scene = Scene::new();
        paint_single_page(&mut scene, &doc, &cr, PageBox::A4);
        // Task 4 完了までは text も stub、Task 4 後は glyph run 0 個 (hidden 配下の text は skip)
        let glyph_commands: Vec<_> = scene.commands.iter()
            .filter(|c| matches!(c, RenderCommand::GlyphRun(_)))
            .collect();
        assert!(
            glyph_commands.is_empty(),
            "text inside display:none subtree should not be painted, got {}",
            glyph_commands.len()
        );
    }
}
```

**注意**: `paint_single_page_hello_world_emits_one_glyph_run` / `_uses_inherited_color_as_brush` / `_positions_glyphs_via_absolute_offset` / `_skips_empty_text` / `_can_be_called_multiple_times` の 5 test は Task 4 (text 実装) / Task 5 (re-entrance) で追加する。ここでは Task 3 の contract だけ pin する。

- [ ] **Step 4: 4 test を run**

Run: `cargo test -p raikiri-paint 2>&1 | tail -15`
Expected: 4 test 全 pass (`paint_single_page_compiles_and_returns_unit`, `paint_single_page_canvas_background_site_is_noop_at_m1_4`, `paint_single_page_without_body_returns_early`, `paint_single_page_skips_zero_size_subtree`)、`test result: ok. 4 passed`。

- [ ] **Step 5: workspace regression 確認**

Run: `cargo test --workspace 2>&1 | grep -E '^test result|FAILED' | tail -10`
Expected: 全 crate 全 test pass (FAILED なし)。

- [ ] **Step 6: Commit**

```bash
git add crates/raikiri-paint/src/walk.rs crates/raikiri-paint/src/lib.rs
git commit -m "$(cat <<'EOF'
feat(raikiri-paint): DOM walker + canvas fill site + kind dispatch (raikiri-spike-m1.7)

Implements walk module with 5 functions:
- paint_canvas_background: M1.4 no-op site (M4 fires when background-color joins
  cascade, blitz-paint render.rs canvas propagation shape)
- paint_document: find_body + start DFS
- paint_element: zero-size early skip + cumulative offset + children iter
- paint_node: kind dispatch (Element / Text via text stub / Document no-op)
- find_body: iterative Vec stack (deep-nesting stack-overflow-safe)

3-fn separation (paint_document/element/node) mirrors blitz-paint render_element
+ render_node — M3 inline formatting context can flip paint_node Text arm to
unreachable without touching paint_element.

Pins 3 contracts via Scene command inspection: canvas site emits nothing at
M1.4, fragment (no body) is silent no-op, display:none subtrees skip walk.

Claude-Session: https://claude.ai/code/session_01Xzb6RqUhDy9rgT65ZF1R5V
EOF
)"
```

---

## Task 4: text module 実装 (parley GlyphRun → anyrender::draw_glyphs bridge)

**Files:**
- Modify: `crates/raikiri-paint/src/text.rs` (stub → full 実装)
- Modify: `crates/raikiri-paint/src/lib.rs:tests` (3 test 追加: hello world 1 glyph run / brush color / positioning / empty text skip)

**Interfaces:**
- Consumes: Task 1 の `raikiri_dom::Node.text_layout: Option<parley::Layout<()>>`, `raikiri_style::CascadeResult.computed`, `raikiri_style::property::CssColor` (r/g/b/a: u8), `anyrender::PaintScene::draw_glyphs`
- Produces:
  - `pub(crate) fn text::draw_text_node(scene, node, cascade, node_id, abs_x, abs_y)` — text_layout None → silent skip、Some → per-line per-GlyphRun iterate + draw_glyphs
  - `fn text::to_anyrender_glyph(g: parley::Glyph) -> anyrender::Glyph` — u16 id → u32、x/y そのまま
  - `fn text::css_color_to_peniko(c: raikiri_style::property::CssColor) -> peniko::Color`

- [ ] **Step 1: text.rs を full 実装で置換**

Overwrite `crates/raikiri-paint/src/text.rs`:

```rust
//! Text glyph draw — parley Layout の GlyphRun を anyrender::draw_glyphs に pipe。
//!
//! m1.6 で `Node.text_layout: Option<parley::Layout<()>>` に pre-shape 済 Layout
//! を格納する design に依拠。paint は line iteration + GlyphRun.positioned_glyphs()
//! を per-run 変換 (parley::Glyph → anyrender::Glyph、u16→u32) して scene に送る。
//!
//! 座標系: parley Layout origin (0,0) 左上、positioned_glyphs() が line 内
//! offset + baseline を baked-in。anyrender transform は text node の絶対座標
//! への平行移動のみで済む (baseline / offset 加算不要)。

use anyrender::{Glyph as AnyrenderGlyph, PaintScene};
use kurbo::{Affine, Vec2};
use parley::{Glyph as ParleyGlyph, PositionedLayoutItem};
use peniko::{Color, Fill};
use raikiri_dom::Node;
use raikiri_style::CascadeResult;
use raikiri_style::property::CssColor;

pub(crate) fn draw_text_node(
    scene: &mut impl PaintScene,
    node: &Node,
    cascade: &CascadeResult,
    node_id: usize,
    abs_x: f32,
    abs_y: f32,
) {
    // Text node の pre-shape 結果を取得。preshape_text が empty text で None を返す
    // ので、None = "empty text" の signal、silent return。
    let Some(text_layout) = node.text_layout.as_ref() else { return; };

    // Text node の brush = cascade で親から inherit された color (M1.4 は color のみ)。
    // ComputedValues.color は Text node 位置にも populate 済 (m1.4 inheritance walk)。
    let cv = &cascade.computed[node_id];
    let brush = css_color_to_peniko(cv.color);

    // parley positioned_glyphs() は line 内 offset + baseline を baked-in するので
    // scene transform は text node の絶対座標への平行移動のみ。
    let base_transform = Affine::translate((abs_x as f64, abs_y as f64));

    for line in text_layout.lines() {
        for item in line.items() {
            let PositionedLayoutItem::GlyphRun(glyph_run) = item else {
                // M1.7 では InlineBox は生成されない (m1.6 preshape_text は inline box
                // を push しない)。M3 で inline formatting context を実装する時に
                // ここで image / replaced element 描画が入る。defensive: continue。
                continue;
            };

            let run = glyph_run.run();
            let font = run.font();                 // &peniko::FontData (parley re-export)
            let font_size = run.font_size();
            let coords = run.normalized_coords();  // &[NormalizedCoord]

            scene.draw_glyphs(
                font,
                font_size,
                true,                    // hint = true (blitz と揃え、m1.13 で判定 flip 余地)
                coords,
                Vec2::ZERO,              // embolden 無し (font-embolden feature は M6+)
                Fill::NonZero,
                brush,                   // peniko::Color → PaintRef auto-convert
                1.0,                     // brush_alpha (color 自身が alpha 持つ)
                base_transform,
                None,                    // glyph_transform (rotate / skew は M4+)
                glyph_run.positioned_glyphs().map(to_anyrender_glyph),
            );
        }
    }
}

/// parley::Glyph → anyrender::Glyph の変換 (id 型が u16 vs u32 で異なる)。
fn to_anyrender_glyph(g: ParleyGlyph) -> AnyrenderGlyph {
    AnyrenderGlyph {
        id: g.id as u32,
        x: g.x,
        y: g.y,
    }
}

/// raikiri-style の CssColor (r/g/b/a: u8) → peniko::Color。
///
/// M3 で `peniko::AlphaColor` へ昇格する move に備えて helper を切り出しておく
/// (今は 1 line だが grep しやすい)。
fn css_color_to_peniko(c: CssColor) -> Color {
    Color::from_rgba8(c.r, c.g, c.b, c.a)
}
```

**parley 0.10 API 差分吸収**: 上の code は sketched pseudo-code + m1.6 の parley 実 API 使用実績に基づいた記述。実装時に compile error が出た場合の対応:
- `Glyph` の re-export path: `parley::Glyph` で通らなければ `parley::layout::Glyph` を試す
- `run.font()` 戻り値: `&peniko::FontData` を期待、実際は `&FontData` where `FontData` は `parley` 経由の re-export。名前空間が違えば use path 調整
- `normalized_coords()` の戻り値型: `&[NormalizedCoord]` where NormalizedCoord は skrifa 型、anyrender の第 4 引数と match するはず (parley/skrifa/anyrender の版が workspace で合わせて解決済)

- [ ] **Step 2: Build を通す**

Run: `cargo build -p raikiri-paint 2>&1 | tail -15`
Expected: `Finished` (warning 0)。もし parley API mismatch で fail した場合、compile error message を読み `Step 1 補足` に沿って fix。

- [ ] **Step 3: 4 unit test を lib.rs の tests mod に追加**

Edit `crates/raikiri-paint/src/lib.rs` の `mod tests` に (既存 4 test の後ろに) 追加:

```rust
    #[test]
    fn paint_single_page_hello_world_emits_one_glyph_run() {
        // hello-world "Hi" → 1 GlyphRun が emit されることを pin。
        let (doc, cr) = hello_world_paint_setup();
        let mut scene = Scene::new();
        paint_single_page(&mut scene, &doc, &cr, PageBox::A4);
        let glyph_commands: Vec<_> = scene.commands.iter()
            .filter(|c| matches!(c, RenderCommand::GlyphRun(_)))
            .collect();
        assert_eq!(
            glyph_commands.len(), 1,
            "hello world 'Hi' should emit exactly 1 GlyphRun, got {}",
            glyph_commands.len()
        );
    }

    #[test]
    fn paint_single_page_uses_inherited_color_as_brush() {
        // <p style="color:red"> → cascade で red が inherit → text の brush が
        // (255, 0, 0, 255) になることを pin。cascade → shape 系譜の regression 保護。
        use anyrender::types::Paint;
        use peniko::Color;

        let (doc, cr) = hello_world_paint_setup();
        let mut scene = Scene::new();
        paint_single_page(&mut scene, &doc, &cr, PageBox::A4);
        let RenderCommand::GlyphRun(glyph_cmd) = scene.commands.iter()
            .find(|c| matches!(c, RenderCommand::GlyphRun(_)))
            .expect("must have 1 GlyphRun")
        else { unreachable!() };
        match &glyph_cmd.brush {
            Paint::Solid(color) => {
                assert_eq!(
                    *color,
                    Color::from_rgba8(255, 0, 0, 255),
                    "inline color:red must produce Color::from_rgba8(255,0,0,255) brush"
                );
            }
            other => panic!("expected Paint::Solid, got {:?}", std::mem::discriminant(other)),
        }
    }

    #[test]
    fn paint_single_page_positions_glyphs_via_absolute_offset() {
        // body / p / text の accumulate location が draw_glyphs の transform に反映
        // される regression pin。text node の origin は body.location + p.location +
        // text.location (block layout の flow 累積)、正の y を持つはず。
        let (doc, cr) = hello_world_paint_setup();
        let mut scene = Scene::new();
        paint_single_page(&mut scene, &doc, &cr, PageBox::A4);
        let RenderCommand::GlyphRun(glyph_cmd) = scene.commands.iter()
            .find(|c| matches!(c, RenderCommand::GlyphRun(_)))
            .expect("must have 1 GlyphRun")
        else { unreachable!() };
        // Affine::translate((x, y)) の translation 部分は matrix の [4, 5] (2 次元 identity + translation)
        let translation = glyph_cmd.transform.translation();
        assert!(
            translation.x >= 0.0,
            "text abs_x should be non-negative (body flow from left edge), got {}",
            translation.x
        );
        assert!(
            translation.y >= 0.0,
            "text abs_y should be non-negative (body flow from top edge), got {}",
            translation.y
        );
        // Note: body / p の location は M1.4 では通常 (0, 0)、text node の
        // location は line box top なので 0 に近いはず。厳密な値 pin は m1.13
        // determinism test で。ここでは "正 offset を持つ" の regression pin のみ。
    }

    #[test]
    fn paint_single_page_skips_empty_text() {
        // text_layout が None (empty text) の Text node は draw_glyphs を呼ばず
        // silent skip する contract。preshape_text が empty text で text_layout = None
        // を残す仕様と対称。
        let mut doc = Document::new();
        let html = doc.append_element(Some(0), "html", Style::default(), None::<&str>);
        let body = doc.append_element(Some(html), "body", Style::default(), None::<&str>);
        let p = doc.append_element(Some(body), "p", Style::default(), None::<&str>);
        let _t = doc.append_text(p, "");  // ← empty text
        let rules = build_rule_tree(&doc);
        let cr = cascade(&doc, &rules).expect("cascade Ok");
        layout_single_page(&mut doc, &cr, PageBox::A4).expect("layout Ok");
        let mut scene = Scene::new();
        paint_single_page(&mut scene, &doc, &cr, PageBox::A4);
        let glyph_commands: Vec<_> = scene.commands.iter()
            .filter(|c| matches!(c, RenderCommand::GlyphRun(_)))
            .collect();
        assert!(
            glyph_commands.is_empty(),
            "empty text should not emit any GlyphRun, got {}",
            glyph_commands.len()
        );
    }
```

**注意**: `paint_single_page_uses_inherited_color_as_brush` の GlyphRunCommand の内部 field 名 (`brush`, `transform`) は anyrender::recording::RenderCommand の実 field。実装時に compile error が出た場合、`anyrender::recording` の doc で確認して field 名を修正。

- [ ] **Step 4: 8 test を run (4 既存 + 4 new)**

Run: `cargo test -p raikiri-paint 2>&1 | tail -15`
Expected: 全 8 test pass。もし positioning test の `translation.y >= 0.0` が fail したら parley の baseline offset の accumulation を再確認 (baseline は positioned_glyphs で baked-in なので transform には含まない、y は text node の layout.location.y のみ)。

- [ ] **Step 5: workspace regression 確認**

Run: `cargo test --workspace 2>&1 | grep -E '^test result|FAILED' | tail -10`
Expected: 全 crate 全 test pass。

- [ ] **Step 6: Commit**

```bash
git add crates/raikiri-paint/src/text.rs crates/raikiri-paint/src/lib.rs
git commit -m "$(cat <<'EOF'
feat(raikiri-paint): draw_text_node bridges parley GlyphRun to anyrender (raikiri-spike-m1.7)

Implements text module: line iteration over parley Layout, per-GlyphRun
draw_glyphs call with brush from CascadeResult.computed[node_id].color
(cascade already inherited the color to the text node via m1.4 walk), Affine
translate to text node absolute origin (baseline + inline offset baked into
positioned_glyphs by parley, no extra addition needed).

parley::Glyph → anyrender::Glyph conversion widens u16 id to u32.
CssColor (u8 rgba) → peniko::Color via from_rgba8 — grep-friendly helper for
M3 AlphaColor promotion.

Pins 4 contracts: hello world "Hi" emits exactly 1 GlyphRun, brush is
red (255,0,0,255) via inherited color, absolute offset transform is applied,
empty text is silent-skipped.

Claude-Session: https://claude.ai/code/session_01Xzb6RqUhDy9rgT65ZF1R5V
EOF
)"
```

---

## Task 5: Re-entrance test + verification (clippy / workspace tests / final commit)

**Files:**
- Modify: `crates/raikiri-paint/src/lib.rs:tests` (1 test 追加: paint_single_page_can_be_called_multiple_times)
- No other file changes

**Interfaces:**
- Consumes: 全 Task 1-4 の実装
- Produces:
  - test suite の完成 (8 unit test 全揃い)
  - workspace build / test / clippy green の gate 通過証明

- [ ] **Step 1: re-entrance test を追加**

Edit `crates/raikiri-paint/src/lib.rs` の `mod tests` に (前 test の後ろに) 追加:

```rust
    #[test]
    fn paint_single_page_can_be_called_multiple_times() {
        // 同じ Document を 2 回 paint、2 回とも同じ command sequence を produce
        // (state mutation なし、re-entrance safety pin)。将来 paint 側で cache
        // 導入した時の silent regression 検出用 pin。
        let (doc, cr) = hello_world_paint_setup();

        let mut scene1 = Scene::new();
        paint_single_page(&mut scene1, &doc, &cr, PageBox::A4);
        let count1 = scene1.commands.len();
        let glyph_count1 = scene1.commands.iter()
            .filter(|c| matches!(c, RenderCommand::GlyphRun(_)))
            .count();

        let mut scene2 = Scene::new();
        paint_single_page(&mut scene2, &doc, &cr, PageBox::A4);
        let count2 = scene2.commands.len();
        let glyph_count2 = scene2.commands.iter()
            .filter(|c| matches!(c, RenderCommand::GlyphRun(_)))
            .count();

        assert_eq!(count1, count2, "total command count must be identical across calls");
        assert_eq!(glyph_count1, glyph_count2, "GlyphRun count must be identical across calls");
        assert_eq!(glyph_count1, 1, "hello world must emit exactly 1 GlyphRun");
    }
```

- [ ] **Step 2: Full 9 test suite を run (8 unit test + 1 compile smoke)**

Run: `cargo test -p raikiri-paint 2>&1 | tail -20`
Expected:
- `paint_single_page_compiles_and_returns_unit` (Task 2)
- `paint_single_page_canvas_background_site_is_noop_at_m1_4` (Task 3)
- `paint_single_page_without_body_returns_early` (Task 3)
- `paint_single_page_skips_zero_size_subtree` (Task 3)
- `paint_single_page_hello_world_emits_one_glyph_run` (Task 4)
- `paint_single_page_uses_inherited_color_as_brush` (Task 4)
- `paint_single_page_positions_glyphs_via_absolute_offset` (Task 4)
- `paint_single_page_skips_empty_text` (Task 4)
- `paint_single_page_can_be_called_multiple_times` (Task 5)

全 9 test pass、`test result: ok. 9 passed`。

- [ ] **Step 3: workspace 全体 build / test regression 確認**

Run: `cargo build --workspace 2>&1 | tail -3 && cargo test --workspace 2>&1 | grep -E '^test result|FAILED' | tail -40`
Expected: workspace build green (warning 0)、全 crate 全 test pass (FAILED なし)。

- [ ] **Step 4: clippy workspace lint 準拠 確認**

Run: `cargo clippy --workspace --all-targets 2>&1 | tail -20`
Expected: `Finished` で warning 0 (`-D warnings` gate)。もし raikiri-paint 側で warning が出たら (unused import / needless_borrow など) 実装で fix。

- [ ] **Step 5: acceptance criteria の目視確認**

Run: `bd show raikiri-spike-m1.7 --json 2>/dev/null | python3 -c "import json,sys; print(json.load(sys.stdin)[0].get('acceptance_criteria',''))" | head -30`
Expected: 13 checkbox の acceptance criteria がすべて満たされている状態。手動チェック:
1. `paint_single_page` pub export ✓ (Task 2)
2. walk.rs 5 関数 ✓ (Task 3)
3. text.rs 3 関数 ✓ (Task 4)
4. Cargo.toml deps ✓ (Task 2)
5. Node pub struct + 5 field pub ✓ (Task 1)
6. raikiri-dom `pub use node::Node` ✓ (Task 1)
7. Document 3 methods ✓ (Task 1)
8. 8 unit tests 全 pass ✓ (Task 3-5、9 個中 compile smoke は bonus)
9. `cargo build -p raikiri-paint -p raikiri-dom` green ✓ (Step 3)
10. `cargo test -p raikiri-paint -p raikiri-dom` green ✓ (Step 3)
11. `cargo clippy` green ✓ (Step 4)
12. blitz-paint / blitz-dom source 非参照 ✓ (Task 1-5 全体で self-enforce)
13. parley 0.10 実 API 差分吸収 ✓ (Task 4 Step 1 の補足で対応)

- [ ] **Step 6: Commit**

```bash
git add crates/raikiri-paint/src/lib.rs
git commit -m "$(cat <<'EOF'
test(raikiri-paint): re-entrance safety pin + 9-test suite complete (raikiri-spike-m1.7)

paint_single_page_can_be_called_multiple_times pins that repeated paint calls on
the same (Document, CascadeResult) produce identical command sequences — future
paint-side cache introductions must preserve this to avoid silent regressions.

Full 9-test suite (8 acceptance-mandated + 1 bonus compile smoke) green,
workspace build/test/clippy all green. M1.7 acceptance criteria satisfied.

Claude-Session: https://claude.ai/code/session_01Xzb6RqUhDy9rgT65ZF1R5V
EOF
)"
```

---

## Self-Review Notes

**Spec coverage**: 各 Section の要件が Task に対応:
- Section 1 (Scope + data flow) → Task 3 (walk 骨格) + Task 4 (text 描画)
- Section 2 (Public API surface) → Task 2 (pub entry + Cargo.toml + module 宣言)
- Section 3 (raikiri-dom pub 化) → Task 1 (Node pub + Document accessors)
- Section 4 (Paint walker) → Task 3 (canvas fill site + find_body + 3-fn dispatch)
- Section 5 (Text draw pipeline) → Task 4 (draw_text_node + helpers)
- Section 6 (Testing、8 tests) → Task 3 (3 test) + Task 4 (4 test) + Task 5 (1 test) + Task 2 (1 compile smoke bonus)
- Section 7 (Deferred / rejected) → 実装 tasks の scope 外に明示化、code comment で M2+/M4 の pointer を残す

**Type consistency check**: 各 task の関数 signature が cross-task で一致:
- `paint_single_page(scene, doc, cascade, page_box)` — Task 2 で定義、Task 5 まで不変
- `paint_canvas_background(scene, doc, cascade, page_box)` — Task 2 stub、Task 3 実 (署名同一)
- `paint_document(scene, doc, cascade)` — Task 2 stub、Task 3 実 (署名同一)
- `paint_element(scene, doc, cascade, node_id, parent_abs_x, parent_abs_y)` — Task 3 で新設
- `paint_node(...)` — Task 3 で新設、signature = paint_element 同型
- `draw_text_node(scene, node, cascade, node_id, abs_x, abs_y)` — Task 2 stub、Task 4 実 (署名同一)
- `Document::get_node(id) -> Option<&Node>` — Task 1 で定義、Task 2/3 で消費
- `Node.text_layout / .unrounded_layout / .children / .kind / .tag_name` — Task 1 で pub 化、Task 3/4 で消費

**Placeholder scan**: 完全 code / commands / expected output あり、"TBD" / "TODO" / "adjust later" は不在 (parley API mismatch fallback の "Step 1 補足" は plan の一部として明示的 fallback 手順)。

**No spec gap**: Design の Deferred / Rejected は Task では実装しない (defer が正)、Open questions / risks は implementation 時判断で対応 (plan に折り込み済)。
